---
phase: 01-migration-safety-net-foundation
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-migration-safety-net-foundation/01-URL-INVENTORY.md
  - .planning/STATE.md
autonomous: true
requirements: [MIGR-01]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All 16 verified filenames from site-current/ appear in 01-URL-INVENTORY.md with a non-blank disposition value (keep-as-is / retire-with-redirect / decision-pending) - none left empty (spec-less edge probe, empty category)."
    - "The inventory lists pages in the same stable order as the site-current/ directory listing already verified in 01-RESEARCH.md; order carries no ranking/priority meaning beyond readability (spec-less edge probe, ordering category)."
    - "Adjacency/merge semantics do not apply to this flat 16-page inventory - each filename is an independently-dispositioned entry, never merged or compared against another (spec-less edge probe, adjacency category, not applicable to this requirement's nature)."
    - "test-laptop.html is explicitly marked keep-as-is, citing DIFF-01's requirement to surface the self-diagnostic tool more prominently - not left as a default/ambiguous disposition despite 01-RESEARCH.md Pitfall D flagging it as borderline."
    - "covid.html and problem-stari.html are both explicitly marked decision-pending (not silently retired) and cross-referenced as new entries in STATE.md's Blockers/Concerns section."
    - "The inventory explicitly labels its cross-check method as a site-current mirror plus public-search substitute, not a real Google Search Console export, per D-08/D-09's deferral."
  artifacts:
    - path: ".planning/phases/01-migration-safety-net-foundation/01-URL-INVENTORY.md"
      provides: "Per-URL disposition table for all 16 pages plus the 7 must-carry root files and 4 root directories, explicitly labeled as GSC-substitute data"
  key_links:
    - from: ".planning/phases/01-migration-safety-net-foundation/01-URL-INVENTORY.md"
      to: ".planning/STATE.md"
      via: "Borderline-page decision-pending rows are cross-referenced as new Blockers/Concerns entries"
      pattern: "decision-pending"

  prohibitions:
    - statement: "MUST NOT mark any of the 16 live pages as retired unilaterally - borderline pages (covid.html, problem-stari.html) must be recorded as pending-owner-decision, not silently resolved by this inventory process, since retiring an indexed URL without sign-off risks the exact ranking loss this phase exists to prevent."
      status: resolved
      verification: judgment
    - statement: "MUST NOT present the site-mirror plus public-search substitute inventory as if it were a real Search-Console-verified cross-check - the data source must be explicitly labeled as a substitute pending GSC access (D-09), to avoid false confidence that MIGR-01's GSC cross-check already happened."
      status: resolved
      verification: judgment
---

<objective>
Build the complete, per-page-dispositioned URL inventory for all 16 live pages (MIGR-01), using the site-current/ mirror plus a public-search substitute in place of the currently-unavailable Google Search Console access (D-08/D-09), and record the two genuinely-undecided pages (covid.html, problem-stari.html) as explicit open decisions rather than silent defaults.

Purpose: this document is the single source of truth every later phase's URL/redirect/content decision checks against - an incomplete or silently-resolved inventory here becomes a broken link or a lost ranking three phases from now.
Output: `01-URL-INVENTORY.md`, plus two new entries in STATE.md's Blockers/Concerns.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-migration-safety-net-foundation/01-CONTEXT.md
@.planning/phases/01-migration-safety-net-foundation/01-RESEARCH.md
@.planning/phases/01-migration-safety-net-foundation/01-PATTERNS.md
@site-current/
</context>

<tasks>

<task type="auto">
  <name>Build the 16-page + must-carry-file URL inventory with per-page disposition</name>
  <read_first>01-RESEARCH.md "Code Examples" section (verified 16-page listing + must-carry root files), 01-RESEARCH.md "Common Pitfalls" Pitfall D (borderline pages), 01-PATTERNS.md URL inventory section, REQUIREMENTS.md DIFF-01 and CONTENT-02 (rationale for test-laptop.html and covid.html), site-current/ root directory listing (confirm no drift since RESEARCH.md was written)</read_first>
  <files>.planning/phases/01-migration-safety-net-foundation/01-URL-INVENTORY.md</files>
  <action>
1. Re-run a fresh top-level directory listing of site-current/ and diff it against 01-RESEARCH.md's verified 16-filename list to confirm no drift since research was done.
2. Create 01-URL-INVENTORY.md with a header section explicitly labeling the cross-check method: GSC access is unavailable this session (D-08/D-09); this inventory substitutes a direct site-current/ mirror listing plus a public site:torin.bg search check, and must be retrofitted with real GSC Pages-export data once access is obtained.
3. Attempt a public web search for "site:torin.bg" as the D-09 substitute signal and record whatever is observed - if the query cannot be executed or the result is inconclusive, state that explicitly rather than fabricating a result.
4. Build the main table: one row per each of the 16 verified filenames, columns Filename | Full URL (https://torin.bg/<filename>) | Disposition | Rationale. Disposition is keep-as-is for every page except covid.html and problem-stari.html (decision-pending) and test-laptop.html (keep-as-is, rationale citing DIFF-01's homepage-surfacing requirement).
5. Build a second table for the 7 non-page root files (.htaccess, favicon.ico, google1718743335455f1c.html, header.js, otpuska.js, mailer.php, error_log) and the 4 root directories (.well-known/, cgi-bin/, covid-19/, assets1/), each marked must-carry - out of this phase's scope to migrate, but recorded now for Phase 4's cutover checklist (MIGR-02) to consume.
  </action>
  <verify>
    <automated>for f in index.html about.html covid.html laptopi.html mehanichni-problemi.html msg.html optimizatsiq.html problem-stari.html profilaktika-laptop.html rezervni-chasti.html test-laptop.html tokov-udar.html uslovia.html warrently.html za-bateriite.html zalivane-technosti.html; do grep -q "$f" .planning/phases/01-migration-safety-net-foundation/01-URL-INVENTORY.md || echo "MISSING $f"; done</automated>
  </verify>
  <acceptance_criteria>
- 01-URL-INVENTORY.md contains all 16 filenames, each with a non-blank Disposition cell.
- The document's header explicitly states the GSC-substitute labeling sentence.
- test-laptop.html's row cites DIFF-01 in its Rationale column.
- covid.html and problem-stari.html are both marked decision-pending, not keep-as-is or retire.
  </acceptance_criteria>
  <done>01-URL-INVENTORY.md exists, covers all 16 pages plus must-carry files, and is explicitly labeled as GSC-substitute data.</done>
</task>

<task type="auto">
  <name>Record borderline-page decisions as explicit pending-owner-decision blockers in STATE.md</name>
  <read_first>.planning/STATE.md (existing Blockers/Concerns section - append, do not overwrite), 01-URL-INVENTORY.md (just created)</read_first>
  <files>.planning/STATE.md</files>
  <action>
1. Edit .planning/STATE.md's "### Blockers/Concerns" section, appending two new bullet items: one for covid.html's pending fate (cross-referencing CONTENT-02 and Phase 3, since its content is slated to move to the About page), one for problem-stari.html's pending fate (no requirement currently addresses it; content overlap with mehanichni-problemi.html suspected - flag for owner review).
2. Confirm test-laptop.html is NOT added as a blocker - it is already resolved to keep-as-is per DIFF-01, so only the two genuinely-undecided pages get blocker entries.
  </action>
  <verify>
    <automated>grep -c "problem-stari.html" .planning/STATE.md</automated>
  </verify>
  <verify>
    <automated>grep -c "covid.html" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
- STATE.md's Blockers/Concerns section contains new entries for covid.html and problem-stari.html, each citing which future phase/requirement should resolve them.
- The pre-existing blocker rows in STATE.md (Phase 1/3/4 items already listed) are untouched - git diff shows only additions to this section.
  </acceptance_criteria>
  <done>Borderline page dispositions are traceable in STATE.md as explicit, non-silent pending decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| This document -> future-phase decisions | Later phases (2-4) read this inventory as authoritative input for redirect/content decisions - an error here propagates silently downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Information Disclosure | 01-URL-INVENTORY.md content | low | accept | Document contains only publicly-known filenames and disposition notes already visible on the live site; no credentials or PII involved - safe to keep in the (currently local, soon-to-be-private per plan 01-04) repository. |
| T-01-07 | Tampering (business-impact integrity) | Page disposition decisions (retire vs keep) | high | mitigate | Borderline pages (covid.html, test-laptop.html, problem-stari.html) are each given an explicit, requirement-backed or pending disposition rather than a default - prevents an unreviewed change from silently deindexing a page that currently ranks (directly enforced by this plan's prohibition items). |

No npm/pip/cargo installs occur in this phase - the generic T-{phase}-SC row is not applicable.
</threat_model>

<verification>
- 01-URL-INVENTORY.md exists and lists all 16 pages with a disposition and the 7 must-carry root files.
- STATE.md's Blockers/Concerns section has 2 new, clearly-labeled entries and no removed content.
</verification>

<success_criteria>
MIGR-01's URL inventory exists, is cross-checked to the extent possible without GSC access, and makes zero unilateral page-retirement decisions.
</success_criteria>

<output>
Create `.planning/phases/01-migration-safety-net-foundation/01-02-SUMMARY.md` when done.
</output>
