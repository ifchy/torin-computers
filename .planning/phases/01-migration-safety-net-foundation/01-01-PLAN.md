---
phase: 01-migration-safety-net-foundation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/includes/site-config.php
  - src/includes/header.php
  - src/includes/footer.php
  - src/.htaccess
  - src/index.html
  - src/phptest.html
  - .planning/STATE.md
autonomous: true
requirements: [SEO-04]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Requesting https://torin.bg/new/index.html returns rendered HTML containing the real site title (TORIN KOMPYUTARS, Cyrillic), proving header.php/footer.php includes executed via PHP rather than being returned as unparsed source (SEO-04 foundation, D-03)."
    - "The four URL/protocol variants (http/https x www/non-www) each canonicalize to https://torin.bg/new/... in exactly 1 redirect hop against the /new/ rehearsal .htaccess; the already-canonical https://torin.bg/new/... variant returns 200 with 0 redirects (D-06, D-07)."
    - "public_html/new/ responses carry an X-Robots-Tag noindex header, keeping the staging subtree out of Google's index without touching the live root robots.txt (RESEARCH.md Pitfall C)."
    - "The temporary phptest.html spike probe returns 404 after the PHP-version+execution check is confirmed (ASVS V4 - no lingering phpinfo-style disclosure)."
    - "Ordering has no meaningful analog for a single canonical-URL redirect target - SEO-04's guarantee is per-request identity, not a sequence with comparable elements (spec-less edge probe, ordering category, not applicable to this requirement's nature)."
    - "STATE.md's existing PHP-version blocker is updated to reflect the live-reconfirmed PHP 5.2.17 result, plus the open question of whether a cPanel/control-panel login exists separately from the FTP-only credentials, needed before Phase 4 locks in a PHPMailer version (RESEARCH.md Pitfall B / Open Question 1)."
  artifacts:
    - path: "src/includes/site-config.php"
      provides: "Single-source-of-truth PHP 5.2-safe array of site values (phone, email), sourced only from verified site-current content"
    - path: "src/includes/header.php"
      provides: "Shared head+contact-info chrome, PHP 5.2-safe, extracted from site-current/index.html lines 1-80"
    - path: "src/includes/footer.php"
      provides: "Shared footer+script-include chrome with dynamic copyright year (date function), replacing the hardcoded stale year"
    - path: "src/.htaccess"
      provides: "PHP-as-.html AddType/AddHandler directive, hardcoded-target canonicalization RewriteRule, and X-Robots-Tag noindex header, scoped to /new/"
    - path: "src/index.html"
      provides: "First live-verified page skeleton using the include pattern end-to-end"
  key_links:
    - from: "src/index.html"
      to: "src/includes/header.php"
      via: "require_once(dirname(__FILE__) . '/includes/header.php') - PHP 5.2-safe, no __DIR__"
      pattern: "require_once.*includes/header\\.php"
    - from: "src/includes/header.php"
      to: "src/includes/site-config.php"
      via: "require_once for the $site array, referenced as $site['phone'] / $site['email']"
      pattern: "site-config\\.php"
---

<objective>
Prove, end-to-end on the real live host (bell.host.bg), that the PHP-include foundation works: a page can keep its exact live filename/extension while being parsed as PHP, shared header/footer chrome renders through includes, and the staging subfolder (`public_html/new/`, per D-01/D-02) is safely canonicalized and kept out of search results while the rebuild is in progress (D-06/D-07).

Purpose: This is the phase's tracer bullet - every later page (Plan 01-05) and every later phase (2-4) inherits whatever this task proves works. Getting the `.htaccess` handler directive, the PHP 5.2-safe include pattern, and the canonicalization rule right here, on the real host, prevents a fatal-error or open-redirect discovery three phases from now.
Output: `src/includes/{site-config,header,footer}.php`, `src/.htaccess`, `src/index.html`, all live-verified at `https://torin.bg/new/index.html`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-migration-safety-net-foundation/01-CONTEXT.md
@.planning/phases/01-migration-safety-net-foundation/01-RESEARCH.md
@.planning/phases/01-migration-safety-net-foundation/01-PATTERNS.md
@site-current/index.html
@site-current/mailer.php
@site-current/.htaccess
@filezilla-server-data.xml
</context>

<tasks>

<task type="tracer">
  <name>Tracer: PHP-include foundation wired end-to-end on the live host</name>
  <reversibility rating="reversible">D-03/D-06/D-07 are explicitly rated reversible in 01-CONTEXT.md - isolated to the /new/ subfolder, no production impact, redirect rules easy to adjust. No checkpoint needed.</reversibility>
  <read_first>site-current/index.html (lines 1-48 head/meta block and the contact-info block ~lines 48-80 - exact markup to extract, per 01-PATTERNS.md), site-current/mailer.php (confirms bare PHP-tag execution baseline and the office@torin.bg address), site-current/.htaccess (confirms today's cPanel stub has zero active rules), 01-PATTERNS.md sections "includes/header.php", "includes/footer.php" and "includes/site-config.php", 01-RESEARCH.md Patterns 1-3 and Pitfalls A-D, filezilla-server-data.xml (bell.host.bg Server entry - Host/User/Pass/Protocol fields; decode Pass from base64 at runtime, never print or hardcode it into any committed file)</read_first>
  <files>src/includes/site-config.php, src/includes/header.php, src/includes/footer.php, src/.htaccess, src/index.html, src/phptest.html</files>
  <action>
Per D-01/D-02: all work targets the `public_html/new/` subfolder on bell.host.bg, uploaded by Claude via curl (the user only ever visits the URL to check progress).

1. Confirm FTP connectivity before writing anything: decode the bell.host.bg Pass value from filezilla-server-data.xml (base64) into a shell variable only (never write it to a file or print it). Try `curl --ftp-ssl -v` against ftp://bell.host.bg/public_html/ with the torin user first (T-01-05: prefer encrypted transport); fall back to plain `ftp://bell.host.bg/public_html/` only if the TLS handshake is rejected. Confirm a directory listing comes back before proceeding, and record in the SUMMARY which mode worked.
2. Create `public_html/new/` and `public_html/new/includes/` on the host via curl's FTP MKD if they do not already exist.
3. Write src/includes/site-config.php: PHP 5.2-safe (array(), no closures, no namespaces, full `<?php ... ?>` tags only - never short-echo). Holds one `$site` array with exactly two keys, `phone` and `email`, values taken verbatim from the read_first sources (phone from index.html's contact block; email from mailer.php's office@torin.bg). Do not add an `hours` or any other field - no verified source exists for it.
4. Write src/includes/header.php: `require_once(dirname(__FILE__) . '/site-config.php')` (not `__DIR__`, unavailable on PHP 5.2), then the head block (doctype, charset, viewport, the real page title, favicon link) and the contact-info block referencing `$site['phone']` via full `<?php echo ...; ?>` - extracted from the read_first index.html slice, not invented markup.
5. Write src/includes/footer.php: the closing footer markup and script includes from the read_first index.html tail, replacing the hardcoded stale copyright year with a dynamic `<?php echo date("Y"); ?>` call (01-PATTERNS.md flags the hardcoded year as stale content this config layer should fix).
6. Write src/index.html containing only two require_once calls (header.php, footer.php, both via `dirname(__FILE__)`) plus a minimal placeholder body. This is the only page this task builds - the other 15 filenames are plan 01-05's job.
7. Write src/.htaccess with three directive groups, in this order: (a) `RewriteEngine On` plus ONE combined RewriteRule using `RewriteCond ... [OR]` to catch "HTTPS off" OR "Host is www.torin.bg", redirecting to the hardcoded literal target `https://torin.bg/new/$1` with `[R=301,L]` - deliberately NOT reflecting `%{HTTP_HOST}` into the redirect target the way 01-RESEARCH.md's example does, because echoing the client-supplied Host header into a redirect Location is a host-header-injection/open-redirect vector (T-01-03); the hardcoded single-rule form also keeps every one of the four variants to exactly one hop instead of the two-hop chain the original two-rule pattern would produce for the http+www case; (b) an `<IfModule mod_headers.c>` block setting the X-Robots-Tag response header to noindex/nofollow; (c) the `AddType application/x-httpd-php .html .htm` directive. Note in the SUMMARY that at Phase 4 cutover promotion to the root `.htaccess`, the redirect target changes from `https://torin.bg/new/$1` to `https://torin.bg/$1` (drop the /new/ segment).
8. Write src/phptest.html containing exactly one line of PHP that echoes a fixed marker string concatenated with the phpversion() function's return value.
9. Upload src/.htaccess, src/includes/site-config.php, src/includes/header.php, src/includes/footer.php, src/index.html, and src/phptest.html to their mirrored paths under `public_html/new/` via curl -T, using the connectivity mode confirmed in step 1.
10. Request phptest.html live. If the response is the literal unexecuted PHP source instead of the expected marker+version output, replace the AddType directive with `AddHandler application/x-httpd-php5 .html .htm`, re-upload src/.htaccess, and retest; if still failing, try `AddHandler php-script .html .htm`. Record in the SUMMARY which directive actually worked on bell.host.bg.
11. Edit .planning/STATE.md's existing "[Phase 1]: PHP version and Composer availability..." blocker line under Blockers/Concerns: confirm PHP 5.2.17 is now live-reconfirmed (not just header-inferred), and append the open question of whether a cPanel/control-panel login exists separately from the FTP-only credentials in filezilla-server-data.xml - needed before Phase 4 selects a PHPMailer version compatible with PHP 5.2 (01-RESEARCH.md Pitfall B / Open Question 1). Append only - do not remove or reorder the other existing blocker rows.
  </action>
  <verify>
    <automated>curl -s https://torin.bg/new/phptest.html | grep -c "PHP-IN-HTML-OK"</automated>
  </verify>
  <verify>
    <automated>curl -s https://torin.bg/new/index.html | grep -c "ТОРИН КОМПЮТЪРС"</automated>
  </verify>
  <acceptance_criteria>
- curl against https://torin.bg/new/phptest.html outputs the fixed marker string followed by a real PHP version string (confirms which AddType/AddHandler directive works on bell.host.bg).
- curl against https://torin.bg/new/index.html contains the real site title text, proving header.php rendered through the live PHP-as-.html handler rather than being served as raw source.
- src/includes/site-config.php, header.php, footer.php use array() for the $site value, contain no closures, and no namespace declarations.
- No FTP password value appears in plaintext in any committed file (git diff of this task's changes contains no credential string).
- .planning/STATE.md's PHP-version blocker line is updated and the cPanel-login open question is appended, with all pre-existing blocker rows untouched.
  </acceptance_criteria>
  <done>The PHP-include mechanism is proven end-to-end on bell.host.bg: index.html renders through header.php/footer.php via a confirmed-working AddType/AddHandler directive, and the PHP-version blocker in STATE.md is refreshed with the live-confirmed result.</done>
</task>

<task type="auto">
  <name>Verify canonicalization + noindex header across all 4 URL variants, then remove the spike probe</name>
  <read_first>src/.htaccess (just written by the tracer task), 01-RESEARCH.md "Live host verification results" (baseline curl output for the 4 root-domain variants, for comparison), 01-CONTEXT.md D-06/D-07 (canonical target: https://torin.bg, non-www, HTTPS)</read_first>
  <files>src/.htaccess</files>
  <action>
1. Run `curl -sIL` (or `curl -o /dev/null -s -w '%{num_redirects} %{url_effective}\n'`) against all four /new/index.html variants: http://torin.bg/new/index.html, https://torin.bg/new/index.html, http://www.torin.bg/new/index.html, https://www.torin.bg/new/index.html. Confirm the first three each land on https://torin.bg/new/index.html in exactly 1 redirect hop, and the fourth (already-canonical) returns http_code 200 with 0 redirects.
2. Run `curl -sI https://torin.bg/new/index.html` and confirm the response headers include an X-Robots-Tag value of noindex - if mod_headers is not enabled on this host (header absent), record this explicitly in the SUMMARY as an open follow-up for Phase 4 (assumption A3 in 01-RESEARCH.md) rather than silently assuming it worked.
3. Delete the spike probe from the host only (not the local repo) using curl's FTP DELE command against `/public_html/new/phptest.html`, over the same connectivity mode confirmed in the tracer task.
4. Keep src/phptest.html in the local repo as a documented, non-deployed reference of the spike method - add a one-line comment at its top noting it is not uploaded to the live site.
  </action>
  <verify>
    <automated>curl -o /dev/null -s -w "%{http_code}" https://torin.bg/new/phptest.html</automated>
  </verify>
  <verify>
    <automated>curl -sI https://torin.bg/new/index.html | grep -ci "x-robots-tag"</automated>
  </verify>
  <acceptance_criteria>
- All three non-canonical variants (http/https x www) redirect to https://torin.bg/new/... in exactly 1 hop each.
- https://torin.bg/new/index.html itself returns 200 directly with 0 redirects.
- https://torin.bg/new/phptest.html returns 404 after deletion.
- The X-Robots-Tag response header is present, or its absence is explicitly documented in the SUMMARY as an open A3 follow-up if mod_headers is unavailable on this host.
  </acceptance_criteria>
  <done>Canonicalization rehearsal and staging de-index are both live-verified against bell.host.bg, and the debug spike probe is removed from the live host.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Internet -> public_html/new/ (Apache/PHP 5.2.17) | Any visitor or crawler can request any file under /new/ once uploaded - untrusted network access to a pre-launch staging area on the production domain |
| Developer machine -> bell.host.bg (FTP) | Credential-bearing FTP session used for every upload this phase performs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | src/phptest.html spike probe (phpversion() output) | medium | mitigate | Delete phptest.html from the host immediately after the spike confirms PHP execution (Task 2); verify 404 via curl before considering the plan complete. |
| T-01-02 | Information Disclosure | public_html/new/ staging subfolder | medium | mitigate | `Header set X-Robots-Tag "noindex, nofollow"` scoped to /new/ via .htaccess, not a robots.txt (robots.txt is ignored outside true domain root - 01-RESEARCH.md Pitfall C); verified via curl -I in Task 2. |
| T-01-03 | Tampering (open redirect / host-header injection) | .htaccess canonicalization RewriteRule | high | mitigate | The redirect target is a hardcoded literal (`https://torin.bg/new/$1`), never `%{HTTP_HOST}` reflection - a request with a spoofed Host header cannot be redirected anywhere the operator did not choose. Verified via curl against the 4 known variants only (Task 2). |
| T-01-04 | Information Disclosure | Misconfigured AddType/AddHandler serving PHP as plain text | medium | mitigate | The spike test (Task 1, step 10) explicitly confirms PHP is EXECUTING (marker + version string in the response) before any real include file is uploaded; if raw source is ever returned, the task halts and iterates the handler directive rather than proceeding. |
| T-01-05 | Tampering / Information Disclosure | FTP credential transport to bell.host.bg | medium | mitigate | Every curl FTP operation this task introduces attempts `--ftp-ssl` (explicit TLS upgrade) first, falling back to plain FTP only if the host rejects TLS; whichever mode succeeds is recorded in the SUMMARY. |

No npm/pip/cargo installs occur in this phase (01-RESEARCH.md's Package Legitimacy Audit is explicitly "not applicable") - the generic T-{phase}-SC row is omitted as not applicable.
</threat_model>

<verification>
- `curl -s https://torin.bg/new/phptest.html` contains the expected marker + PHP version, then returns 404 after Task 2's cleanup.
- `curl -s https://torin.bg/new/index.html` renders the real site title and current-year footer via the includes.
- All four URL/protocol variants against /new/index.html canonicalize correctly (1 hop or 0 hops as appropriate) with X-Robots-Tag present.
</verification>

<success_criteria>
The PHP-include foundation, the .htaccess handler directive, and the canonicalization + de-index rules are all proven live on bell.host.bg - the exact mechanism every later page and phase depends on.
</success_criteria>

<output>
Create `.planning/phases/01-migration-safety-net-foundation/01-01-SUMMARY.md` when done.
</output>
