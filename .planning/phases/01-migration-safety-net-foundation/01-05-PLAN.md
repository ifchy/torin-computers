---
phase: 01-migration-safety-net-foundation
plan: 5
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/about.html
  - src/covid.html
  - src/laptopi.html
  - src/mehanichni-problemi.html
  - src/msg.html
  - src/optimizatsiq.html
  - src/problem-stari.html
  - src/profilaktika-laptop.html
  - src/rezervni-chasti.html
  - src/test-laptop.html
  - src/tokov-udar.html
  - src/uslovia.html
  - src/warrently.html
  - src/za-bateriite.html
  - src/zalivane-technosti.html
autonomous: true
requirements: [SEO-04]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Each of the 16 scaffolded page files under public_html/new/ (index.html from plan 01-01 plus the 15 built here) is non-empty and returns rendered HTML - not a 0-byte file or raw unparsed PHP source - when requested via curl (spec-less edge probe, empty category)."
    - "New page filenames under public_html/new/ are byte-for-byte identical, including case, to the corresponding filenames in site-current/ - Apache/Linux filename matching is case-sensitive and byte-exact, not normalized (spec-less edge probe, encoding category)."
    - "Ordering has no meaningful analog for a set of preserved filenames - SEO-04's guarantee is per-URL identity, not a sequence with comparable elements (spec-less edge probe, ordering category, not applicable to this requirement's nature)."
  artifacts:
    - path: "src/about.html"
      provides: "Filename-preserved page skeleton using the proven include pattern"
    - path: "src/covid.html"
      provides: "Filename-preserved page skeleton; disposition decision-pending per 01-URL-INVENTORY.md, not resolved here"
    - path: "src/laptopi.html"
      provides: "Filename-preserved page skeleton using the proven include pattern"
    - path: "src/mehanichni-problemi.html"
      provides: "Filename-preserved page skeleton using the proven include pattern"
    - path: "src/msg.html"
      provides: "Filename-preserved page skeleton using the proven include pattern"
    - path: "src/optimizatsiq.html"
      provides: "Filename-preserved page skeleton using the proven include pattern"
    - path: "src/problem-stari.html"
      provides: "Filename-preserved page skeleton; disposition decision-pending per 01-URL-INVENTORY.md, not resolved here"
    - path: "src/profilaktika-laptop.html"
      provides: "Filename-preserved page skeleton using the proven include pattern"
    - path: "src/rezervni-chasti.html"
      provides: "Filename-preserved page skeleton using the proven include pattern"
    - path: "src/test-laptop.html"
      provides: "Filename-preserved page skeleton; disposition keep-as-is per DIFF-01 (self-diagnostic tool)"
    - path: "src/tokov-udar.html"
      provides: "Filename-preserved page skeleton using the proven include pattern"
    - path: "src/uslovia.html"
      provides: "Filename-preserved page skeleton using the proven include pattern"
    - path: "src/warrently.html"
      provides: "Filename-preserved page skeleton using the proven include pattern"
    - path: "src/za-bateriite.html"
      provides: "Filename-preserved page skeleton using the proven include pattern"
    - path: "src/zalivane-technosti.html"
      provides: "Filename-preserved page skeleton using the proven include pattern"
  key_links:
    - from: "src/about.html (and its 14 siblings)"
      to: "src/includes/header.php"
      via: "require_once(dirname(__FILE__) . '/includes/header.php') - identical pattern established by plan 01-01's index.html"
      pattern: "require_once.*includes/header\\.php"

  prohibitions:
    - statement: "MUST NOT rename, restructure, or clean up any of the 16 live page filenames or extensions during template scaffolding, even where a cleaner name seems preferable - SEO-04's guarantee is byte-identical URLs, not improved ones."
      status: resolved
      verification: judgment
---

<objective>
Expand the include-based page pattern proven by plan 01-01's tracer to the remaining 15 live filenames, so that all 16 of site-current/'s pages exist under `public_html/new/` with byte-identical filenames - directly delivering SEO-04's "no visitor-facing link changes" success criterion.

Purpose: the tracer proved the mechanism works for one page; this plan proves it holds for the whole site's URL surface before any real content work (Phase 2/3) begins.
Output: 15 new filename-preserved skeleton pages, live-verified alongside index.html.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-migration-safety-net-foundation/01-RESEARCH.md
@.planning/phases/01-migration-safety-net-foundation/01-URL-INVENTORY.md
@site-current/
</context>

<tasks>

<task type="auto">
  <name>Scaffold the remaining 15 page files using the proven include pattern</name>
  <precondition>src/includes/header.php, src/includes/footer.php, and src/includes/site-config.php exist and were live-verified end-to-end by plan 01-01's tracer task</precondition>
  <read_first>plan 01-01's src/index.html (the proven template pattern to replicate), .planning/phases/01-migration-safety-net-foundation/01-URL-INVENTORY.md (per-page disposition/rationale, read-only reference - no dependency on plan 01-02 itself since this is a documentation file), site-current/ per-page &lt;title&gt; tags (the real page titles to reuse as placeholder content rather than inventing new ones)</read_first>
  <files>src/about.html, src/covid.html, src/laptopi.html, src/mehanichni-problemi.html, src/msg.html, src/optimizatsiq.html, src/problem-stari.html, src/profilaktika-laptop.html, src/rezervni-chasti.html, src/test-laptop.html, src/tokov-udar.html, src/uslovia.html, src/warrently.html, src/za-bateriite.html, src/zalivane-technosti.html</files>
  <action>
This is a single, homogeneous templated-scaffold task by design: all 15 files repeat the exact two-include pattern plan 01-01 already proved works, differing only in the placeholder title text - low per-file complexity, not a multi-subsystem task.

1. For each of the 15 remaining filenames, extract that file's existing &lt;title&gt; text from the matching site-current/&lt;filename&gt; via a simple text search.
2. Create src/&lt;filename&gt; with the same two-require_once skeleton as plan 01-01's src/index.html (header.php, footer.php, both via `dirname(__FILE__)`), plus a minimal placeholder body containing a heading with the extracted real title text - do not port full page content (that is Phase 3's job) and do not alter the filename or extension from what site-current/ uses.
3. Do not implement any redirect logic for covid.html or problem-stari.html despite their decision-pending disposition in 01-URL-INVENTORY.md - this phase's scope is filename/URL preservation only; the actual keep/retire execution is a later-phase decision.
  </action>
  <verify>
    <automated>ls src/*.html | wc -l</automated>
  </verify>
  <acceptance_criteria>
- src/ contains exactly 16 .html files (index.html from plan 01-01 plus these 15).
- The filename set in src/ is identical to the filename set in site-current/ (same 16 names, same case, same extension).
- Each new file's placeholder heading text matches that page's real site-current &lt;title&gt; (not invented text).
  </acceptance_criteria>
  <done>All 16 live filenames exist locally under src/ using the proven include pattern.</done>
</task>

<task type="auto">
  <name>Upload and live-verify all 16 pages preserve their exact URLs</name>
  <read_first>the previous task's output (src/*.html), plan 01-01's confirmed FTP connectivity mode (--ftp-ssl or plain, whichever worked)</read_first>
  <files>src/about.html, src/covid.html, src/laptopi.html, src/mehanichni-problemi.html, src/msg.html, src/optimizatsiq.html, src/problem-stari.html, src/profilaktika-laptop.html, src/rezervni-chasti.html, src/test-laptop.html, src/tokov-udar.html, src/uslovia.html, src/warrently.html, src/za-bateriite.html, src/zalivane-technosti.html</files>
  <action>
1. Upload the 15 newly-created files to `public_html/new/` on bell.host.bg via curl -T, using the same FTP connectivity mode confirmed working in plan 01-01.
2. Loop over all 16 filenames requesting https://torin.bg/new/&lt;filename&gt; via curl, collecting the HTTP status code and downloaded body size for each into a single report.
3. Confirm every one of the 16 returns http_code 200 and a body size greater than 0.
4. Diff the local src/*.html filename set against site-current/*.html one more time post-upload as the final SEO-04 proof, and record the result in the SUMMARY.
  </action>
  <verify>
    <automated>for f in index.html about.html covid.html laptopi.html mehanichni-problemi.html msg.html optimizatsiq.html problem-stari.html profilaktika-laptop.html rezervni-chasti.html test-laptop.html tokov-udar.html uslovia.html warrently.html za-bateriite.html zalivane-technosti.html; do code=$(curl -o /dev/null -s -w "%{http_code}" https://torin.bg/new/$f); test "$code" = "200" || echo "FAIL $f $code"; done</automated>
  </verify>
  <acceptance_criteria>
- All 16 URLs under https://torin.bg/new/ return HTTP 200 with non-zero body size.
- diff of the local src/*.html filename list against site-current/*.html produces no output.
  </acceptance_criteria>
  <done>All 16 live page URLs are preserved and live-verified under the /new/ staging subtree - SEO-04's guarantee holds for every page, not just the tracer's one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Internet -> the 15 new pages under public_html/new/ | Same staging-subfolder boundary plan 01-01 already established and mitigated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Tampering (data integrity) | Accidental filename drift across 15 near-identical files | medium | mitigate | Task 2 runs an automated diff of the uploaded filename set against site-current/'s filename set, catching any typo/rename introduced during the repetitive authoring step before it is considered done - directly protects SEO-04. |
| T-01-13 | Information Disclosure | Placeholder/stub pages temporarily live at predictable /new/ URLs | low | accept | Already mitigated at the whole /new/ subtree level by plan 01-01's X-Robots-Tag noindex rule (T-01-02) - no additional per-page control is needed; accepted as covered by the existing subtree-wide mitigation. |

No npm/pip/cargo installs occur in this phase - the generic T-{phase}-SC row is not applicable.
</threat_model>

<verification>
- All 16 pages exist locally under src/ and live at https://torin.bg/new/&lt;filename&gt; with HTTP 200.
- The filename set is byte-identical to site-current/'s filename set, both locally and post-upload.
</verification>

<success_criteria>
SEO-04's URL-preservation guarantee is proven for the site's complete 16-page URL surface, not just the single tracer page.
</success_criteria>

<output>
Create `.planning/phases/01-migration-safety-net-foundation/01-05-SUMMARY.md` when done.
</output>
