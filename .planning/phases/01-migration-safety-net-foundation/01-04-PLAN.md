---
phase: 01-migration-safety-net-foundation
plan: 4
type: execute
wave: 1
depends_on: []
files_modified:
  - .git/config
autonomous: false
requirements: [MIGR-03]

user_setup:
  - service: github
    why: "Off-site backup of the local git repository (D-04) protects against local machine loss; gh CLI is not installed/authenticated in this environment (D-05) so Claude cannot create the repo directly via API."
    dashboard_config:
      - task: "Create a new EMPTY private repository on github.com (leave README/license/.gitignore auto-init UNCHECKED, to avoid an unrelated-histories conflict on first push) and share its HTTPS or SSH clone URL"
        location: "github.com -> New repository -> Visibility: Private"

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "git remote -v shows an origin remote pointing at the user-provided private GitHub URL, and the remote's default branch HEAD matches the local main branch HEAD, once the checkpoint is resumed with a URL (D-04)."
  artifacts: []
  key_links: []
---

<objective>
Wire the local git repository (already the MIGR-03 source of truth) to a private GitHub remote, per D-04, as an off-site backup against local machine loss. Since `gh` CLI is not installed/authenticated (D-05), repo creation requires one human action; everything else is automated.

Purpose: local-only git history is a single point of failure. This plan closes that gap without gating the rest of the phase - the tracer, URL inventory, and backup-script plans in this same wave do not depend on this one.
Output: a private GitHub remote named `origin`, holding a full mirror of the local `main` branch.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-migration-safety-net-foundation/01-CONTEXT.md
@.gitignore
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Create the private GitHub repository (Claude cannot do this without gh CLI/API access)</name>
  <read_first>.planning/phases/01-migration-safety-net-foundation/01-CONTEXT.md D-04/D-05 (why this needs a human step and what "done" looks like)</read_first>
  <action>Create an empty private repository on github.com for this project and share its clone URL.</action>
  <instructions>
Claude has confirmed locally: git works, there are 6+ commits on `main`, no remote is currently configured (D-05), and `gh` (GitHub CLI) is not installed/authenticated in this environment, so Claude cannot create or authenticate a GitHub repo via API.

The one thing only you can do: go to github.com -> New repository, name it (e.g. torin-website), set Visibility to Private, and leave "Initialize this repository with a README" UNCHECKED (an auto-initialized repo has a commit that conflicts with this project's existing history on the first push). Then share the resulting clone URL.
  </instructions>
  <verification>Claude will run `git remote -v` after resuming to confirm the URL was recorded, and `git ls-remote &lt;url&gt;` to confirm the repo is reachable and empty before pushing.</verification>
  <acceptance_criteria>
- A GitHub clone URL (https://github.com/... or git@github.com:...) was provided in the resume signal.
  </acceptance_criteria>
  <resume-signal>Paste the repository's clone URL (https://github.com/... or git@github.com:...) to continue.</resume-signal>
</task>

<task type="auto">
  <name>Wire the GitHub remote and push, confirming private visibility and no leaked secrets</name>
  <precondition>A GitHub repository clone URL was provided via the previous task's resume signal</precondition>
  <read_first>.gitignore (confirm filezilla-server-data.xml and .DS_Store remain excluded), full local git history (to scan before the first push)</read_first>
  <files>.git/config</files>
  <action>
1. Run `git ls-remote &lt;provided-url&gt;` to confirm the repo exists and is empty (no existing refs) before pushing - if it already has commits (auto-init was not unchecked), stop and report back rather than force-pushing over it.
2. Scan the full local history for accidentally-committed secrets before the first push: confirm `git log --all --oneline -- filezilla-server-data.xml` returns no results, and search the full patch history (`git log -p --all`) for the literal marker string that appears only inside the FileZilla credential XML block, confirming it never leaked into any other committed file.
3. Add the remote: `git remote add origin &lt;provided-url&gt;`.
4. Push: `git push -u origin main`.
5. Confirm: `git remote -v` shows origin pointing at the provided URL, and `git ls-remote origin main` resolves to the same SHA as local `git rev-parse main`.
  </action>
  <verify>
    <automated>git remote -v | grep -c origin</automated>
  </verify>
  <verify>
    <automated>test "$(git rev-parse main)" = "$(git ls-remote origin main | cut -f1)" && echo MATCH</automated>
  </verify>
  <acceptance_criteria>
- `git remote -v` lists origin pointing at the exact URL provided in the checkpoint.
- The remote's main branch HEAD SHA matches the local main branch HEAD SHA.
- The secret-history scan in step 2 returned no results.
  </acceptance_criteria>
  <done>The local git repository is pushed to a private GitHub remote, serving as the D-04 off-site backup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local git history -> GitHub (new, external service) | The full commit history, previously local-only, becomes reachable from a third-party service for the first time this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Information Disclosure | Private GitHub repo visibility | high | mitigate | The checkpoint instructions explicitly require Visibility: Private during creation; Task 2 does not push until the checkpoint is resumed, and the SUMMARY records the confirmed-private status as reported by the user. |
| T-01-11 | Information Disclosure | Secrets committed to the new remote | critical | mitigate | Before the first `git push`, Task 2 scans the entire local history (not just the latest commit) for the FTP-credential file and its unique marker string; `.gitignore` already excludes filezilla-server-data.xml, but this re-verifies at push-time since the history predates this phase's own commits. |

No npm/pip/cargo installs occur in this phase - the generic T-{phase}-SC row is not applicable.
</threat_model>

<verification>
- `git remote -v` shows origin after the checkpoint is resumed.
- Local and remote `main` HEAD SHAs match.
- No secret marker string is found anywhere in the pushed history.
</verification>

<success_criteria>
D-04's off-site GitHub backup exists and mirrors the local repository exactly, without blocking any other Phase 1 plan.
</success_criteria>

<output>
Create `.planning/phases/01-migration-safety-net-foundation/01-04-SUMMARY.md` when done.
</output>
