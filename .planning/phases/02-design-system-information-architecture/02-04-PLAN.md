---
phase: 02-design-system-information-architecture
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/about.html
  - src/covid.html
  - src/laptopi.html
  - src/mehanichni-problemi.html
  - src/msg.html
  - src/optimizatsiq.html
  - src/problem-stari.html
  - src/profilaktika-laptop.html
  - src/rezervni-chasti.html
  - src/test-laptop.html
  - src/tokov-udar.html
  - src/uslovia.html
  - src/warrently.html
  - src/za-bateriite.html
  - src/zalivane-technosti.html
  - src/includes/category-page.php
  - src/css/components.css
autonomous: true
requirements: [SEO-02, DESIGN-01, IA-01]
user_setup: []

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Phase-goal truths (goal-backward) ---
    - "All sixteen live URLs render with the new responsive design system through the shared includes, and not one of them references jQuery, jQuery UI, ScrollMagic, pagePiling, Modernizr, the bundled vendor script, the legacy asset directory, or the Zendesk widget (DESIGN-01, success criterion 1)."
    - "Not one of the sixteen deployed responses, and not one source file under src/, references the Google Fonts CDN or any of the four legacy webfont files D-08 dropped — all typography resolves from the two self-hosted Sofia Sans subsets, and no stylesheet references a remote asset of any kind (D-08, D-06a)."
    - "All sixteen live URLs declare lang=bg — proven by fetching each deployed URL, not by observing the attribute in the shared include (SEO-02)."
    - "Every one of the sixteen pages returns its own distinct title, so the mechanism Phase 3 needs for per-page SEO metadata exists and is demonstrably wired, replacing the single hardcoded string all sixteen shared."
    - "The category page template renders its required spine and omits every optional block whose content is absent — an absent block emits nothing at all, never an empty heading, which is the thin-content shape the publish gate exists to prevent (D-24)."
    - "No page filename was added, renamed, or retired: the sixteen live URLs are byte-for-byte what Phase 1 locked (SEO-04 remains intact through the redesign)."
    - "No file under src/ carries a UTF-8 byte-order mark and every Bulgarian string round-trips through the live response without mojibake."
  artifacts:
    - path: "src/includes/category-page.php"
      provides: "PHP 5.2-safe D-24 category page template — renders the required spine and conditionally omits optional blocks whose content is absent"
      exports: ["torin_render_category_page"]
    - path: "src/mehanichni-problemi.html"
      provides: "Category 1 page rendered through the D-24 template — the thin-content shape that proves optional blocks omit cleanly"
    - path: "src/optimizatsiq.html"
      provides: "Category 3 page rendered through the D-24 template, including the cross-listed профилактика link"
    - path: "src/zalivane-technosti.html"
      provides: "Category 4 page rendered through the D-24 template — the deep-content shape that proves the spine scales"
    - path: "src/about.html"
      provides: "Per-page metadata assignment and design-system container, one of the fifteen-page mechanical pass"
  key_links:
    - from: "src/about.html"
      to: "src/includes/header.php"
      via: "require_once relative to the page file — the mechanism that makes lang=bg true site-wide"
      pattern: "require_once"
    - from: "src/mehanichni-problemi.html"
      to: "src/includes/category-page.php"
      via: "the page assigns its content array and calls the renderer"
      pattern: "torin_render_category_page"
    - from: "src/includes/category-page.php"
      to: "src/includes/categories.php"
      via: "title and symptom line read from the category record rather than retyped per page"
      pattern: "\\$torin_categories"
  prohibitions:
    - requirement_id: SEO-04
      category: safety
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT rename, add, or retire any visitor-facing page filename during the restyle — the two pages whose fate is genuinely undecided are awaiting owner sign-off, and quietly dropping an indexed URL during a design pass destroys ranking that took years to accumulate and is invisible in every design review."
---

<objective>
Roll the design system across the remaining fifteen pages, deliver the category page template
the six-category architecture needs, and prove the whole phase against the live site rather
than against the repository.

Purpose: everything before this plan is true on one page. Success criterion 1 says *every*
page renders with the new design system with no dependency on the legacy vendor stack, and
success criterion 4 says *every* page declares `lang="bg"`. Both are site-wide claims, and
02-RESEARCH Pitfall 1 is precisely the failure of proving them the cheap way — grepping for
vendor filenames finds nothing while ScrollMagic, Isotope, imagesLoaded, Velocity and jQuery UI
all still ship inside one bundled 530 KB file. The assertion has to be made against the
deployed response, on all sixteen URLs.

The per-page metadata pass is design-system plumbing, not SEO copy: SEO-01 belongs to Phase 3,
but the mechanism has to exist here or Phase 3 has nowhere to put its titles and descriptions.

**Standing guarantee this plan must not break:** SEO-04. The sixteen filenames are locked. This
plan restyles and restructures page *content*; it renames nothing, adds no visitor-facing URL,
and retires nothing. `covid.html` stays live and deliberately unlinked from the navigation
(D-35 — the EU grant publicity obligation has very likely lapsed, but the downside of being
wrong is an audit finding, and CONTENT-02 only requires the content stop competing for
attention on the homepage). `problem-stari.html` stays live pending owner sign-off.

Output: sixteen live URLs on the new design system, a working category template, and a
verification record good enough to hand to Phase 3.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-design-system-information-architecture/02-CONTEXT.md
@.planning/phases/02-design-system-information-architecture/02-UI-SPEC.md
@.planning/phases/02-design-system-information-architecture/02-PATTERNS.md
@.planning/phases/01-migration-safety-net-foundation/01-URL-INVENTORY.md
@.planning/phases/02-design-system-information-architecture/02-03-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by **this plan** (excluded from drift verification):

**Files:** `src/includes/category-page.php`.

**PHP symbols:** `torin_render_category_page()`, `$torin_page`. Recognised `$torin_page` keys:
`cat_id`, `intro`, `fixes`, `warranty`, `process`, `faq`, `related`, `prices`.

**CSS class names:** `.svc`, `.svc__intro`, `.svc__fixes`, `.svc__symptoms`, `.svc__warranty`,
`.svc__process`, `.svc__faq`, `.svc__related`, `.svc__prices`.

<tasks>

<task type="auto">
  <name>Task 1: The fifteen-page mechanical pass — per-page metadata and the design-system container</name>
  <files>src/about.html, src/covid.html, src/laptopi.html, src/mehanichni-problemi.html, src/msg.html, src/optimizatsiq.html, src/problem-stari.html, src/profilaktika-laptop.html, src/rezervni-chasti.html, src/test-laptop.html, src/tokov-udar.html, src/uslovia.html, src/warrently.html, src/za-bateriite.html, src/zalivane-technosti.html</files>
  <read_first>
    - `src/about.html` — the current 8-line stub. Thirteen of the fifteen are byte-identical to it, so this is one mechanical pass, not fifteen decisions
    - `src/index.html` — as built by plans 02-01 and 02-02: the exact metadata-assignment position above the header include, and the design-system container class that replaced the Bootstrap stub classes
    - `src/includes/header.php` — the per-page variables it consumes and the defaults it falls back to
    - `.planning/phases/01-migration-safety-net-foundation/01-URL-INVENTORY.md` — the sixteen locked URLs and the two whose disposition is still pending owner sign-off
    - `02-PATTERNS.md` §`src/index.html` and all 16 page templates — the three rules the wrapper encodes and the one structural change this phase adds
    - `02-RESEARCH.md` N-5 (why the metadata mechanism belongs here rather than in Phase 3), §6a (charset, encoding hygiene, and what SEO-02 needs beyond the attribute)
    - `site-current/` — the corresponding legacy page for each filename, read only far enough to name each page's subject correctly
  </read_first>
  <action>
Apply the same three-part edit to each of the fifteen page files. This is deliberately
mechanical: the wrapper is identical across all of them and any per-page judgement beyond the
title and description text is out of scope.

1. **Assign the per-page metadata above the header include**, in the position `index.html`
   already uses: set `$torin_title` and `$torin_desc` before `require_once` pulls in
   `header.php`. Keep the existing relative-to-file include idiom exactly — never a bare
   relative path, never `include`, and never the 5.3+ magic directory constant.

2. **Replace the Bootstrap-4 leftover classes on `<main>`** with the design-system container
   class. The `<main>` element itself stays; a page file contains `<main>` and nothing outside
   it — no head, no body, no footer, ever.

3. **Leave the existing placeholder body copy in place** on every page except the three the
   next task rebuilds. Page content is Phase 3 (CONTENT-01); this task delivers the chrome and
   the metadata mechanism only.

**Working titles.** Each must be distinct — sixteen identical titles is the defect being fixed,
and a mechanism that produces one string for every page proves nothing. Derive each from the
page's own subject as it exists in `site-current/`, suffixed with the shop name:

`about.html` За нас · `covid.html` Проект BG16RFOP002-2.073 · `laptopi.html` Употребявани
лаптопи · `mehanichni-problemi.html` Счупвания и механични повреди · `msg.html` Съобщение ·
`optimizatsiq.html` Оптимизация · `problem-stari.html` Чести проблеми ·
`profilaktika-laptop.html` Профилактика на лаптоп · `rezervni-chasti.html` Резервни части ·
`test-laptop.html` Тествай сам своя лаптоп · `tokov-udar.html` Токов удар и захранване ·
`uslovia.html` Общи условия · `warrently.html` Гаранционни условия · `za-bateriite.html` За
батериите · `zalivane-technosti.html` Заливане и ремонт на дънни платки.

Mark these in a comment as **working titles carrying the mechanism**; SEO-01 in Phase 3 owns
the search-tuned copy. Descriptions follow the same rule: one honest sentence naming what the
page is about, distinct per page, replaced in Phase 3.

**Do not** rename any file, create any new page file, or delete any page file. The three new
category pages named in `categories.php` are *not* created here — see Task 2. `covid.html` and
`problem-stari.html` both keep their exact filenames and stay deployed; neither is linked from
the nav, and neither is retired, because both are awaiting explicit owner sign-off and an
unreviewed retirement forfeits existing ranking.

Upload all fifteen to `public_html/new/` and confirm each returns 200 before moving on.
  </action>
  <verify>
    <automated>
      # Filename set is unchanged — SEO-04
      ls src/*.html | wc -l                                   # 17 (16 live + the local-only phptest spike)
      diff <(ls src/*.html | xargs -n1 basename | grep -v '^phptest.html$' | sort) \
           <(ls site-current/*.html | xargs -n1 basename | grep -v '^google.*\.html$' | sort)

      # Every page assigns its own metadata and uses the safe include idiom
      grep -lc '$torin_title' src/*.html | wc -l              # 16
      grep -c 'require_once' src/about.html                    # 2 (header + footer)
      grep -rn '__DIR__' src/*.html                            # no match
      grep -rn '<?=' src/*.html                                # no match
      grep -rn 'container py-5' src/*.html                     # no match (Bootstrap leftover gone)

      # Titles are distinct across all 16
      grep -h '$torin_title' src/*.html | sort -u | wc -l      # 16

      # Encoding hygiene after adding Cyrillic to fifteen files
      for f in src/*.html; do test "$(head -c 3 "$f" | xxd -p)" != "efbbbf" || echo "FAIL BOM $f"; done

      # LIVE — every one of the sixteen locked URLs
      for p in index about laptopi profilaktika-laptop optimizatsiq mehanichni-problemi \
               za-bateriite tokov-udar zalivane-technosti rezervni-chasti warrently \
               uslovia covid test-laptop problem-stari msg; do
        code=$(curl -s -o /dev/null -w '%{http_code}' "https://torin.bg/new/$p.html")
        lang=$(curl -s "https://torin.bg/new/$p.html" | grep -c '<html lang="bg"')
        printf '%-24s %s lang=%s\n' "$p.html" "$code" "$lang"
      done
      # sixteen distinct titles, live
      for p in index about laptopi profilaktika-laptop optimizatsiq mehanichni-problemi \
               za-bateriite tokov-udar zalivane-technosti rezervni-chasti warrently \
               uslovia covid test-laptop problem-stari msg; do
        curl -s "https://torin.bg/new/$p.html" | grep -o '<title>[^<]*</title>'
      done | sort -u | wc -l                                   # 16
    </automated>
  </verify>
  <acceptance_criteria>
    - The basename set of `src/*.html` (excluding the local-only `phptest.html` spike) is identical to the basename set of `site-current/*.html` (excluding the Google verification file) — no filename added, renamed, or removed
    - All 16 page files contain `$torin_title` and `$torin_desc` assigned above the header include
    - `grep -h '$torin_title' src/*.html | sort -u | wc -l` returns 16 — every page's working title is distinct
    - `grep -rn 'container py-5' src/*.html` returns no match; `grep -rn '__DIR__' src/*.html` and `grep -rn '<?=' src/*.html` both return no match
    - No `src/*.html` file begins with the three-byte UTF-8 BOM sequence
    - LIVE: all 16 URLs return HTTP 200, and `grep -c '<html lang="bg"'` returns exactly 1 for each — this, not the presence of the attribute in `header.php`, is what proves SEO-02
    - LIVE: extracting the title element from all 16 deployed URLs yields 16 distinct values
    - `src/covid.html` and `src/problem-stari.html` are both still present, still deployed, still returning 200, and linked from no navigation surface
  </acceptance_criteria>
  <done>
All sixteen locked URLs are live on the new design system with their own distinct title and
description, the per-page metadata mechanism Phase 3 needs is wired and demonstrably working,
and the filename set is byte-identical to what Phase 1 locked.
  </done>
</task>

<task type="auto">
  <name>Task 2: The category page template — required spine, optional blocks that omit cleanly</name>
  <files>src/includes/category-page.php, src/css/components.css, src/mehanichni-problemi.html, src/optimizatsiq.html, src/zalivane-technosti.html</files>
  <read_first>
    - `src/includes/categories.php` — the six records; the template reads the title and symptom line from here rather than retyping them per page
    - `src/includes/site-config.php` — the data-file conventions; this new include follows the same comment-header and provenance rules
    - `src/mehanichni-problemi.html`, `src/optimizatsiq.html`, `src/zalivane-technosti.html` — as left by Task 1
    - `src/index.html` — the CTA block markup this template reuses as a component, not a copy
    - `02-CONTEXT.md` D-24 (the required spine and the optional blocks), D-25 (content-depth targets are Phase 3), D-23 (the publish gate), D-26 (the service mapping table — the source of every sub-service name used here), D-28 (профилактика is cross-listed as a link to one page, never duplicated content), D-14 (BGA expertise lives inside category 4)
    - `02-RESEARCH.md` §6c — the conditional-optional-block rule and why an empty heading is worse than an absent section; Assumption A10 and Open Question 4 on whether unpublished pages exist as files
    - `02-UI-SPEC.md` §Typography and §Spacing Scale — the four sizes and seven spacing steps this template's sections use
  </read_first>
  <action>
**Planner call, recorded so it is not re-litigated:** the three **new** category pages
(`ekran-klaviatura-portove.html`, `pregryavane-ohlazhdane.html`, `nestandartna-technika.html`)
are **not created as files in this phase**. D-23 gates publication on genuine content, D-25
assigns content-depth targets to Phase 3, and 02-RESEARCH Open Question 4 recommends creating
each file at publish time. Their `published` flags are already false, so
`torin_category_href()` routes both the cards and the dropdown to their homepage anchors and
nothing is broken. Phase 3 creates each file and flips one boolean. What lands here is D-24's
*structure*, which is what CONTEXT.md's own phase boundary asks for: define the slot, record
the dependency, do not invent the content.

**`src/includes/category-page.php`.** A PHP 5.2-safe renderer, `torin_render_category_page($torin_page)`,
following the same file conventions as the other includes: comment header with the 5.2-safety
note and a line naming its consumers, `array()` only, no closures, no short echo tags, tabs.

It reads the category's display name and symptom line from `$torin_categories` using
`$torin_page['cat_id']`, so those two strings exist in exactly one place site-wide.

The **required spine**, in order: the `h1` (the category name), the intro, «Какво ремонтираме»
(the sub-service list), the symptom line, the warranty summary, and the CTA block.

The **optional blocks**: process, FAQ, related work, prices. Each renders **only when its key
is present and non-empty**. An absent block emits nothing at all — not an empty heading, not an
empty section wrapper. An empty heading with no body is worse than an absent section: it is
exactly the thin-content signal D-23 exists to avoid. Apply the same conditional rule to the
two spine slots whose content is a Phase 3 deliverable — the intro and the warranty summary
(TRUST-03) — so a page missing them today renders cleanly rather than shipping empty headings,
and record that dependency in the file's comment header.

Unused slots cost nothing in search, which is exactly why D-24 chose core-plus-optional over a
lean fixed template: a lean template would cap depth on the strongest categories, which are the
ones that could actually rank.

**Instantiate on the three published category pages.** Each page assigns a `$torin_page` array
naming its `cat_id` and its `fixes` list, then calls the renderer. The sub-service names come
from D-26's mapping table and nowhere else — this is existing, already-mapped content being
carried forward, not new copy:

- `mehanichni-problemi.html` → `kat-1`. Fixes: счупени панти (moved here from category 2 by
  D-41's physical-damage split) and корпус/шаси damage. This is deliberately the **thin** case
  and it is the one that proves optional blocks omit cleanly.
- `optimizatsiq.html` → `kat-3`. Fixes: безплатни съвети за бързодействие, plus профилактика as
  a **link** to the existing `profilaktika-laptop.html`. Never duplicate that page's content
  here: one URL, one canonical, no duplicate-content exposure — the internal link from a second
  topical context is the entire benefit (D-28).
- `zalivane-technosti.html` → `kat-4`. Fixes: дозапояване на отпоени BGA чипове, ребоулинг на
  BGA чипове, подмяна на чипсет, сервиз на захранващи вериги. This is the **deep** case, and
  it is where the BGA and chip-level expertise lives naturally (D-14) rather than needing its
  own homepage treatment.

Leave the intro, warranty, process, FAQ, related and prices keys unset on all three. That is
the point of the exercise: the same template must render a two-item category and a four-item
category correctly, and omit six blocks without leaving a trace.

**`src/css/components.css`.** Add the `.svc` section rules, reusing the existing eyebrow,
heading, list and CTA styling. No new tokens, no new type sizes — the four-size scale is
closed. Keep the running CSS total inside the budget established in plan 02-01.

Deploy the three pages and the new include, then verify.
  </action>
  <verify>
    <automated>
      # The three new category page files were deliberately NOT created
      ls src/ekran-klaviatura-portove.html 2>/dev/null                 # absent
      ls src/pregryavane-ohlazhdane.html 2>/dev/null                   # absent
      ls src/nestandartna-technika.html 2>/dev/null                    # absent

      # Template shape and PHP 5.2 safety
      grep -c 'function torin_render_category_page' src/includes/category-page.php   # 1
      grep -c 'isset(' src/includes/category-page.php                  # >= 6 (one per optional block)
      grep -rnE '(=>[^;]*\]|\[\s*[^]]*=>)' src/includes/category-page.php
      grep -rn 'function *(' src/includes/category-page.php
      grep -rn '__DIR__' src/includes/category-page.php

      # Title and symptom line are single-sourced, not retyped on the pages
      grep -c 'Счупвания и механични повреди' src/mehanichni-problemi.html   # 0
      grep -c 'cat_id' src/mehanichni-problemi.html                    # 1
      grep -c 'torin_render_category_page' src/zalivane-technosti.html # 1

      # Профилактика is a link, never duplicated content
      grep -c 'profilaktika-laptop.html' src/optimizatsiq.html          # >= 1

      # LIVE — the spine renders, the optional blocks leave no trace
      curl -s https://torin.bg/new/zalivane-technosti.html | grep -c 'Ребоулинг на BGA чипове'   # >= 1
      curl -s https://torin.bg/new/zalivane-technosti.html | grep -c 'Заливане и ремонт на дънни платки'  # >= 1
      curl -s https://torin.bg/new/mehanichni-problemi.html | grep -c 'svc__fixes'               # 1
      # absent optional blocks emit nothing at all
      curl -s https://torin.bg/new/mehanichni-problemi.html | grep -ciE 'svc__process|svc__faq|svc__related|svc__prices'  # 0
      curl -s https://torin.bg/new/mehanichni-problemi.html | grep -c 'Как работим'              # 0
      # no empty headings anywhere on the three pages
      for p in mehanichni-problemi optimizatsiq zalivane-technosti; do
        curl -s "https://torin.bg/new/$p.html" | grep -cE '<h[23][^>]*>[[:space:]]*</h[23]>'      # 0 each
      done
      for p in mehanichni-problemi optimizatsiq zalivane-technosti; do
        curl -s -o /dev/null -w "$p %{http_code}\n" "https://torin.bg/new/$p.html"                # 200 each
      done
      wc -c src/css/base.css src/css/layout.css src/css/components.css
    </automated>
    <human-check>
      Open the deep case (`zalivane-technosti.html`) and the thin case
      (`mehanichni-problemi.html`) side by side at 360x640 and at 900x800. Confirm the thin page
      does not look broken or truncated by the six omitted blocks — no orphaned rule, no double
      gap, no section that reads as if content is missing. That judgement is the actual purpose
      of instantiating both shapes, and it cannot be made from a grep.
    </human-check>
  </verify>
  <acceptance_criteria>
    - None of `src/ekran-klaviatura-portove.html`, `src/pregryavane-ohlazhdane.html`, `src/nestandartna-technika.html` exists — the publish gate is honoured and the deferral is recorded in the SUMMARY with its D-23/D-25 rationale
    - `grep -c 'function torin_render_category_page' src/includes/category-page.php` returns 1, and the file guards each optional block with an `isset` plus non-empty test (at least 6 such guards)
    - The 02-RESEARCH V3 lint greps return no output for `category-page.php`
    - The three category pages contain `cat_id` and `torin_render_category_page`, and none of them retypes its category display name — `grep -c 'Счупвания и механични повреди' src/mehanichni-problemi.html` returns 0
    - `grep -c 'profilaktika-laptop.html' src/optimizatsiq.html` returns at least 1, and no профилактика body copy is duplicated onto that page
    - LIVE: `zalivane-technosti.html` renders its four sub-services including «Ребоулинг на BGA чипове» and its category name from the shared record
    - LIVE: `grep -ciE 'svc__process|svc__faq|svc__related|svc__prices'` on `mehanichni-problemi.html` returns 0 — every absent optional block emits nothing
    - LIVE: `grep -cE '<h[23][^>]*>[[:space:]]*</h[23]>'` returns 0 on all three pages — no empty heading anywhere
    - LIVE: all three pages return HTTP 200
    - The running CSS byte total is recorded and still inside the budget established in plan 02-01
    - The human check records an explicit judgement on whether the thin page reads as broken
  </acceptance_criteria>
  <done>
One PHP 5.2-safe template renders both a two-sub-service category and a four-sub-service
category correctly from the shared category records, omitting six optional blocks without
leaving an empty heading or a stray gap, and the three unpublished categories still route to
their homepage anchors because their files deliberately do not exist yet.
  </done>
</task>

<task type="auto">
  <name>Task 3: Phase verification sweep — prove the site-wide claims against the deployed site</name>
  <precondition>All sixteen page files, the four stylesheets, the two font binaries, the six includes and `site.js` are deployed to `public_html/new/` — plans 02-01 through 02-03 and the two tasks above each deploy their own artefacts, and this sweep asserts against what is live rather than what is committed.</precondition>
  <files>src/css/components.css</files>
  <read_first>
    - `02-RESEARCH.md` §Validation Architecture — V2 through V8 in full, including the runtime DevTools checks and the exact viewport assertion table
    - `02-RESEARCH.md` Pitfall 1 — why a filename grep produces a false pass, and what the assertion must be instead; Pitfall 7 — why a local preview proves nothing here
    - `02-RESEARCH.md` §4a — the verified inventory, in particular that the three named vendor libraries are not separate script tags but are bundled inside one file
    - `.planning/phases/02-design-system-information-architecture/02-01-SUMMARY.md` — whether the compression probe made the 20 KB CSS budget a target or a hard limit
    - `02-UI-SPEC.md` §Design System (the CSS budget table) and §Layout & Breakpoints (the above-the-fold arithmetic)
  </read_first>
  <action>
Run the full validation architecture against the live staging site and record every result. Do
not fix defects silently inside this task — if a check fails, fix it, re-deploy, re-run, and
record both the failure and the fix in the SUMMARY.

**Vendor-stack removal, done properly (02-RESEARCH V2 / Pitfall 1).** The naive check is a
grep for the three named library filenames, and it passes on a page still shipping half a
megabyte of them, because they are not separate script tags — they live bundled inside one
vendor file. So assert three things instead: (a) the static sweep over every page file, every
include and the JavaScript file returns nothing for the full pattern set; (b) the same sweep
run against each of the sixteen **deployed** responses returns zero hits; (c) at runtime in the
DevTools console on the live homepage, the jQuery global, the dollar global and the ScrollMagic
global are all undefined, and the Network panel shows zero requests into the legacy asset
directory.

**Dropped fonts, swept the same way (D-08).** D-08 drops four families outright — Basier Square,
Glacial Indifference, Cerebri Sans, and the Google-hosted Barlow — because none of them contains
a single Cyrillic glyph, so the Bulgarian-only site has never actually rendered in any of them.
Plan 02-01 replaced all typography with the two self-hosted Sofia Sans subsets (D-06a). Until
this task, that removal was structural rather than asserted: the vendor sweep pattern names the
JavaScript stack only, so a re-added stylesheet link to the font CDN, or a surviving `@font-face`
still pointing at one of the four dropped webfont files, would have passed every check in the
phase. Extend both sweeps below with the four legacy webfont filenames and the two Google Fonts
hostnames, and add one structural check that no stylesheet references any remote asset at all —
that second check catches a re-added remote font by shape rather than by name, which is what
survives a family being renamed upstream.

**Language and encoding (V2 tail, V4).** Fetch each of the sixteen URLs and assert exactly one
`lang="bg"` declaration. This is what proves SEO-02 — the attribute sitting in `header.php`
proves only that the include *would* emit it, not that every page uses the include. Then run the
BOM and encoding sweep across every `.php`, `.html`, `.css` and `.js` under `src/`, and confirm
a known Cyrillic string round-trips through a live response.

**PHP 5.2 safety (V3).** Run all five lint greps across every include and every page file. Each
must return nothing. On this host a 5.4-only construct is a parse error, which surfaces as a
blank 200 or a 500 only after upload — there is no local PHP to catch it first.

**Responsive behaviour (V6).** Run the automatable precheck for oversized fixed widths and
horizontal-overflow hacks, then the manual pass at the four viewports with the exact assertions
from the research table, plus the keyboard-only pass and the JavaScript-disabled pass.

**Structured data (V7).** Rich Results Test on a rendered page; zero errors expected.

**Byte budget.** Measure the four stylesheets, the JavaScript file, the two font binaries and
the icon library. Compare the production CSS total against the 20 KB target and against the
compression finding recorded in plan 02-01 — if the host serves nothing compressed, that number
is a hard wire cost on every page view and any overage must be trimmed here, not deferred.

**`src/css/components.css`** is listed as modifiable for exactly one reason: trimming to budget
if the measurement comes in over. Do not restructure.

Record every measured number in the SUMMARY. Phase 3 and Phase 4 both read them: Phase 4 owns
DESIGN-02 and needs the byte baseline, and the Phase 4 cutover checklist needs confirmation
that the dev-only switcher and the Theme A stylesheet are still present and still isolated to
two deletable files.
  </action>
  <verify>
    <automated>
      # V2a — static sweep, must return nothing.
      # Second alternation group is the D-08 font set: the two Google Fonts hosts and the four
      # dropped legacy webfont filenames.
      grep -rniE 'jquery|modernizr|scrollmagic|pagepiling|theme-vendors|theme\.min|assets1/|zdassets|ajax\.googleapis|cdnjs\.cloudflare|fonts\.googleapis|fonts\.gstatic|basiersquare-|GlacialIndifference-|cerebri-sans|family=Barlow' \
        src/*.html src/includes/*.php src/js/*.js

      # D-08 / D-06a — no stylesheet pulls a remote asset. Catches a re-added remote font by
      # shape rather than by family name, which survives an upstream rename.
      grep -nE "@import|url\(['\"]?https?:" src/css/*.css                        # no match

      # V2b — live sweep across all sixteen locked URLs
      for p in index about laptopi profilaktika-laptop optimizatsiq mehanichni-problemi \
               za-bateriite tokov-udar zalivane-technosti rezervni-chasti warrently \
               uslovia covid test-laptop problem-stari msg; do
        n=$(curl -s "https://torin.bg/new/$p.html" | grep -ciE 'jquery|modernizr|scrollmagic|pagepiling|theme-vendors|theme\.min|assets1/|zdassets|fonts\.googleapis|fonts\.gstatic|basiersquare-|GlacialIndifference-|cerebri-sans|family=Barlow')
        l=$(curl -s "https://torin.bg/new/$p.html" | grep -c '<html lang="bg"')
        r=$(curl -s "https://torin.bg/new/$p.html" | grep -c '<?php')
        printf '%-24s vendor=%s lang=%s rawphp=%s\n' "$p.html" "$n" "$l" "$r"
      done   # expect vendor=0 lang=1 rawphp=0 on every row

      # V3 — PHP 5.2 lint, each returns nothing
      grep -rnE '(=>[^;]*\]|\[\s*[^]]*=>)' src/includes/*.php
      grep -rn 'function *(' src/includes/*.php
      grep -rn '<?=' src/includes/*.php src/*.html
      grep -rnE 'namespace |::class|\?\?|\.\.\.|yield ' src/includes/*.php
      grep -rnE 'JSON_UNESCAPED|JSON_PRETTY' src/includes/*.php
      grep -rn '__DIR__' src/includes/*.php src/*.html

      # V4 — encoding hygiene + live mojibake round-trip
      for f in $(find src -name '*.php' -o -name '*.html' -o -name '*.css' -o -name '*.js'); do \
        enc=$(file -b --mime-encoding "$f"); bom=$(head -c 3 "$f" | xxd -p); \
        [ "$bom" = "efbbbf" ] && echo "FAIL BOM  $f"; \
        case "$enc" in utf-8|us-ascii) ;; *) echo "FAIL ENC $enc $f";; esac; done; echo "encoding check done"
      curl -s https://torin.bg/new/index.html | grep -c 'ТОРИН КОМПЮТЪРС'    # >= 1

      # V6 precheck — no oversized fixed widths, no horizontal-overflow hacks
      grep -rnE 'width: *[3-9][0-9]{2,}px|min-width: *[3-9][0-9]{2,}px' src/css/*.css
      grep -rn 'overflow-x' src/css/*.css

      # Byte budget
      wc -c src/css/base.css src/css/layout.css src/css/components.css src/css/theme-a.css \
            src/js/site.js src/includes/icons.php src/fonts/*.woff2

      # Phase 4 cutover surface is still exactly two deletable files
      ls src/css/theme-a.css src/includes/dev-switcher.php
      grep -c 'DEV-ONLY' src/includes/header.php                             # >= 1 (fence intact)

      # SEO-04 filename set unchanged, final check
      diff <(ls src/*.html | xargs -n1 basename | grep -v '^phptest.html$' | sort) \
           <(ls site-current/*.html | xargs -n1 basename | grep -v '^google.*\.html$' | sort)
    </automated>
    <human-check>
      **Runtime check (02-RESEARCH V2, the one a grep cannot make).** In the DevTools console on
      https://torin.bg/new/index.html evaluate the jQuery global, the dollar global and the
      ScrollMagic global — all three must report undefined. Open the Network panel, hard-reload,
      and confirm zero requests to the legacy asset directory and zero requests to any
      third-party host other than none at all. Confirm the total transferred bytes for the page.

      **Responsive pass (02-RESEARCH V6).** At 360x640: no horizontal scrollbar, hamburger
      visible with the nav list hidden, hero at or under 384px, the first category card's media
      box and title visible without scrolling, grid at one column. At 560x800: two columns, no
      orphan single card except the trailing pair. At 900x800: hamburger hidden with five nav
      items inline, grid three columns breaking 3+3, dropdown opening on click with all six
      names on one line each. At 1440x900: container capped rather than full-bleed, body measure
      at or under roughly 66 characters. Evaluate the scroll-width assertion at each.

      **Keyboard-only pass at 900x800** and **JavaScript-disabled pass** as specified in plan
      02-03 — re-run them here against the finished site rather than the mid-phase state.

      **Structured data (02-RESEARCH V7).** Rich Results Test on the rendered homepage source:
      zero errors on the LocalBusiness block, Cyrillic decoding correctly in the parsed view.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The static vendor sweep across `src/*.html`, `src/includes/*.php` and `src/js/*.js` returns no output, with the pattern set covering **both** the legacy JavaScript stack **and** the D-08 font set — the two Google Fonts hostnames and the four dropped legacy webfont filenames
    - `grep -nE "@import|url\(['\"]?https?:" src/css/*.css` returns no match — no stylesheet references a remote asset, so neither the font CDN nor any of the four D-08 families has a re-entry path that a name-based grep could miss
    - The live sweep prints `vendor=0 lang=1 rawphp=0` for every one of the sixteen URLs — sixteen rows, no exceptions, with the same D-08-extended pattern applied to each deployed response
    - All six 02-RESEARCH V3 lint greps return no output across every include and every page file
    - No file under `src/` carries a UTF-8 BOM, every file reports `utf-8` or `us-ascii` encoding, and the live homepage contains the Cyrillic shop name
    - `grep -rnE 'width: *[3-9][0-9]{2,}px|min-width: *[3-9][0-9]{2,}px' src/css/*.css` produces no unreviewed hit, and `grep -rn 'overflow-x' src/css/*.css` returns no match
    - The measured byte totals for the four stylesheets, `site.js`, `icons.php` and both font binaries are recorded in the SUMMARY, with the production CSS total compared explicitly against the 20 KB budget and against the compression finding from plan 02-01
    - `src/css/theme-a.css` and `src/includes/dev-switcher.php` both exist and the `DEV-ONLY` fence is still present in `header.php` — the Phase 4 cutover remains a two-file deletion plus a marked line removal, with no token file edited
    - The SEO-04 filename diff between `src/` and `site-current/` is empty
    - Runtime check recorded: the three globals report undefined and the Network panel shows zero legacy asset requests
    - All four viewport assertions, the keyboard-only pass, the JavaScript-disabled pass and the Rich Results Test are recorded with observed results
  </acceptance_criteria>
  <done>
Every site-wide claim this phase makes is proven against the deployed site rather than the
repository: sixteen URLs return 200 with exactly one `lang="bg"` declaration and zero legacy
vendor references, no PHP 5.2-invalid construct exists anywhere, encoding is clean, the CSS
fits its budget, the responsive and keyboard contracts hold, and the Phase 4 cutover surface is
still two deletable files.
  </done>
</task>

</tasks>

## Flagged assumptions (probe-surfaced and planner-made — not silently dropped)

| Source | Item | Status | Disposition |
|---|---|---|---|
| Planner call (D-22 / D-23 / D-25 / 02-RESEARCH Open Question 4, A10) | Whether the three **new** category page files exist before they publish | resolved by planner | **They do not.** D-22's structural deliverable — six categories with assigned slugs, publish flags and a single href accessor — lands in this phase; the files themselves are created in Phase 3 when they have the content D-25 assigns there. Nothing is broken meanwhile: both the cards and the dropdown route to homepage anchors. Recorded here because the alternative (empty shells) is worse and 02-RESEARCH asked for an explicit call either way. |
| Planner call (D-24 / D-25 / CONTEXT phase boundary) | How much of the category page template is Phase 2 | resolved by planner | The **template** is Phase 2 structure and is delivered and instantiated on the three already-published categories using only content D-26 already mapped. The **depth** — intro, warranty summary, process, FAQ, related work — is Phase 3 per D-25, and every one of those slots renders nothing rather than an empty heading until then. |
| D-13 / OWNER-QUESTIONS #9 / 02-RESEARCH Pitfall 8 | DIFF-02 (battery regeneration) placed in a folded section | carried forward | Knowingly downgraded, and 02-RESEARCH adds a ranking cost the original trade-off did not price. Phase 3 verification must treat DIFF-02 as knowingly unmet rather than silently passing. Not resolvable in this phase. |

<threat_model>
**Standard: OWASP ASVS L1. Blocking threshold: high.** Every threat rated `high` must have its
mitigation implemented in a task action and asserted by an acceptance criterion in this plan;
`medium` and `low` may be accepted with recorded rationale.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → each of 16 deployed pages | Every page must independently pull the shared chrome; a page that fails to include it loses the language declaration and the design system silently |
| dev machine → FTPS → `public_html/new/` | Fifteen page files plus a new include cross the network |
| editor → new PHP and HTML files | A byte-order mark or a 5.4-only construct introduced here fails only after upload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16 | Information disclosure | Legacy vendor references, the plain-HTTP CDN script, or a re-added third-party font request surviving into any of the sixteen pages | high | mitigate | The live sweep runs against all sixteen deployed responses, not against filenames — 02-RESEARCH Pitfall 1 is that a filename grep passes while the libraries still ship bundled inside one file. The pattern set covers the D-08 font surface too (both Google Fonts hostnames and the four dropped webfont filenames), plus a structural check that no stylesheet references a remote asset, so a re-added font request cannot slip through under a renamed family. Backed by a runtime check that the three globals are undefined and the Network panel shows zero legacy asset requests and zero third-party hosts. Removing the plain-HTTP script closes an existing mixed-active-content injection surface. |
| T-02-17 | Spoofing | A page file that fails to include the shared chrome | medium | mitigate | Assert `lang="bg"` on each of the sixteen deployed responses individually. A page that silently stops including `header.php` loses its language declaration, its design system, and its place in the noindexed staging context — and looks fine in the repository. |
| T-02-18 | Denial of Service | A PHP 5.2-invalid construct or a UTF-8 BOM introduced while editing fifteen files | medium | mitigate | The five-part V3 lint plus the BOM sweep run over every include and every page file before and after deploy. On this host a 5.4-only construct is a parse error producing a blank 200 or a 500, and a BOM emits stray bytes that break both rendering and the theme switcher's `setcookie()` call. Backed by the live 200-and-not-raw-PHP assertion on all sixteen URLs. |
| T-02-19 | Repudiation | Silently retiring an indexed URL during a design pass | high | mitigate | A filename-set diff against `site-current/` is asserted twice in this plan. `covid.html` and `problem-stari.html` are both pending owner sign-off; neither is retired, both stay deployed and unlinked, and any retirement later must be a 301 rather than a bare 404 (D-36). |
| T-02-20 | Elevation of privilege | The dev-only theme switcher leaking toward production | medium | mitigate | Not cut over in this phase, but assert now that the cutover surface is still exactly two deletable files plus one marked fence in `header.php`, with no token file edited — so the Phase 4 gate is a deletion with no production CSS diff to review. |
| T-02-SC | Tampering | Supply chain | low | accept | Zero packages installed across the entire phase and zero third-party runtime assets. The only third-party artefact is the font binary, audited under OFL and verified in plan 02-01 by the V1 Bulgarian-shaping gate — the authoritative integrity check — with its byte size recorded for comparison rather than pinned as a hard gate, because Google re-cuts subset binaries on upstream revision. No legitimacy gate applies. |
</threat_model>

<verification>
The full 02-RESEARCH Validation Architecture, run against the deployed site:

1. **V2** — vendor removal: static sweep, live sweep across all sixteen URLs, runtime globals
   and Network panel. The pattern set covers the D-08 font surface as well as the legacy
   JavaScript stack, and a separate structural check asserts that no stylesheet references any
   remote asset. Plus the sixteen-URL `lang="bg"` assertion that is what actually proves SEO-02.
2. **V3** — PHP 5.2 syntax safety across every include and every page file.
3. **V4** — encoding hygiene plus the live Cyrillic round-trip.
4. **V6** — responsive precheck, the four viewports with their exact assertions, the
   keyboard-only pass, and the JavaScript-disabled pass.
5. **V7** — structured data through the Rich Results Test.
6. **Byte budget** — four stylesheets, the script, the icon library, both font binaries,
   measured and compared against the compression finding from plan 02-01.
7. **SEO-04** — filename-set diff against `site-current/`, empty.
</verification>

<success_criteria>
- Sixteen live URLs, sixteen HTTP 200s, sixteen `lang="bg"` declarations, sixteen distinct
  titles, zero legacy vendor references, zero references to the font CDN or any of the four
  fonts D-08 dropped, zero raw PHP in any response.
- The category page template renders both a thin and a deep category correctly and omits every
  absent optional block without leaving an empty heading.
- The three unpublished category pages do not exist as files, and nothing links to a missing
  page because every href resolves through the publish-gate accessor.
- No filename added, renamed, or retired since Phase 1 locked them.
- Production CSS inside budget, with the measured numbers recorded for Phase 4's DESIGN-02.
- The Phase 4 cutover surface is still two deletable files and one marked fence.
</success_criteria>

<output>
Create `.planning/phases/02-design-system-information-architecture/02-04-SUMMARY.md` when done.

Record explicitly: the sixteen-row live sweep table verbatim; every measured byte total; the
planner call that the three new category page files are deferred to Phase 3 with its
D-23/D-25 rationale; the D-13 carry-forward for DIFF-02; the three `[ASSUMED]` business facts
still blocking the Phase 4 cutover; and the confirmed-open JavaScript-disabled nav gap.
</output>
