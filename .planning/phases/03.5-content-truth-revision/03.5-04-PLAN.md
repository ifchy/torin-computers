---
phase: 03.5-content-truth-revision
plan: 04
type: execute
wave: 2
depends_on: ["03.5-01"]
files_modified:
  - src/profilaktika-laptop.html
  - src/pregryavane-ohlazhdane.html
  - src/img/repairs/profilaktika17.jpg
  - src/img/repairs/profilaktika7.jpg
  - src/img/repairs/profilaktika15.jpg
autonomous: true
requirements: [DIFF-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Neither the maintenance page nor the overheating page makes a BGA, reballing or chip-level claim (ROADMAP SC-1)."
    - "The three reballing photographs are withdrawn from the repository and referenced by no page (ROADMAP SC-3)."
    - "The maintenance page's evidence strip still renders with at least two photographs and no chip-level caption (ROADMAP SC-3)."
    - "The maintenance page still reads as a complete service page about cleaning and thermal paste, which is what it is for (ROADMAP SC-2 discipline)."
    - "Both pages carry the free-diagnostics claim in its «първоначална» form (ROADMAP SC-7)."
  artifacts:
    - src/profilaktika-laptop.html
    - src/pregryavane-ohlazhdane.html
  key_links:
    - "$page['evidence'] -> torin_render_evidence() at category-page.php:265 — two of this page's three entries are being withdrawn, and the renderer emits nothing at all when every entry is absent"
    - "profilaktika-laptop.html -> index.html catch-all «Профилактика и поддръжка» disclosure link (read-only here; plan 03.5-02 owns index.html)"
    - "site-config.php warranty['default'] -> both pages' .svc__warranty__term"
---

<objective>
Remove the chip-level material from the two thermal pages, and withdraw the three reballing
photographs from the repository.

`profilaktika-laptop.html` carries the highest chip-claim density in the tree — sixteen instances,
including two whole `prose` blocks about the infrared station, its software profiles, its flux
vendor, the ten-degree figure and the ninety-percent success rate. Its `<title>` names the withdrawn
process. Two of its three evidence photographs are reballing photographs.

The page is nonetheless in a much better position than category 4 was: профилактика — cleaning the
cooling system and changing thermal paste — **is** the page's actual subject and every word of it
survives. The chip-level material was an addition to a complete page, not its substance. Expect the
page to come out shorter and still whole. Do not add anything to compensate.

`pregryavane-ohlazhdane.html` carries three instances, all of the same shape: overheating that goes
on long enough lifts a chip off the board, "and that is chip-level repair, not cleaning". The cause
is true and stays; the clause promising the repair goes.

Why `requirements: [DIFF-04]`: DIFF-04 exists because DIFF-02 and DIFF-03 retired (REQUIREMENTS.md);
removing DIFF-03's claims is part of delivering it.

Purpose: make the thermal pages true without hollowing the one that was never hollow.
Output: two corrected pages and three photographs out of the repository.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-content-truth-revision/03.5-CONTEXT.md
@.planning/phases/03-content-trust-signal-build-out/03-UI-SPEC.md
@.planning/phases/03.5-content-truth-revision/03.5-01-SUMMARY.md
@src/profilaktika-laptop.html
@src/pregryavane-ohlazhdane.html
@src/includes/category-page.php
</context>

<environment_rules>
Same as 03.5-01 and binding here:
- PHP 5.2.17, `array()` only, no short-array literals, no closures, no namespaces.
- No local PHP interpreter. The deploy is the only syntax check. HTTP 200 proves nothing.
- `scripts/deploy-new.sh` is denied to executor subagents. Record live checks as **NOT RUN** and
  print the exact deploy command. **Deploy uploads and never deletes** — removing a photograph from
  `src/` does not remove it from the server.
- Page data arrays keep their page-specific names. Never `$torin_page` or any generic `$torin_*`.
- A source comment must never quote verbatim a string the gate greps for.
  `src/profilaktika-laptop.html:26` currently names the withdrawn flux vendor in an English comment.
- Cyrillic token counts must count only `[а-яА-Я]` tokens, never `wc -w`.
- `grep -c` counts lines; use `grep -o ... | wc -l` for occurrences.
</environment_rules>

<sources>
| ID | Source | What it supplies |
|----|--------|------------------|
| S1 | The pages themselves | Everything about dust, thermal paste, heat pipes, contact pads, fans and lubrication — all true, all staying, all already in the shop's voice |
| S2 | CONTEXT D3.5-02 / OWNER-QUESTIONS #31 | The boundary: everything chip-level goes; component-level work on a board survives |
| S3 | `src/includes/site-config.php` | The free-diagnostics strings and the warranty key |

Nothing is written to replace a removed paragraph. If a section loses its content entirely, the
section goes with it (D3.5-08).
</sources>

<!-- planner-discipline-allow: profilaktika17.jpg -->
<!-- planner-discipline-allow: profilaktika7.jpg -->
<!-- planner-discipline-allow: profilaktika15.jpg -->
<!-- planner-discipline-allow: profilaktika14.jpg -->

<tasks>

<task type="auto">
  <name>Task 1: Strip the chip-level material from profilaktika-laptop.html and rebuild its evidence strip</name>
  <files>src/profilaktika-laptop.html</files>
  <read_first>
    - `src/profilaktika-laptop.html` in full — the head comment at line 26 names the withdrawn
      consumable; `$torin_title` at line 44 names the withdrawn process; the `intro` at line 69
      ends on a chip-level premise; `evidence` at lines 80-84; `fixes` at 86-92; the two `prose`
      blocks at roughly 96-125; `process` at 139; FAQ at 150
    - `src/includes/category-page.php` lines 247-300 — `torin_render_evidence()`: absent files are
      skipped silently and an all-absent strip emits nothing at HTTP 200
    - `03-UI-SPEC.md` §4 — the evidence contract: cap of three, true intrinsic `width`/`height`, a
      caption says what the photograph proves
    - The candidate photographs. `src/img/repairs/` holds `profilaktika1` through `profilaktika16`
      besides the withdrawn three. **Open them.** Two prior plans independently found
      plan-named photographs to be wrong: one supposed to be the infrared station was a dust-clogged
      heatsink, two supposed to show battery packs were schematic diagrams unreadable at a 100x100
      crop, and one `.jpg` was a GIF. A filename is not a description.
    - `.planning/phases/03.5-content-truth-revision/03.5-01-SUMMARY.md` — config value names for the
      free-diagnostics strings
  </read_first>
  <action>
    **(a) Remove the two chip-level `prose` blocks entirely.** One describes the machine, its heat
    source, the German know-how answer and the flux vendor; the other describes the hot-air-gun
    comparison, the cooling reinforcement, the ten-degree figure and the success rate. Both are
    wholly about the withdrawn service. Delete the blocks, not selected sentences — a block with two
    of five paragraphs removed is the "page with holes" shape D3.5-02 names. Delete the head comment
    at line 26 that introduces them, which names the flux vendor.

    Do not replace them. The page's subject is профилактика and every paragraph about dust, paste,
    heat pipes and pads survives untouched. Shorter and complete beats padded.

    **(b) `$torin_title`.** It currently names the withdrawn process in the title tag, which is also
    the strongest SEO signal on the page. Rewrite it around what the page is for. It must stay under
    55 code points and stay distinct from every other page title — `optimizatsiq.html` and
    `pregryavane-ohlazhdane.html` are its nearest neighbours and the uniqueness gate compares all of
    them.

    **(c) `intro`.** Its closing clause reasons from dust, to heat, to a chip lifting off the board,
    to "and a lifted chip is already chip-level repair", to "the cheapest repair is the one that
    never became necessary". The causal chain is true and is the best argument the page makes for
    doing профилактика at all. Keep the chain; end it at the damage rather than at the repair. The
    closing line stays.

    **(d) `fixes`.** One entry names reinforcing the cooling after chip-level repair — remove it.
    Another names changing thermal paste on the processor and the main packages; that is a
    description of the maintenance work itself and the package noun there is a location, not a
    service claim. Judge it against the Class-B rule and record the judgement.

    **(e) `process` and `faq`.** The process step describing paste and contact pads stays. The FAQ
    answer that ends "that is chip-level repair, not cleaning" keeps its true half — that prolonged
    overheating can lift a chip and that this is a different and heavier job — and drops the half
    that promises the job. Do not replace the promise with a different promise.

    **(f) The diagnostics claim.** This page carries it in the description and in FAQ prose. Both
    take the «първоначална» form; read the shared value from `site-config.php` where the slot allows
    a value.

    **(g) `$torin_desc`.** Rewrite whole — patching it with «първоначална» takes it to 143 code
    points against a ceiling of 140. It must land within 120-140, put the call before code point 95,
    stay distinct, and describe only cleaning and cooling work.

    **(h) The evidence strip.** Two of its three entries are withdrawn photographs. The third
    remains: contact pads on a chip damaged by uneven heating. Judge that one against the Class-B
    rule as well — it illustrates *damage another shop's method causes*, which is a cause, not a
    service Torin offers; but its caption must not imply the shop performs the work.

    Rebuild the strip to two or three entries from photographs you have actually opened and that
    honestly show what this page is about: dust-clogged cooling, a heatsink before and after, old
    thermal paste, a cleaned fan. Read each chosen file's true intrinsic pixels off the file and put
    those in `w` and `h` — a mismatch reserves the wrong box before load and is a CLS defect no
    static check catches. Write each caption to say what the photograph proves about maintenance.
    Never a filename, never the word for "photo", never a decorative label.

    If fewer than two of the available photographs honestly illustrate this page, ship the ones that
    do — even one — and say so. Do not reach for an unrelated image to hit the count.
  </action>
  <verify>
    <automated>node scripts/truth-gate.js src/profilaktika-laptop.html &amp;&amp; node scripts/seo-metadata-check.js &amp;&amp; grep -cE "(=>|=)[[:space:]]*\[|return[[:space:]]+\[" src/profilaktika-laptop.html | grep -qx 0 &amp;&amp; N=$(grep -o "'file' =>" src/profilaktika-laptop.html | wc -l | tr -d ' '); echo "ENTRIES=$N"; test "$N" -ge 1 &amp;&amp; for f in $(grep -o "'file' => '[a-z0-9.]*'" src/profilaktika-laptop.html | sed "s/.*'\\([a-z0-9.]*\\)'$/\\1/"); do test -f "src/img/repairs/$f" || { echo "MISSING FILE: $f"; exit 1; }; done; C=$(grep -o '[а-яА-Я][а-яА-Я-]*' src/profilaktika-laptop.html | wc -l | tr -d ' '); echo "CYRILLIC=$C"; test "$C" -ge 600 &amp;&amp; echo PROFILAKTIKA-GATES-OK</automated>
    <live-check status="NOT RUN — executor cannot deploy">
      In plan 03.5-07: `curl -s https://torin.bg/new/profilaktika-laptop.html | grep -o 'img/repairs/[a-z0-9.]*' | sort -u | wc -l`
      must equal the entry count shipped here, and `grep -c 'class="evidence"'` must return 1. A
      strip that silently shortened returns a lower number at HTTP 200 with no error anywhere.
    </live-check>
  </verify>
  <acceptance_criteria>
    - `node scripts/truth-gate.js src/profilaktika-laptop.html` exits 0 — comments included.
    - Cyrillic token count is **at least 600**, the bar Phase 3 held every category page to.
      Baseline is 952 and the two removed blocks are worth roughly 300, so this should clear
      comfortably. If it does not, report the figure and stop — do not pad (D3.5-08).
    - The evidence strip declares at least one entry, every referenced file exists in
      `src/img/repairs/`, and no entry is one of the three withdrawn photographs.
    - Every surviving entry's `w`/`h` are the file's real intrinsic pixels, read off the file in this
      task and stated in the SUMMARY beside what the photograph actually shows.
    - `node scripts/seo-metadata-check.js` exits 0: title under 55 code points and distinct,
      description 120-140 with the call before code point 95.
    - Zero PHP 5.2 short-array literals under the corrected regex; the page data array keeps its
      page-specific name.
    - Every Class-B survivor on the page is tabulated in the SUMMARY against the
      symptom/diagnostic/cause rule.
  </acceptance_criteria>
  <done>The maintenance page is about maintenance, complete, and shows photographs of it.</done>
</task>

<task type="auto">
  <name>Task 2: Correct pregryavane-ohlazhdane.html and withdraw the three reballing photographs</name>
  <files>src/pregryavane-ohlazhdane.html, src/img/repairs/profilaktika17.jpg (deleted), src/img/repairs/profilaktika7.jpg (deleted), src/img/repairs/profilaktika15.jpg (deleted)</files>
  <reversibility rating="costly">
    Deleting the three image files is recoverable from git, but the copies already uploaded to the
    staging server are not removed by any script available to this project — `deploy-new.sh` uploads
    and never deletes. Clearing them needs a manual FileZilla pass. That is why the deletion is
    recorded as a Phase 4 cutover item rather than claimed as done.
  </reversibility>
  <read_first>
    - `src/pregryavane-ohlazhdane.html` — the `fixes` entry at line 77, the `prose` paragraph at 93
      and the FAQ answer at 133 are the three instances
    - `src/img/repairs/` — confirm the three withdrawn filenames exist before deleting, and confirm
      by grep that no surviving page references them
    - `.planning/STATE.md` — the 03-09 note that the GSD cleanup helper blocks merges on
      `branch_contains_deletions` and that the merge then needed manual review
  </read_first>
  <action>
    **(a) `src/pregryavane-ohlazhdane.html`.** All three instances share one shape: overheating that
    continues long enough lifts a chip off the board, and that is chip-level repair rather than
    cleaning. The first clause is a **cause** and is the page's central argument for acting early —
    keep it. The second clause promises a service that no longer exists — remove it, and do not
    substitute a different promise. The honest ending is that the work is then different and heavier
    and that which of the two it is shows on inspection, which the page already says two sentences
    later.

    The `fixes` entry naming thermal paste on the processor and the main packages is the same
    judgement as its twin on the maintenance page; make the two consistent and record the ruling
    once.

    Its description sits at 122 code points and takes «първоначална» to 136, inside the ceiling — so
    it may be patched rather than rewritten. Verify the number after editing rather than trusting
    this one. Its body prose carries the claim in a second phrasing; both change.

    **(b) Withdraw the three photographs.** Delete `src/img/repairs/profilaktika17.jpg`,
    `src/img/repairs/profilaktika7.jpg` and `src/img/repairs/profilaktika15.jpg` from the
    repository. Before deleting, grep the whole of `src/` and confirm zero remaining references —
    plan 03.5-02 removed the homepage's three and Task 1 of this plan removed the maintenance
    page's two, and if either has not landed yet you would be deleting files a page still names,
    which produces a silently shortened strip rather than an error.

    Note in the SUMMARY, plainly and without claiming it is done: the three files remain on the
    staging server under `public_html/new/img/repairs/` and are reachable at their direct URLs.
    Nothing in this project can delete a remote file. The staging subtree carries
    `X-Robots-Tag: noindex, nofollow`, so the exposure is bounded; clearing them is a manual
    FileZilla step for the Phase 4 cutover. Record it as such.

    Note also that the GSD cleanup helper blocks merges containing deletions and that this plan
    contains four (three images plus whatever 03.5-01 left), so the merge may need the same manual
    review 03-09 needed.
  </action>
  <verify>
    <automated>node scripts/truth-gate.js src/pregryavane-ohlazhdane.html &amp;&amp; node scripts/seo-metadata-check.js &amp;&amp; grep -cE "(=>|=)[[:space:]]*\[|return[[:space:]]+\[" src/pregryavane-ohlazhdane.html | grep -qx 0 &amp;&amp; for f in profilaktika17.jpg profilaktika7.jpg profilaktika15.jpg; do test ! -f "src/img/repairs/$f" || { echo "STILL PRESENT: $f"; exit 1; }; R=$(grep -ro "$f" src/ | wc -l | tr -d ' '); test "$R" -eq 0 || { echo "STILL REFERENCED: $f x$R"; exit 1; }; done; C=$(grep -o '[а-яА-Я][а-яА-Я-]*' src/pregryavane-ohlazhdane.html | wc -l | tr -d ' '); echo "CYRILLIC=$C"; test "$C" -ge 850 &amp;&amp; echo PREGRYAVANE-GATES-OK</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/truth-gate.js src/pregryavane-ohlazhdane.html` exits 0.
    - The three withdrawn photographs are absent from `src/img/repairs/` and referenced zero times
      anywhere under `src/`.
    - Cyrillic token count of the overheating page is at least 850 (baseline 973; three clause-level
      edits should cost far less than that).
    - `node scripts/seo-metadata-check.js` exits 0 with the overheating page's description inside
      120-140 code points.
    - The SUMMARY states, as an open item and not as a completed one, that the three files remain on
      the staging server and that removing them is a manual FileZilla step at Phase 4 cutover.
    - The paste-on-main-packages `fixes` entry is resolved identically on both pages, with the
      ruling recorded once.
  </acceptance_criteria>
  <done>The overheating page states the cause without promising the withdrawn cure, and the three photographs are out of the repository.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> published service claim | `fixes` entries and FAQ answers are service promises |
| source tree -> server filesystem | The evidence renderer reads the server's image directory at request time; deletions in `src/` never propagate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-12 | Repudiation | Chip-level claims on thermal pages | high | mitigate | `truth-gate.js` over both files including comments; every Class-B survivor tabulated against the symptom/diagnostic/cause rule rather than waved through. |
| T-03.5-13 | Denial of service | Evidence strip silently emptying | medium | mitigate | Two of three entries are withdrawn at once, which is the exact condition under which `torin_render_evidence()` can emit nothing at HTTP 200. Local file-existence assertion plus a rendered count of `img/repairs/` references in the served body; the second is the one that catches a photograph never uploaded. |
| T-03.5-14 | Information disclosure | Withdrawn photographs still served at direct URLs | low | accept | No script in this project deletes a remote file. Bounded by the staging subtree's `X-Robots-Tag: noindex, nofollow`. Recorded as a manual Phase 4 cutover step, explicitly not claimed as done. |
| T-03.5-SC | Tampering | package-manager installs | n/a | accept | No package-manager install task exists in this plan. |
</threat_model>

<verification>
- `node scripts/truth-gate.js src/profilaktika-laptop.html src/pregryavane-ohlazhdane.html` -> exit 0
- `node scripts/seo-metadata-check.js` -> exit 0
- Cyrillic counts: maintenance page >= 600, overheating page >= 850
- Three withdrawn photographs absent from `src/img/repairs/` and referenced zero times under `src/`
- Zero PHP 5.2 short-array literals; both page data arrays keep page-specific names
- Live (03.5-07): both pages 200 with zero PHP warnings; `svc-page.js` PASS at 360x640 and
  1440x900 **without** `SVC_EXPECT_URGENT`; the maintenance page's rendered strip carries the same
  photograph count the source declares
</verification>

<success_criteria>
- ROADMAP SC-1 met for both pages: zero Class-A hits.
- ROADMAP SC-3 met: the three reballing photographs are out of the repository, referenced nowhere,
  and the maintenance page's strip is verified as rendered rather than as declared.
- ROADMAP SC-7 advanced: both pages carry the reworded diagnostics claim.
- ROADMAP SC-9 held, pending the live check in 03.5-07.
- The maintenance page is shorter and complete. No sentence was written to compensate for a removal.
</success_criteria>

<artifacts_this_phase_produces>
This plan contributes:
- `src/profilaktika-laptop.html` — chip-level blocks removed, title and description rewritten,
  evidence strip rebuilt from photographs that were actually opened
- `src/pregryavane-ohlazhdane.html` — cause kept, promise removed
- Removal of `profilaktika17.jpg`, `profilaktika7.jpg`, `profilaktika15.jpg` from the repository
- `03.5-04-SUMMARY.md` carrying the photograph inventory and the open staging-server cleanup item
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03.5-content-truth-revision/03.5-04-SUMMARY.md` when done.

It must record:
- what each evidence photograph actually depicts, having opened it, and the intrinsic pixels read
  off each file against the values shipped
- the Cyrillic count before and after for both pages, with no padding to clear a floor
- the single ruling on the paste-on-main-packages entry, applied to both pages
- every Class-B survivor with a one-line justification
- the staging-server photograph cleanup as an OPEN Phase 4 item, never as done
- the GSD cleanup-helper deletion-block warning for the merge
- the exact deploy command: `scripts/deploy-new.sh profilaktika-laptop.html pregryavane-ohlazhdane.html`
</output>
</content>
