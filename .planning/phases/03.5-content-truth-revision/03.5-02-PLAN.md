---
phase: 03.5-content-truth-revision
plan: 02
type: execute
wave: 2
depends_on: ["03.5-01"]
files_modified:
  - src/index.html
  - src/includes/categories.php
  - scripts/probes/trust-signals.js
autonomous: true
requirements: [DIFF-04, SEO-05]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The homepage makes no battery-regeneration claim and no chip-level claim (ROADMAP SC-1, D3.5-01)."
    - "The three reballing photographs no longer appear on the homepage, and no evidence strip renders short or empty as a result (ROADMAP SC-3)."
    - "Category 1 reads as «има видима повреда» and category 2 as «изглежда здрав, но не работи»; the «пукнат екран» misplacement is resolved (ROADMAP SC-5, D3.5-04)."
    - "No homepage link points at a retired URL (ROADMAP SC-4)."
    - "The homepage carries the free-diagnostics claim in its «първоначална» form (ROADMAP SC-7, D3.5-06)."
    - "RENDERED: the homepage serves six sections — hero, categories, brands, catch-all, self-diagnostic, contact — of which three are tinted, in strict plain/tint alternation, with adjacentTintedPairs === 0 and no horizontal scroll at 360px (03-UI-SPEC §Section order)."
    - "SOURCE: src/index.html contains five `<section` literals and three `section--tint` literals. The rendered count is one higher because torin_render_brand_row() injects its own section at render time; the two domains must never be checked against each other's number."
  artifacts:
    - src/index.html
    - src/includes/categories.php
    - scripts/probes/trust-signals.js
  key_links:
    - "categories.php symptoms -> index.html:80 .cat-card__symptoms (the only consumer of the symptom line)"
    - "kat-1 symptom line -> mehanichni-problemi.html's existing torin_service_href() links to svc-panti and svc-ekran (read-only check here; plan 03.5-06 owns that file)"
    - "index.html section count -> trust-signals.js adjacentTintedPairs and evidenceStrips gates"
    - "site-config.php free-diagnostics values (from 03.5-01) -> index.html hero badge and CTA note"
---

<objective>
Cut the two retired differentiator surfaces out of the homepage, fix the category 1/2 boundary in
the data file that feeds them, and stop the rendered probe from going permanently INCONCLUSIVE as a
consequence.

This is the plan where subtraction's own failure mode bites hardest. The homepage loses its two
biggest below-the-fold content sections. It will still return 200, still pass every static gate, and
still look fine in the markup — and it may read as though something was torn out. What proves
otherwise is a real layout engine measuring the six surviving sections, which is why the probe work
is in this plan and not deferred.

Why `requirements: [DIFF-04]`: REQUIREMENTS.md records that DIFF-04 exists *because* retiring
DIFF-02 and DIFF-03 left DIFF-01 as the only surviving differentiator. Removing the two withdrawn
claims is the first half of that requirement's delivery; category 6's promotion is the second half
and is gated (CONTEXT Deferred).

Purpose: make the site's most-read page true again without leaving a hole.
Output: a six-section homepage, a corrected category boundary, and a probe that still measures
something.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-content-truth-revision/03.5-CONTEXT.md
@.planning/phases/03-content-trust-signal-build-out/03-UI-SPEC.md
@.planning/phases/03.5-content-truth-revision/03.5-01-SUMMARY.md
@src/index.html
@src/includes/categories.php
@scripts/probes/trust-signals.js
@scripts/probes/svc-page.js
</context>

<environment_rules>
Same as 03.5-01 and binding here:
- PHP 5.2.17, `array()` only, `dirname(__FILE__)` only, no closures, no namespaces.
- No local PHP interpreter. The deploy is the only syntax check. HTTP 200 proves nothing.
- `scripts/deploy-new.sh` is denied to executor subagents. Record live checks as **NOT RUN** and
  print the exact deploy command.
- `deploy-new.sh` with no arguments uploads all of `src/` — always print an explicit file list.
- A source comment must never quote verbatim a string the gate greps for. `truth-gate.js` scans
  comments; `src/index.html` currently carries forbidden literals in four PHP comments.
- `grep -c` counts lines, not occurrences. zsh does not word-split unquoted expansions — use arrays.
- Cyrillic word counts must count only `[а-яА-Я]` tokens. `wc -w` counts English comments and has
  produced a false pass on this project before.
</environment_rules>

<!-- planner-discipline-allow: za-bateriite.html -->
<!-- planner-discipline-allow: <section -->
<!-- planner-discipline-allow: profilaktika17.jpg -->
<!-- planner-discipline-allow: profilaktika7.jpg -->
<!-- planner-discipline-allow: profilaktika15.jpg -->

<tasks>

<task type="auto">
  <name>Task 1: Remove the two retired differentiator sections and the battery disclosure from the homepage</name>
  <files>src/index.html</files>
  <read_first>
    - `src/index.html` in full — you need the section boundaries exactly, and four PHP comments in
      it quote forbidden literals
    - `03-UI-SPEC.md` §Section order and tint alternation — the eight-row table; rows 5 and 6 are
      what this task removes
    - `src/smyana-na-buksa.html` lines 148-158 — the shop's own existing description of how a flat
      battery, a dead charger and a broken jack present identically and all three get checked. This
      is the **source** for the replacement disclosure body. Do not write new copy; condense this.
    - `.planning/phases/03.5-content-truth-revision/03.5-01-SUMMARY.md` — the config value names
      03.5-01 added for the two free-diagnostics strings, and the redirect target chosen for the
      battery page
  </read_first>
  <action>
    **(a) Delete the chip-level section outright** — the one whose heading asks what the shop can do
    that others refuse, at `src/index.html:193-209`. Delete the `<section>`, its two paragraphs, its
    inline `torin_render_evidence()` call with all three photographs, its depth-page link, and the
    PHP comment block above it at lines 176-191, which names the withdrawn equipment and the
    success-rate figure. Nothing replaces it. Category 6 is the intended replacement differentiator
    (DIFF-04) but its publication is gated pending OWNER-QUESTIONS #3a-#3f, so inventing a stand-in
    here would be exactly the fabrication D3.5-08 forbids.

    **(b) Delete the battery-regeneration section outright** — `src/index.html:227-238` plus its
    comment block at 212-226. Same reasoning, same discipline: no replacement.

    **(c) Rewrite the «Батерията не държи» disclosure** in the catch-all, `src/index.html:138-146`.
    Do not delete it: the catch-all exists precisely for symptoms that map to no single category,
    and a flat battery is one of the commonest calls a repair shop takes. What must go is the claim
    that the shop regenerates packs, and the link to the retired page.

    Replace the body with a condensation of what `src/smyana-na-buksa.html` already says — that a
    worn pack, a failed charger, a broken cable and a damaged charging jack produce a very similar
    picture, and that all of them get checked before anything is proposed. Route the disclosure's
    links to `smyana-na-buksa.html` and to the category-2 hub through the existing accessor
    functions, never as hand-typed hrefs. Do not state or imply that the shop supplies, sells or
    fits a replacement battery: the sales line is discontinued (D3.5-01) and no source supports it.

    **(d) Fix the remaining homepage instances.** The card-grid comment at line 55 names the
    withdrawn service as one of the items that map to none of the six categories; reword it to name
    only what still applies. The hero trust badge at line 48 and the CTA note at line 272 take the
    free-diagnostics strings 03.5-01 added to `site-config.php` — read them from config, do not
    retype the literal, so the site has one writer for this claim. The CTA note is the surface that
    carries the category-6 exclusion; the hero badge carries only the claim, because a pill is not
    where a carve-out belongs and the two are visible together on the same viewport.

    **(e) Rewrite `$torin_desc` at line 15.** It currently ends by listing the withdrawn battery
    service, and adding «първоначална» to it would take it to 143 code points against a hard ceiling
    of 140. Rewrite the whole description rather than patching it. It must land within 120-140 code
    points, keep the call to action before code point 95, stay distinct from all other page
    descriptions, and name only services that still exist.

    **(f) The brand row is injected at render time — source and rendered section counts differ by
    exactly one. Never check one domain against the other's number.**

    `torin_render_brand_row()` (`src/includes/brand-row.php:64`) emits its own
    `<section class="section brands">`, untinted, and `src/index.html:93` calls it between the
    categories section (which closes at line 86) and the catch-all section (which opens at 120).
    Being a PHP include, it appears in **no** `<section` literal in the source. That one injected
    band is what keeps the two tinted bands on either side of it from being neighbours.

    Measured 2026-09-12 on HEAD, both domains:

    | | source `<section` literals | rendered sections | tinted | adjacent tinted pairs |
    |---|---|---|---|---|
    | HEAD | 7 (lines 33, 69, 120, 193, 227, 244, 256) | 8 | 4 | **0** |
    | after this task deletes 193 and 227 | **5** | **6** | **3** | **0** |

    The rendered sequence after this task is: plain(hero) · tint(categories) · plain(brands) ·
    tint(catch-all) · plain(self-diagnostic) · tint(contact). Strict alternation, no adjacent pair,
    before and after.

    So: the automated gate in this task greps **source** and expects **5** and **3**. The live check
    in plan 03.5-07 Task 2 measures **rendered** and expects **6**, **3**, and
    `adjacentTintedPairs: 0`. Both are right; they count different things. A future reader who greps
    the source to check a rendered figure will conclude a section is missing and add one — inventing
    content in the one phase that forbids it (D3.5-08). That is the mistake this note exists to stop.

    `adjacentTintedPairs: 0` is a property that **must hold** after this task, not a known failure to
    tolerate. If the probe ever reports non-zero, the likeliest cause is the brand row having
    vanished, not a tint class being wrong: `torin_render_brand_row()` emits **nothing at all** when
    the brand list is empty, and plan 03.5-01 edits exactly that list to drop the two excluded
    manufacturers (D3.5-05). Five or six brands remain, so the row still renders — but if a later
    change empties it, the two tinted bands become neighbours and this gate is the alarm. Diagnose
    the brand list first; do not add a section and do not change a tint class.

    Record the amended rendered order in the SUMMARY. **Do not edit `03-UI-SPEC.md`**: Phase 3 is
    closed and its contract is a historical artifact.

    Do not touch the brand row, the category grid, the self-diagnostic section or the CTA actions.
    Do not touch `src/css/components.css` — `.diff` rules become unused but the stylesheet is
    comment-stripped at deploy and has +15,171 B of headroom, and the Scope Fence forbids design-
    system changes beyond what removal requires. Plan 03.5-06 owns the one CSS comment that must
    change.
  </action>
  <verify>
    <automated>node scripts/truth-gate.js src/index.html &amp;&amp; node scripts/seo-metadata-check.js &amp;&amp; test "$(grep -o '<section' src/index.html | wc -l | tr -d ' ')" = 5 &amp;&amp; test "$(grep -o 'torin_render_evidence' src/index.html | wc -l | tr -d ' ')" = 0 &amp;&amp; test "$(grep -o 'section--tint' src/index.html | wc -l | tr -d ' ')" = 3 &amp;&amp; echo HOMEPAGE-GATES-OK</automated>
    <live-check status="NOT RUN — executor cannot deploy">See Task 3 and plan 03.5-07.</live-check>
  </verify>
  <acceptance_criteria>
    - `node scripts/truth-gate.js src/index.html` exits 0 — zero Class-A hits, comments included.
    - **Source domain** (`grep -o '<section' src/index.html | wc -l`): exactly **5** openings and
      **3** `section--tint`. HEAD carries 7 and 4; this task deletes 193 and 227, one of them tinted.
    - **Rendered domain** (served HTML, checked in plan 03.5-07 Task 2): exactly **6** sections and
      **3** tinted, alternating, `adjacentTintedPairs: 0`. The extra section is the brand row, which
      `torin_render_brand_row()` injects at render time and which therefore has no source literal.
    - These two numbers are both correct and differ by exactly one, always. **Never satisfy one by
      editing toward the other**, and never close a shortfall by adding a section — this is the one
      phase in the project that forbids inventing content (D3.5-08). Re-read the deletion boundaries
      instead.
    - `torin_render_evidence` is called zero times from the homepage; none of the three withdrawn
      photograph filenames appears anywhere in the file.
    - The «Батерията не држи» disclosure still exists, its body names no service the shop no longer
      offers, and every href in it comes from an accessor call rather than a typed filename.
    - `node scripts/seo-metadata-check.js` exits 0 with the homepage description inside 120-140 code
      points, its call before code point 95, and distinct from every other page.
    - Zero occurrences of the retired battery page's filename anywhere in `src/index.html`.
    - Cyrillic token count of `src/index.html` is reported in the SUMMARY. It will fall from 465;
      that is expected and is not padded back. If it falls below 300, say so and explain what a
      visitor now sees between the catch-all and the self-diagnostic block rather than silently
      shipping a thin page.
  </acceptance_criteria>
  <done>The homepage describes only services the shop offers, and still reads as a whole page.</done>
</task>

<task type="auto">
  <name>Task 2: Redraw the category 1/2 boundary in categories.php</name>
  <files>src/includes/categories.php</files>
  <read_first>
    - `src/includes/categories.php` — the six records; only two `symptoms` values change
    - `src/includes/services.php` — the five child records and `torin_service_href()`; read it to
      confirm what must NOT change
    - `.planning/phases/03.5-content-truth-revision/03.5-CONTEXT.md` D3.5-04 — the exact revised
      symptom lines, quoted verbatim by the owner
    - `src/mehanichni-problemi.html:95-98` — read-only. Category 1's page already links to four of
      the five child pages through `torin_service_href()`. This task asserts that; plan 03.5-06 owns
      the file.
  </read_first>
  <action>
    Replace exactly two `symptoms` values, per D3.5-04:

    - `kat-1` («Счупвания и механични повреди») becomes the four-item line naming a dropped laptop,
      a broken case, a cracked screen and broken hinges. Note what changed: hinges move from
      «разхлабени» (loose) to «счупени» (broken), and a cracked screen is **added** — it is physical
      damage and belongs here.
    - `kat-2` («Екран, клавиатура и портове») becomes the three-item line naming a screen that does
      not light up, keys that do not respond, and a port that does not charge. The cracked screen
      that currently leads this line is removed — it is the «пукнат екран» misplacement ROADMAP SC-5
      names.

    The customer-facing rule these two lines encode is the observable proxy, not the technician's
    one: category 1 is «има видима повреда», category 2 is «изглежда здрав, но не работи». Write
    that rule into a comment above the two records so the next reader does not re-derive the
    technician's version. The comment must not quote either symptom line verbatim — an
    acceptance criterion greps for them.

    **Do not re-parent any of the five child records.** D3.5-04 is explicit: they are COMPONENT
    pages, a component fails both ways, and a cracked screen and a dark screen are the same repair
    reached from either category. `svc-panti` keeps `'parent' => 'kat-2'`. Record the reason in a
    comment on that record, because it looks like an oversight to anyone who reads the new kat-1
    symptom line and nothing else. Changing it would also rewrite that page's breadcrumb and its
    indexed `BreadcrumbList`, for no gain.

    The `[ASSUMED]` marker stays on every symptom line. OWNER-QUESTIONS #16 — the real customer
    phrasing — was deferred by the owner to developer research and is not answered by this phase.
    Update the marker's comment to say the line is now owner-derived for these two records, still
    unconfirmed as literal customer speech.

    Do not touch the six category names, the page filenames, the icons, or any `published` flag.
    `kat-6` stays `false` (CONTEXT Deferred).
  </action>
  <verify>
    <automated>node scripts/truth-gate.js src/includes/categories.php &amp;&amp; grep -oF 'пукнат екран' src/includes/categories.php | wc -l | tr -d ' ' | grep -qx 1 &amp;&amp; grep -oE "'parent'[[:space:]]*=>[[:space:]]*'kat-2'" src/includes/services.php | wc -l | tr -d ' ' | grep -qx 5 &amp;&amp; grep -cE "(=>|=)[[:space:]]*\[|return[[:space:]]+\[" src/includes/categories.php | grep -qx 0 &amp;&amp; grep -o "'id'" src/includes/categories.php | wc -l | tr -d ' ' | grep -qx 6 &amp;&amp; grep -oE "torin_service_href\(" src/mehanichni-problemi.html | wc -l | tr -d ' ' | grep -qx 5 &amp;&amp; echo CATEGORY-GATES-OK</automated>
  </verify>
  <acceptance_criteria>
    - «пукнат екран» appears exactly once in `categories.php` — in kat-1's line, never kat-2's.
    - All five child records still carry `'parent' => 'kat-2'`; the count is exactly 5.
    - Exactly six category records survive (`'id'` count is 6) and `kat-6` is still unpublished.
    - Zero PHP 5.2 short-array literals under the corrected regex. Do **not** run a quote-balance
      check on this file: it has an odd single-quote count at HEAD from English possessives in
      comments, which is pre-existing, benign, and makes that gate useless here.
    - `src/mehanichni-problemi.html` resolves five child hrefs through `torin_service_href()`, so
      category 1 already reaches the hinge and screen pages it now names. (Read-only assertion — if
      this fails, do not fix it here; report it to plan 03.5-06, which owns that file.)
    - `node scripts/truth-gate.js src/includes/categories.php` exits 0.
  </acceptance_criteria>
  <done>Category 1 and 2 divide by failure type, and no child page was re-parented to achieve it.</done>
</task>

<task type="auto">
  <name>Task 3: Make the evidence gate in trust-signals.js opt-in, the way the urgent-block gate already is</name>
  <files>scripts/probes/trust-signals.js</files>
  <read_first>
    - `scripts/probes/trust-signals.js` — the `inconclusive` array and the `verdict` expression at
      the end of `run()`
    - `scripts/probes/svc-page.js:85-95` — the precedent. `SVC_EXPECT_URGENT=1` made a
      page-specific surface opt-in after the unconditional version reported INCONCLUSIVE on every
      correctly built child page.
    - `.planning/STATE.md` — the recorded reason: a verdict that is always INCONCLUSIVE "trains the
      reader to ignore the verdict"
  </read_first>
  <behavior>
    - With no environment variable set, a page served with zero `.evidence` strips is **reported**
      as `evidenceStrips: 0` and does **not** force INCONCLUSIVE.
    - With `TRUST_EXPECT_EVIDENCE=1`, a page served with zero `.evidence` strips **does** force
      INCONCLUSIVE, naming the absent surface.
    - Every other gate is unchanged and stays unconditional: brand row items, the trademark note,
      the 100x100 box contract, honest intrinsic width/height attributes, adjacent tinted pairs,
      horizontal scroll, single h1, empty headings, the image-did-not-load guard.
    - The rating badge keeps being reported rather than gated, but its comment is now wrong in the
      opposite direction — it says the absent badge is the specified shipped state. Correct it.
  </behavior>
  <action>
    Task 1 removes both homepage evidence strips, so `evidenceStrips` becomes 0 on `index.html`. The
    probe currently pushes that into `inconclusive` unconditionally, which would make every future
    homepage run INCONCLUSIVE forever — a verdict nobody reads is worse than no verdict, which is
    the exact finding that produced `SVC_EXPECT_URGENT`.

    Gate the zero-strip case behind `TRUST_EXPECT_EVIDENCE=1`, read in Node and injected into the
    evaluated expression the same way `svc-page.js` injects `expectUrgent`. Keep reporting
    `evidenceStrips` unconditionally — the number is still evidence even when it is not a gate.

    Update the probe's head comment, which currently describes measuring "the two differentiator
    sections with their evidence strips" on the homepage. Both are gone. Say what the probe now
    measures and why the evidence gate is opt-in, and name the plan and the date.

    Update the rating-badge comment: it states the absent badge is the specified state per UI-SPEC
    §2a. As of 03.5-01 the present state is what ships, so absence on the homepage is now a defect,
    not a contract. Leave the badge reported rather than gated — it is genuinely absent on the
    non-service pages this probe may be pointed at — but say so in the comment.

    Prove it in both directions. Absent surfaces forcing a false pass is the exact trap this file
    was hardened against, and `[].every()` is `true`: a change here that silently vacates the box
    check would be invisible.
  </action>
  <verify>
    <automated>for t in TRUST_EXPECT_EVIDENCE expectEvidence evidenceBoxesOk attrsHonest brandDuplicates adjacentTintedPairs; do grep -qF "$t" scripts/probes/trust-signals.js || { echo "LOST: $t"; exit 1; }; done; node --check scripts/probes/trust-signals.js &amp;&amp; echo PROBE-STATIC-OK</automated>
    <live-check status="NOT RUN — executor cannot deploy">
      Both directions must be exercised against a deployed page, in plan 03.5-07:
      `scripts/render-check.sh scripts/probes/trust-signals.js https://torin.bg/new/index.html 360 640`
      -> PASS with `evidenceStrips: 0` and an empty `inconclusive`; the same run prefixed with
      `TRUST_EXPECT_EVIDENCE=1` -> INCONCLUSIVE naming the absent strip. A flag proven in only one
      direction is not proven.
    </live-check>
  </verify>
  <acceptance_criteria>
    - `node --check scripts/probes/trust-signals.js` passes.
    - The opt-in flag name, its injected in-page counterpart, and every pre-existing gate identifier
      (`evidenceBoxesOk`, `attrsHonest`, `brandDuplicates`, `adjacentTintedPairs`) are all still
      present in the file — this task removes no gate, it conditions one.
    - The probe's head comment no longer describes the two deleted homepage sections, and the
      rating-badge comment no longer calls absence the specified state.
    - `evidenceStrips` is still returned unconditionally in the result object.
  </acceptance_criteria>
  <done>The homepage probe still measures something after the strips are gone, and says which.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config data -> rendered homepage | `categories.php` symptom strings and `site-config.php` diagnostics strings are interpolated into HTML |
| repository -> published claim | Any sentence left standing on the homepage is a promise the shop must keep |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-06 | Tampering | `.cat-card__symptoms` interpolation | low | mitigate | The existing `htmlspecialchars($cat['symptoms'], ENT_QUOTES, 'UTF-8')` at `index.html:80` stays exactly as it is. Values are developer-authored literals today, but this loop becomes Phase 4's sitemap generator (T-02-07) and the escaping is what keeps that safe when the source stops being a literal. |
| T-03.5-07 | Repudiation | A withdrawn claim left on the homepage | high | mitigate | `truth-gate.js` runs over `src/index.html` including its comments, with a hard-zero Class-A list. A customer acting on a claim the shop cannot honour is a real-world harm, not a cosmetic defect — this is why the gate is an exit code and not a report. |
| T-03.5-08 | Information disclosure | Orphaned reballing photographs on the staging server | low | accept | Task 1 removes the references; the files stay on the server because deploy uploads and never deletes, and they stay in `src/img/repairs/` until plan 03.5-04 removes them. Staging carries `X-Robots-Tag: noindex, nofollow`. Server-side cleanup is a manual FileZilla step recorded for Phase 4 cutover. |
| T-03.5-SC | Tampering | package-manager installs | n/a | accept | No package-manager install task exists in this plan. The probe change runs on bare Node 20 with no dependencies. |
</threat_model>

<verification>
- `node scripts/truth-gate.js src/index.html src/includes/categories.php` -> exit 0
- `node scripts/seo-metadata-check.js` -> exit 0, homepage description 120-140 code points, distinct
- Section arithmetic: 6 `<section` openings, 3 `section--tint`, alternating
- `node --check scripts/probes/trust-signals.js` passes with every pre-existing gate intact
- Live (plan 03.5-07): homepage 200, zero PHP warnings, `trust-signals.js` PASS at 360x640 and
  1440x900 with `adjacentTintedPairs: 0`, `horizontalScroll: false`, `ratingBadgePresent: true`;
  and the same probe with `TRUST_EXPECT_EVIDENCE=1` correctly goes INCONCLUSIVE
</verification>

<success_criteria>
- ROADMAP SC-1 met for `src/index.html`: zero Class-A hits, comments included.
- ROADMAP SC-3 met for the homepage: none of the three withdrawn photographs is referenced, and no
  evidence strip is left short — the homepage now renders none at all, which is the honest outcome
  of deleting both sections that carried them.
- ROADMAP SC-5 met: the two symptom lines encode «има видима повреда» versus «изглежда здрав, но не
  работи», the «пукнат екран» misplacement is gone, and no child page was re-parented.
- ROADMAP SC-4 advanced: the homepage no longer links to the retired battery page.
- ROADMAP SC-7 advanced: the homepage carries the reworded claim, sourced from config.
- ROADMAP SC-9 held for the homepage, pending the live check in 03.5-07.
</success_criteria>

<artifacts_this_phase_produces>
This plan contributes:
- `src/index.html` — five sections (three tinted), no withdrawn claims, reworded diagnostics claim, rewritten
  description, an honest battery-symptom disclosure
- `src/includes/categories.php` — the redrawn category 1/2 boundary
- `scripts/probes/trust-signals.js` — an evidence gate that is opt-in rather than permanently
  INCONCLUSIVE
- `03.5-02-SUMMARY.md` recording the amended homepage section order and the Class-B survivors
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03.5-content-truth-revision/03.5-02-SUMMARY.md` when done.

It must record:
- the amended homepage section order and tint assignment, as a table, because `03-UI-SPEC.md` is a
  closed-phase artifact and is deliberately not rewritten
- the homepage's Cyrillic token count before and after, with a plain statement of what a visitor now
  sees where the two sections were
- the source of every sentence in the rewritten battery disclosure, file and line
- every Class-B survivor in the two source files, one justification line each
- the exact deploy command for the orchestrator: `scripts/deploy-new.sh index.html includes/categories.php`
</output>
