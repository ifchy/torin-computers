---
phase: 03-content-trust-signal-build-out
plan: 06
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/uslovia.html
  - src/warrently.html
  - src/msg.html
  - src/laptopi.html
  - src/rezervni-chasti.html
  - src/covid.html
  - src/img/eu/
autonomous: true
requirements: [SEO-01, TRUST-03]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A visitor reading the privacy declaration sees commitments scoped to this site alone — the second domain it used to cover no longer exists and the declaration no longer claims to govern it"
    - "A visitor reading the warranty page sees the full terms the shared per-page summary is drawn from, so the summary and the full page cannot disagree"
    - "No page in the new tree still serves the temporary-skeleton placeholder sentence"
    - "The EU funding disclosure renders with its logos on ASCII-only asset paths — no served path depends on percent-encoded Cyrillic"
    - "The funding disclosure's factual content, including the results paragraph, is byte-identical in meaning to the live original; the discrepancy in it is on the owner's list rather than edited away"
    - "The two sales pages state honestly that availability must be confirmed by phone rather than implying a stocked catalogue"
    - "Every ported page carries corrected spelling where the source was visibly wrong, and no ported page carries a second copy of a contact fact the shared config owns"
  artifacts:
    - path: "src/uslovia.html"
      provides: "Privacy declaration and terms, scoped to this site only"
      min_lines: 40
    - path: "src/warrently.html"
      provides: "Full warranty terms — the source the shared summary is drawn from"
      min_lines: 50
    - path: "src/covid.html"
      provides: "EU funding disclosure with ASCII asset paths"
      min_lines: 40
    - path: "src/img/eu/"
      provides: "Programme logos renamed to ASCII on port"
  key_links:
    - from: "src/includes/site-config.php"
      to: "src/warrently.html"
      via: "the shared warranty summary's href points at the full terms on this page"
      pattern: "warrently.html"
    - from: "src/includes/footer.php"
      to: "src/covid.html"
      via: "the existing footer disclosure link, which must survive the port"
      pattern: "covid.html"
  prohibitions:
    - "MUST NOT edit the factual content of the funding disclosure while porting it — the wrong company name in its results paragraph stays exactly as published and is raised with the owner instead"
    - "MUST NOT let the privacy declaration keep making data-processing commitments on behalf of a site that no longer exists; the minimal correct edit narrows the scope rather than deleting the declaration"
    - "MUST NOT write sales copy implying stock, price or delivery for a business line whose status is unconfirmed (OWNER-QUESTIONS #15) — the honest form is an invitation to call and confirm"
---

<objective>
Port the six legal and utility pages, fixing what is visibly wrong on the way and stopping short of
the one thing that must not be touched.

These are the pages D3-13 assigns to near-verbatim porting rather than rewriting — but a port is not
a copy, and each of the six carries something broken. The privacy declaration extends
data-processing commitments to a second domain that no longer exists, which is not a dead link to
delete but a live legal statement to narrow. The warranty page is the source the shared per-page
summary is drawn from, so if it is not ported faithfully the summary and the full terms drift apart.
The disclosure page's logo filenames contain Cyrillic characters and plus signs, a known hazard on
shared hosting over FTP. And two sales pages describe a business line nobody has confirmed is still
running.

Purpose: six of the sixteen locked URLs stop being placeholders, without any of them acquiring a
claim the shop has not made.
Output: six ported pages and one renamed asset set.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-content-trust-signal-build-out/03-CONTEXT.md
@.planning/phases/03-content-trust-signal-build-out/03-RESEARCH.md
@.planning/phases/03-content-trust-signal-build-out/03-PATTERNS.md
@.planning/phases/03-content-trust-signal-build-out/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The privacy declaration and the full warranty terms</name>
  <files>src/uslovia.html, src/warrently.html</files>
  <read_first>
    - `src/uslovia.html` and `src/warrently.html` — the two current stubs, and the non-category page frame they use (`main class="section"` with a container, not the service-page renderer).
    - `site-current/uslovia.html` — read the whole page. Line 109 is the sentence that must be narrowed: it makes consent-and-processing commitments on behalf of two sites, one of which is gone. Read the surrounding paragraphs so the narrowing does not break the sentence's grammar or its meaning. Note also the retention commitment in the same declaration — data deleted once the enquiry is satisfied — because the Phase 4 contact-form work must not contradict it.
    - `site-current/warrently.html` — read lines 105-135 in full. This is the source of every warranty fact on the site: the one-month term for all services, free warranty service at the office with transport at the customer's cost, what the warranty covers and what voids it, the re-repair and refund rules, the success rate, the micro-crack rationale, the cooling-system correction, and the usage requirement.
    - `src/includes/site-config.php` — the `warranty` keyed set written by plan 03-01. The full page and the shared summary must agree; the summary reframes the usage requirement and this page carries the original terms in full.
    - `.planning/phases/03-content-trust-signal-build-out/03-RESEARCH.md` §Compliance Findings C-2 — the exact analysis of the declaration sentence and why the minimal edit is a narrowing rather than a deletion.
    - `.planning/phases/03-content-trust-signal-build-out/03-CONTEXT.md` §D3-13 — a port still means fixing what is visibly wrong.
  </read_first>
  <action>
**`src/uslovia.html`** — port the privacy declaration and terms near-verbatim into the non-category
page frame, as escaped Bulgarian strings rendered from the page file, never as ported markup.

One substantive edit: the declaration currently extends consent, collection and processing
commitments to a second domain alongside this one. That domain is dead (D3-12). Narrow the sentence
so the declaration is scoped to this site alone, keeping its grammar intact — remove the second
domain and its conjunction, nothing more. Do **not** delete the declaration and do not rewrite any
other clause; this is a live legal statement, and the smallest correct change is the right one. The
source comment recording the edit must cite D3-12 by decision id and must not spell the removed
domain, because the grep gate over `src/` is what enforces the do-not-import rule and a comment
naming it would trip its own gate.

Two spelling corrections: CONTEXT D3-13 names one misspelling on this page — locate it by comparing
the source against the correct form `условията` — and fix any other obvious misspelling you find
while porting. Do not name a misspelled form in a source comment; state that spelling was corrected
on port and leave it there.

Keep the retention commitment as written, and add a source comment noting that the Phase 4
contact-form work must not contradict it.

- `$torin_title` = `Общи условия и поверителност · Торин`
- `$torin_desc` — 120-140 characters. This page has no commercial intent; the description should
  describe the page plainly rather than chase a keyword.

**`src/warrently.html`** — port the full warranty terms near-verbatim into the same frame. This page
is the authority the shared summary points at, so it must carry every term: the one-month period for
all services, free warranty service performed at the office with transport at the customer's cost,
what the warranty covers and what voids it, the re-repair and refund rules, and a new one-month
period after a successful re-repair.

Port the usage requirement as it is written — this page is where the full condition legitimately
lives, in the shop's own framing, alongside its stated reason. The reframing belongs to the shared
summary that appears on every service page; the full terms page carries the original. Add a source
comment stating that split explicitly, so a later editor does not "harmonise" the two by weakening
this page or by hardening the summary.

- `$torin_title` = `Гаранционни условия · Торин Компютърс`
- `$torin_desc` — 120-140 characters.

Both pages: no brand row, no warranty summary block, no evidence strip — these are legal pages and
the brand row's trademark disclaimer on a privacy policy would be noise. Neither page may retype a
phone number, the address or the opening hours.
  </action>
  <verify>
    <automated>
      set -e
      # `! cmd` does NOT trip set -e (POSIX suppresses errexit for inverted status), so every
      # negative assertion below runs through nomatch, which checks the status and halts.
      nomatch() { if "$@"; then echo "GATE FAILED - unexpected match: $*" >&2; exit 1; fi; }
      for f in uslovia warrently; do
        nomatch grep -c 'Тази страница е временен скелет' "src/$f.html"
        nomatch grep -rniE 'smartbattery' "src/$f.html"
        nomatch grep -rnE ' on[a-z]+=|javascript:|class="(fa |col-md-|mb-[0-9]|font-size-)' "src/$f.html"
        nomatch grep -rn '__DIR__' "src/$f.html"
        nomatch grep -rnE '(=>[^;]*\]|\[\s*[^]]*=>)' "src/$f.html"
        nomatch grep -nE '02 9549710|088 9458404|087 9128244|Свети Иван Рилски|8:00|16:00' "src/$f.html"
        nomatch grep -n 'brand-row' "src/$f.html"
        node -e 'const s=require("fs").readFileSync(process.argv[1],"utf8");if((s.match(/'"'"'/g)||[]).length%2)process.exit(1)' "src/$f.html"
      done
      nomatch grep -rniE 'smartbattery' src/
      nomatch grep -n 'усливията' src/uslovia.html
      grep -c 'условията' src/uslovia.html                              # >= 1
      grep -c 'D3-12' src/uslovia.html                                  # >= 1
      grep -c 'torin.bg' src/uslovia.html                               # >= 1
      grep -c '1 месец' src/warrently.html                              # >= 1
      W=$(tr -cs '[:alpha:][:digit:]' ' ' < src/warrently.html | wc -w); test "$W" -ge 400
      # LIVE
      scripts/deploy-new.sh uslovia.html warrently.html
      for f in uslovia warrently; do
        U="https://torin.bg/new/$f.html"
        curl -sf -o /dev/null -w "$f %{http_code}\n" "$U"
        test "$(curl -s "$U" | grep -c '<h1')" -eq 1
        test "$(curl -s "$U" | grep -cE '<h[23][^>]*>[[:space:]]*</h[23]>')" -eq 0
        test "$(curl -s "$U" | grep -ci 'smartbattery')" -eq 0
        test "$(curl -s "$U" | grep -c 'brand-row__item')" -eq 0
      done
      # the shared summary points at a page that exists and states the same term
      curl -s https://torin.bg/new/zalivane-technosti.html | grep -c 'warrently.html'   # >= 1
      curl -s https://torin.bg/new/warrently.html | grep -c '1 месец'                   # >= 1
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'усливията' src/uslovia.html` returns no match and `grep -c 'условията'` returns at least 1 — the misspelling is corrected and its misspelled form appears nowhere, including in comments.
    - `src/uslovia.html` still names this site in the declaration (`grep -c 'torin.bg'` returns at least 1) and `grep -rniE 'smartbattery' src/` returns no match — the sentence was narrowed, not deleted.
    - `grep -c 'D3-12' src/uslovia.html` returns at least 1 — the edit is recorded by decision id.
    - `src/warrently.html` contains the one-month term and at least 400 words of ported terms.
    - Neither page contains a brand row, a phone number, the street address or an opening-hours string.
    - LIVE both pages return 200 with one `<h1`, zero empty headings, zero brand-row items and zero case-insensitive matches for the dead domain.
    - LIVE a service page's warranty block links to `warrently.html`, and that page states the same term the shared summary does.
  </acceptance_criteria>
  <done>The privacy declaration no longer speaks for a site that does not exist, and the full warranty terms are on the page the per-page summary points at.</done>
</task>

<task type="auto">
  <name>Task 2: The funding disclosure and its ASCII asset paths, and the message page</name>
  <files>src/covid.html, src/msg.html, src/img/eu/</files>
  <read_first>
    - `src/covid.html` and `src/msg.html` — the two current stubs.
    - `site-current/covid.html` — read lines 130-160. Line 140 is the results paragraph, which names a different company than the beneficiary; lines 154-155 carry the contract number, the amounts and the implementation dates. This paragraph must be ported exactly as published.
    - `site-current/covid-19/` — the three logo files. Two of the three filenames contain Cyrillic characters and plus signs.
    - `src/includes/footer.php` around line 111 — the existing disclosure link, which must keep resolving.
    - `.planning/phases/03-content-trust-signal-build-out/03-RESEARCH.md` §Compliance Findings C-1 — the four numbered conclusions, in particular that this page stays live, stays linked and is not retired, and that its factual content is not ours to edit.
    - `.planning/phases/03-content-trust-signal-build-out/03-05-SUMMARY.md` — the owner-question number plan 03-05 assigned to the company-name discrepancy, so this page's source comment can reference it rather than restating the problem.
  </read_first>
  <action>
**Port the logo assets.** Copy the three programme logos from `site-current/covid-19/` into a new
`src/img/eu/` directory, renaming every one to ASCII-only: `eu-erdf.png`, `opic.png` and
`logos.png`. Cyrillic path segments require percent-encoding and are a known hazard on shared hosting
over FTP; the rename is not cosmetic. Run each through `sips` once to strip metadata without
resizing. Record the old-to-new filename mapping in the SUMMARY.

**`src/covid.html`** — port the disclosure into the non-category page frame as escaped strings.
Carry the contract number, the beneficiary, the amounts, the funding split and the implementation
dates exactly as published. Render the three logos as plain `<img>` elements with explicit `width`
and `height` attributes read from the files, and descriptive `alt` text naming the programme each
logo represents.

The results paragraph is ported **exactly as published, including the company name that does not
match the beneficiary**. Do not correct it, do not paraphrase around it, and do not add a bracketed
editorial note. Plan 03-05 has already put the discrepancy on the owner's list; add a source comment
here referencing that owner-question number and stating that the sentence is deliberately unchanged
pending an answer. This is the one place in this phase where improving while porting stops and asks.

- `$torin_title` = `Проект BG16RFOP002-2.073 · Торин Компютърс`
- `$torin_desc` — 120-140 characters describing the disclosure plainly. This page has no commercial
  intent and its description must not chase a repair keyword.

**`src/msg.html`** — the form-submission confirmation page. Port it as a short, honest confirmation:
the message was sent, the shop replies during working hours, and the phone is faster if the matter is
urgent. Do not restate the working hours as a literal — reference the shared config value the footer
already renders. Two or three sentences plus a link back to the homepage is the correct length; this
page is a destination, not a content page.

- `$torin_title` = `Съобщението е изпратено · Торин`
- `$torin_desc` — short and plain.

Neither page carries a brand row, a warranty block or an evidence strip.
  </action>
  <verify>
    <automated>
      set -e
      # `! cmd` does NOT trip set -e (POSIX suppresses errexit for inverted status), so every
      # negative assertion below runs through nomatch, which checks the status and halts.
      nomatch() { if "$@"; then echo "GATE FAILED - unexpected match: $*" >&2; exit 1; fi; }
      test -d src/img/eu
      test "$(ls src/img/eu/*.png | wc -l)" -eq 3
      test "$(ls src/img/eu/ | grep -cP '[^\x00-\x7F]')" -eq 0
      test "$(ls src/img/eu/ | grep -cF '+')" -eq 0
      grep -c 'BG16RFOP002-2.073' src/covid.html                        # >= 1
      grep -c 'Венера-АКС' src/covid.html                               # >= 1 — ported exactly as published
      grep -c 'img/eu/' src/covid.html                                  # 3
      test "$(grep -oE '<img[^>]*img/eu/[^>]*>' src/covid.html | grep -c 'width=')" -eq 3
      test "$(grep -oE '<img[^>]*img/eu/[^>]*>' src/covid.html | grep -c 'alt=')" -eq 3
      for f in covid msg; do
        nomatch grep -c 'Тази страница е временен скелет' "src/$f.html"
        nomatch grep -rnE ' on[a-z]+=|javascript:|class="(fa |col-md-|mb-[0-9]|font-size-)' "src/$f.html"
        nomatch grep -rn '__DIR__' "src/$f.html"
        nomatch grep -n 'brand-row' "src/$f.html"
        node -e 'const s=require("fs").readFileSync(process.argv[1],"utf8");if((s.match(/'"'"'/g)||[]).length%2)process.exit(1)' "src/$f.html"
      done
      nomatch grep -nE '8:00|16:00|02 9549710' src/msg.html
      # LIVE
      scripts/deploy-new.sh img/eu/eu-erdf.png img/eu/opic.png img/eu/logos.png covid.html msg.html
      for f in covid msg; do
        curl -sf -o /dev/null -w "$f %{http_code}\n" "https://torin.bg/new/$f.html"
        test "$(curl -s "https://torin.bg/new/$f.html" | grep -c '<h1')" -eq 1
        test "$(curl -s "https://torin.bg/new/$f.html" | grep -cE '<h[23][^>]*>[[:space:]]*</h[23]>')" -eq 0
      done
      for i in eu-erdf opic logos; do curl -sf -o /dev/null -w "$i %{http_code}\n" "https://torin.bg/new/img/eu/$i.png"; done
      curl -s https://torin.bg/new/index.html | grep -c 'covid.html'    # >= 1 (footer link survives)
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/img/eu/` contains exactly three PNG files, none with a non-ASCII byte or a plus sign in its name, and the SUMMARY records the old-to-new mapping.
    - `src/covid.html` contains the contract number, references all three logos under `img/eu/`, and every one of those three `<img>` elements carries both `width` and `alt`.
    - `grep -c 'Венера-АКС' src/covid.html` returns at least 1 — the results paragraph is ported exactly as published, and a source comment references the owner-question number plan 03-05 assigned rather than correcting the sentence.
    - `src/msg.html` contains no literal opening-hours or phone string.
    - Neither page carries a brand row.
    - LIVE both pages return 200 with one `<h1` and zero empty headings; all three logo URLs return 200; the footer disclosure link still resolves from the homepage.
  </acceptance_criteria>
  <done>The funding disclosure renders with its logos on safe paths and its published wording intact, and the confirmation page says something useful.</done>
</task>

<task type="auto">
  <name>Task 3: The two sales pages, ported honestly</name>
  <files>src/laptopi.html, src/rezervni-chasti.html</files>
  <read_first>
    - `src/laptopi.html` and `src/rezervni-chasti.html` — the two current stubs and their existing working titles.
    - `site-current/laptopi.html` and `site-current/rezervni-chasti.html` — the thin source pages. There is not much here, and that is the finding, not a gap to fill.
    - `.planning/OWNER-QUESTIONS.md` item #15 — whether the sales line is still active and whether it should have navigation prominence. It is open, and it is what bounds these two pages.
    - `.planning/phases/03-content-trust-signal-build-out/03-RESEARCH.md` §Open Questions OQ-7 — the recommended default: port thin, honest, call-to-confirm copy, and do not let the metadata chase commercial keywords.
    - `src/includes/site-config.php` — the contact values these pages point at rather than retyping.
  </read_first>
  <action>
Port both pages into the non-category page frame as short, honest pages. Neither is padded to the
600-word service-page bar — that bar applies to service pages, and inflating a page about a business
line nobody has confirmed is running would be the wrong kind of depth.

**`src/laptopi.html`**
- `$torin_title` = `Употребявани лаптопи · Торин`
- `$torin_desc` — 120-140 characters that describe availability as something to confirm by phone,
  and that do not chase a commercial keyword.
- Body: two or three short paragraphs. State plainly that availability changes, that stock is not
  listed online, and that a phone call is the way to ask what is currently available and at what
  condition. Add a call-to-action link to the homepage contact anchor. Add a source comment marking
  the whole page `[ASSUMED]` against OWNER-QUESTIONS #15 and stating that if the sales line is
  dormant the page becomes a candidate for retirement — which is an owner decision and a Phase 4
  cutover question, not something to act on here.

**`src/rezervni-chasti.html`**
- `$torin_title` = `Резервни части за лаптопи · Торин`
- `$torin_desc` — 120-140 characters, same posture.
- Body: two or three short paragraphs. State that parts are sourced per repair rather than sold from
  a listed catalogue, that the part a visitor needs is identified from the machine and not from a
  model number alone, and that a call with the laptop's model is the way to ask. Link to
  `test-laptop.html` for visitors who are not sure which part has failed, and to the homepage contact
  anchor. Same `[ASSUMED]` source comment against #15.

Neither page may state a price, a delivery time, a stock quantity or a warranty term. Neither carries
a brand row, a warranty block or an evidence strip. Neither retypes a phone number, the address or
the opening hours.
  </action>
  <verify>
    <automated>
      set -e
      # `! cmd` does NOT trip set -e (POSIX suppresses errexit for inverted status), so every
      # negative assertion below runs through nomatch, which checks the status and halts.
      nomatch() { if "$@"; then echo "GATE FAILED - unexpected match: $*" >&2; exit 1; fi; }
      for f in laptopi rezervni-chasti; do
        nomatch grep -c 'Тази страница е временен скелет' "src/$f.html"
        grep -c 'OWNER-QUESTIONS #15' "src/$f.html"                     # >= 1
        grep -c 'ASSUMED' "src/$f.html"                                 # >= 1
        nomatch grep -rniE 'лв\.|лева|евро|EUR|доставка за|работни дни|наличност от' "src/$f.html"
        nomatch grep -nE '02 9549710|088 9458404|087 9128244|Свети Иван Рилски|8:00|16:00' "src/$f.html"
        nomatch grep -n 'brand-row' "src/$f.html"
        nomatch grep -rnE ' on[a-z]+=|javascript:|class="(fa |col-md-|mb-[0-9]|font-size-)' "src/$f.html"
        nomatch grep -rn '__DIR__' "src/$f.html"
        node -e 'const s=require("fs").readFileSync(process.argv[1],"utf8");if((s.match(/'"'"'/g)||[]).length%2)process.exit(1)' "src/$f.html"
      done
      grep -c 'test-laptop.html' src/rezervni-chasti.html               # >= 1
      # No stub remains among THIS plan's six pages. The tree-wide assertion belongs to
      # plan 03-09, because the other stubs are cleared by sibling plans running in the
      # same wave and a tree-wide gate here would fail on their timing rather than on this work.
      nomatch grep -l 'Тази страница е временен скелет' src/uslovia.html src/warrently.html src/msg.html src/laptopi.html src/rezervni-chasti.html src/covid.html
      # LIVE
      scripts/deploy-new.sh laptopi.html rezervni-chasti.html
      for f in laptopi rezervni-chasti; do
        U="https://torin.bg/new/$f.html"
        curl -sf -o /dev/null -w "$f %{http_code}\n" "$U"
        test "$(curl -s "$U" | grep -c '<h1')" -eq 1
        test "$(curl -s "$U" | grep -cE '<h[23][^>]*>[[:space:]]*</h[23]>')" -eq 0
        curl -s "$U" | grep -c 'index.html#contact-us'                  # >= 1
      done
    </automated>
  </verify>
  <acceptance_criteria>
    - Both pages carry an `[ASSUMED]` source comment naming OWNER-QUESTIONS #15.
    - `grep -rniE 'лв\.|лева|евро|EUR|доставка за|работни дни|наличност от'` returns no match on either page — neither implies price, delivery or stock.
    - Neither page retypes a phone number, the address or the opening hours; neither carries a brand row.
    - `src/rezervni-chasti.html` links to `test-laptop.html`.
    - None of this plan's six pages still carries the stub sentinel sentence (the tree-wide assertion is plan 03-09's, because sibling plans in this wave clear the remaining stubs).
    - LIVE both pages return 200 with one `<h1`, zero empty headings, and a link to the homepage contact anchor.
  </acceptance_criteria>
  <done>Both sales pages say something true and useful without promising a business line whose status nobody has confirmed.</done>
</task>

</tasks>

## Artifacts this phase produces

New symbols created by this plan:

- **New directory:** `src/img/eu/` containing `eu-erdf.png`, `opic.png`, `logos.png` (renamed on port from non-ASCII source filenames)
- **New page content (files already existed as stubs):** `src/uslovia.html`, `src/warrently.html`, `src/msg.html`, `src/laptopi.html`, `src/rezervni-chasti.html`, `src/covid.html`
- **New PHP functions, CSS selectors, config keys or includes:** none

## Probe rows discharged by this plan

| Source | Row | Disposition |
|---|---|---|
| Edge probe | SEO-01 / empty | partially covered here — every one of these six pages assigns both metadata variables explicitly rather than relying on the site-level fallback; the site-wide assertion is owned by plan 03-09 |
| RESEARCH | C-2 (privacy declaration naming a dead domain) | covered → truth |
| RESEARCH | C-1 conclusion 4 (the company-name discrepancy) | covered → prohibition; ported unchanged and raised with the owner by plan 03-05 |

## Flagged planner assumptions

- **RESEARCH A8 — the legacy tree is a faithful mirror of what is live today.** Every ported string
  in this plan comes from that tree rather than from a fresh fetch of the production site. The risk is
  low (the inventory was captured deliberately in Phase 1) but it is real for legal text specifically,
  which is the one category where a stale copy matters. Worth one confirming look at the live privacy
  and warranty pages before cutover.
- **OWNER-QUESTIONS #15 is open and bounds two of these pages.** If the sales line is dormant, both
  pages become retirement candidates — an owner decision and a Phase 4 question. They are ported
  honestly rather than deleted, because they are indexed URLs and D3-01 keeps every one of them.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published legal text → data subject | The privacy declaration is a binding statement; its scope is a legal fact, not copy. |
| published disclosure → regulator / auditor | The funding disclosure's factual content is not ours to edit. |
| repository → served static assets | Three logo binaries move into the served tree under new names. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-27 | Repudiation | Privacy declaration scope | high | mitigate | The declaration is narrowed to this site rather than deleted or rewritten — the minimal correct edit for a live legal statement that currently makes commitments on behalf of a site that no longer exists. Gates: the site is still named, the dead domain is absent tree-wide, and the edit is recorded by decision id. |
| T-03-28 | Repudiation | Funding disclosure factual content | high | mitigate | Ported exactly as published, including the mismatched company name, with a source comment pointing at the owner question rather than an editorial correction. Gate: the published company name is still present in the file. |
| T-03-29 | Repudiation | Sales-page claims | medium | mitigate | No price, delivery time, stock quantity or warranty term appears on either sales page; availability is framed as something to confirm by phone. Gate: `! grep -rniE 'лв\.\|лева\|евро\|EUR\|доставка за\|работни дни\|наличност от'`. |
| T-03-30 | Denial of service | Non-ASCII asset paths | low | mitigate | All three logos are renamed to ASCII on port; a percent-encoding-dependent path is a known shared-hosting and FTP hazard. Gates: no non-ASCII byte and no plus sign in any filename, and all three URLs return 200 live. |
| T-03-31 | Injection | Legacy markup carried across with the copy | medium | mitigate | Port strings, not markup, on all six pages. Gate: `! grep -rnE ' on[a-z]+=\|javascript:\|class="(fa \|col-md-\|mb-[0-9]\|font-size-)'`. |
| T-03-32 | Information disclosure | Contact facts duplicated onto legal pages | low | mitigate | Gate: no page in this plan may carry a phone number, the address or an opening-hours string. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed; `03-RESEARCH.md` §Package Legitimacy Audit records the gate as not applicable. |
</threat_model>

<verification>
- All six pages return 200 live, each with one `<h1>` and zero empty headings.
- None of this plan's six pages still carries the stub sentinel sentence.
- `grep -rniE 'smartbattery' src/` returns no match; the privacy declaration still names this site.
- All three EU logo URLs return 200 and no served asset path contains a non-ASCII byte.
- The funding disclosure's results paragraph is unchanged from the published original.
- Neither sales page implies a price, a delivery time or stock.
</verification>

<success_criteria>
- Six of the sixteen locked URLs carry real content.
- The one legal statement that had become wrong is corrected by the smallest edit that fixes it.
- The one factual error that must not be corrected silently is still exactly as published.
</success_criteria>

<output>
Create `.planning/phases/03-content-trust-signal-build-out/03-06-SUMMARY.md` when done.

Record: the old-to-new logo filename mapping; the exact before-and-after text of the narrowed
declaration sentence, so a reviewer can confirm the edit was minimal; and a note that both sales
pages are `[ASSUMED]` against OWNER-QUESTIONS #15 and become retirement candidates only on an owner
decision at cutover.
</output>
