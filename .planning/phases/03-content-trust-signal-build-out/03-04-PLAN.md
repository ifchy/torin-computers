---
phase: 03-content-trust-signal-build-out
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/za-bateriite.html
  - src/test-laptop.html
  - src/profilaktika-laptop.html
autonomous: true
requirements: [DIFF-01, DIFF-02, DIFF-03, SEO-01]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A visitor following the homepage battery block reaches a depth page that explains how regeneration works, what cells are used and why the connections are spot-welded rather than soldered (DIFF-02)"
    - "The battery page states the regenerated-battery warranty term, and it does not contradict the one-month service warranty shown on every other service page — both are rendered from the one shared keyed set (probe DIFF-02/unclassified)"
    - "No page in src/ references the dead specialist battery domain in any form (D3-12)"
    - "A visitor using the self-diagnostic is routed from each symptom group to the service page that fixes it, and every one of those routing links resolves through the publish gate rather than a hand-typed filename (probe DIFF-01/unclassified)"
    - "A visitor on the self-diagnostic sees current instructions — a bootable USB stick, not a bootable optical disc"
    - "A visitor following the homepage chip-level block reaches a depth page showing what the shop's own equipment and consumables are, with photographic evidence (DIFF-03)"
    - "No ported page carries a second copy of the shop's working hours or phone number — those remain single-sourced in site-config.php (RESEARCH P-11)"
  artifacts:
    - path: "src/za-bateriite.html"
      provides: "DIFF-02 depth page: regeneration process, cell sourcing, the battery warranty term"
      min_lines: 80
    - path: "src/test-laptop.html"
      provides: "DIFF-01 self-diagnostic with per-symptom routing into the service pages"
      min_lines: 90
    - path: "src/profilaktika-laptop.html"
      provides: "DIFF-03 depth: the infrared reflow equipment, the flux, the temperature and durability evidence"
      min_lines: 80
  key_links:
    - from: "src/za-bateriite.html"
      to: "src/includes/site-config.php"
      via: "warranty_key 'battery' selects the shared regenerated-battery term; the page authors no warranty literal"
      pattern: "'warranty_key' *=> *'battery'"
    - from: "src/test-laptop.html"
      to: "src/includes/categories.php"
      via: "each symptom group links to its category through torin_category_href()"
      pattern: "torin_category_href"
    - from: "src/profilaktika-laptop.html"
      to: "src/img/repairs/"
      via: "evidence strip showing the reflow station and hot-air-gun damage"
      pattern: "'evidence'"
  prohibitions:
    - "MUST NOT assert a capability, a figure or a certification the shop's own copy does not state — the reflow machine, the flux brand, the temperature delta, the durability rate and the cell sourcing are quoted at their existing strength and not amplified"
    - "MUST NOT reference the dead specialist battery domain in any form, including in an explanatory source comment: the removal rationale cites D3-12 by decision id, and the grep gate over src/ is what enforces the do-not-import rule"
    - "MUST NOT state a battery warranty term that contradicts the service warranty shown elsewhere on the site — both are rendered from the one shared keyed set and the distinction between them is stated in the copy, both marked [ASSUMED] against OWNER-QUESTIONS #23"
---

<objective>
Give the three differentiators their depth pages — the pages the homepage blocks hand off to.

All three are pages the phase is unusually well resourced for. The battery story, the self-diagnostic
and the chip-level repair evidence all exist on the live site today, in the shop's own Bulgarian, at
real length and real quality. What is missing is that they are stubs in the new tree, that the battery
page's closing sentence points at a domain that no longer exists, that the self-diagnostic dead-ends
instead of routing anyone to a service, and that the strongest evidence the shop has is buried in a
page about cleaning.

Two corrections land here that nothing else in the phase would catch. The site states one month of
warranty on services and one year on regenerated batteries and has never said why they differ; the
shared keyed set makes that a stated distinction rather than a contradiction a customer discovers.
And the self-diagnostic still tells visitors to burn a bootable optical disc.

Purpose: phase success criterion 4 in full — the self-diagnostic, the battery-regeneration story and
the chip-level expertise each as distinct, prominent content rather than buried text.
Output: three finished depth pages.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-content-trust-signal-build-out/03-CONTEXT.md
@.planning/phases/03-content-trust-signal-build-out/03-RESEARCH.md
@.planning/phases/03-content-trust-signal-build-out/03-PATTERNS.md
@.planning/phases/03-content-trust-signal-build-out/03-UI-SPEC.md
@.planning/phases/03-content-trust-signal-build-out/03-01-SUMMARY.md
@.planning/phases/03-content-trust-signal-build-out/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: «За батериите» — the battery-regeneration depth page</name>
  <files>src/za-bateriite.html</files>
  <read_first>
    - `src/za-bateriite.html` — the current stub, and the metadata ordering it already demonstrates.
    - `src/includes/category-page.php` — the recognised-keys doc block as extended by plan 03-01. This page carries no `cat_id`; it sets `name` directly.
    - `src/includes/site-config.php` — the `warranty` keyed set written by plan 03-01, specifically the `battery` entry. This page selects that key and authors no warranty literal of its own.
    - `site-current/za-bateriite.html` — read the whole page (~1,900 words). Line 111 carries the regeneration process: cell replacement plus reprogramming the electronics so they register new cells, and spot welding with HILUMIN strip because any thermal intervention such as a soldering iron damages the cell chemistry. Line 129 carries the cell-certification claim and the one-year warranty. Line 158 is a pointer to a specialist site that no longer exists (D3-12) — it is dropped, and its removal must not be explained by naming the domain.
    - `src/index.html` as written by plan 03-02 — the homepage battery block this page is the depth destination for. Do not duplicate its paragraphs; extend them.
    - `.planning/phases/03-content-trust-signal-build-out/03-RESEARCH.md` §Common Pitfalls P-6 — the warranty numbers and why the distinction has to be stated rather than reconciled.
    - `.planning/phases/03-content-trust-signal-build-out/03-CONTEXT.md` §D3-11 and §D3-12.
  </read_first>
  <action>
Rewrite `src/za-bateriite.html` through the generalized service-page renderer, 600-1000 Bulgarian
words, against the D3-14 Definition of Done.

- `$torin_title` = `Регенерация на батерии за лаптоп · Торин`
- `$torin_desc` — 120-140 characters ending in a call to action, with the call inside the first 95.
- `'name'` = `Регенерация на батерии за лаптоп`
- `'h1'` = `Регенерация на батерии за лаптоп в София` (39 characters)
- `'crumbs'` = Начало → this page's own name.
- `'intro'` — one paragraph carrying the positioning: the shop stopped importing new batteries
  because of the cell quality it was receiving, and offers full regeneration instead, which in
  practice means it manufactures the pack to current standards.
- `'fixes'` — regeneration of laptop packs, cell replacement with electronics reprogramming, new
  electronics where the pack is password-locked, packs for cameras and other devices.
- `'blocks'` — three entries:
  1. `steps`, heading `Как протича регенерацията`, 5-6 items covering diagnosis and capacity
     measurement, opening the pack, replacing the cells, spot welding with HILUMIN strip, reprogramming
     or replacing the electronics so the pack reports its real new capacity, and load testing before
     handover.
  2. `prose`, heading `Защо заваряваме, а не запояваме`, two paragraphs on why any thermal
     intervention such as a soldering iron damages a cell's chemistry and reduces its working life —
     this is the shop's own technical argument and it is the page's strongest differentiator.
  3. `callout`, heading `Гаранцията на регенерирана батерия е различна`, two paragraphs stating
     plainly that a repair carries the one-month service warranty while a regenerated battery carries
     its own longer term, because one is a service and the other is a product the shop builds. Add a
     source comment marking this distinction `[ASSUMED]` against OWNER-QUESTIONS #23: the site has
     stated both numbers for years and has never said why they differ, and the shared keyed set is
     what stops them contradicting each other page to page.
- `'warranty_key'` = `battery`. The page must not author a warranty string.
- `'evidence'` — `baterry.jpg` and `baterry2.jpg` with their true intrinsic dimensions and captions
  describing what each diagram shows.
- `'process'` — 4-5 steps covering how a customer brings a pack in and what happens next.
- `'faq'` — 3-5 entries, one paragraph each: whether an old pack is always recoverable, how long the
  regenerated pack lasts, whether the laptop will report the capacity correctly, whether packs from
  other devices are accepted.
- `'related'` — links to `zalivane-technosti.html`, `tokov-udar.html` and `test-laptop.html`.

Drop the source page's closing pointer to the specialist site. The comment recording why must cite
D3-12 by decision id and must not spell the domain — the grep gate over `src/` is what enforces the
do-not-import rule, and a comment naming it would trip that gate. Do not port a second copy of the
working hours or the phone number; `site-config.php` owns those. Port the strings, not the markup.
  </action>
  <verify>
    <automated>
      set -e
      # `! cmd` does NOT trip set -e (POSIX suppresses errexit for inverted status), so every
      # negative assertion below runs through nomatch, which checks the status and halts.
      nomatch() { if "$@"; then echo "GATE FAILED - unexpected match: $*" >&2; exit 1; fi; }
      grep -c "'warranty_key' *=> *'battery'" src/za-bateriite.html      # 1
      nomatch grep -n "'warranty' *=>" src/za-bateriite.html
      nomatch grep -n "'cat_id'" src/za-bateriite.html
      grep -c 'torin_render_service_page' src/za-bateriite.html          # 1
      grep -c 'HILUMIN' src/za-bateriite.html                            # >= 1
      grep -c 'Panasonic' src/za-bateriite.html                          # >= 1
      grep -c 'ASSUMED' src/za-bateriite.html                            # >= 1
      grep -c 'D3-12' src/za-bateriite.html                              # >= 1
      nomatch grep -rniE 'smartbattery' src/
      nomatch grep -c 'Тази страница е временен скелет' src/za-bateriite.html
      nomatch grep -nE '8:00|9:00|02 9549710|088 9458404|087 9128244' src/za-bateriite.html
      nomatch grep -rnE ' on[a-z]+=|javascript:|class="(fa |col-md-|mb-[0-9]|font-size-)' src/za-bateriite.html
      nomatch grep -rn '__DIR__' src/za-bateriite.html
      nomatch grep -rnE '(=>[^;]*\]|\[\s*[^]]*=>)' src/za-bateriite.html
      W=$(sed -n "/= array(/,/^);/p" src/za-bateriite.html | tr -cs '[:alpha:][:digit:]' ' ' | wc -w); test "$W" -ge 600
      node -e 'const s=require("fs").readFileSync("src/za-bateriite.html","utf8");if((s.match(/'"'"'/g)||[]).length%2)process.exit(1)'
      # LIVE
      scripts/deploy-new.sh za-bateriite.html
      U=https://torin.bg/new/za-bateriite.html
      curl -sf -o /dev/null -w '%{http_code}\n' "$U"                      # 200
      curl -s "$U" | grep -c '1 година гаранция на регенерирана батерия'   # 1
      test "$(curl -s "$U" | grep -c '1 месец гаранция на всеки ремонт')" -eq 0
      curl -s https://torin.bg/new/zalivane-technosti.html | grep -c '1 месец гаранция на всеки ремонт'  # 1
      curl -s "$U" | grep -c 'class="evidence"'                            # 1
      test "$(curl -s "$U" | grep -c '<h1')" -eq 1
      test "$(curl -s "$U" | grep -cE '<h[23][^>]*>[[:space:]]*</h[23]>')" -eq 0
      test "$(curl -s "$U" | grep -ci 'smartbattery')" -eq 0
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'warranty_key' *=> *'battery'" src/za-bateriite.html` returns 1 and `grep -n "'warranty' *=>"` returns no match — the page selects a shared key and authors no warranty literal.
    - The page's data array contains at least 600 words and no longer contains the stub sentinel sentence.
    - `grep -rniE 'smartbattery' src/` returns no match, and `grep -c 'D3-12' src/za-bateriite.html` returns at least 1 — the removal is recorded by decision id rather than by naming the domain.
    - `grep -nE '8:00|9:00|02 9549710|088 9458404|087 9128244' src/za-bateriite.html` returns no match — no second copy of a fact `site-config.php` owns.
    - LIVE `za-bateriite.html` serves `1 година гаранция на регенерирана батерия` exactly once and `1 месец гаранция на всеки ремонт` zero times, while LIVE `zalivane-technosti.html` serves the one-month term exactly once — the keyed set genuinely resolves per page.
    - LIVE `za-bateriite.html` serves one evidence strip, one `<h1`, zero empty headings, and zero case-insensitive matches for the dead domain.
  </acceptance_criteria>
  <done>DIFF-02 has its depth page, the two warranty terms are stated as a distinction rather than a contradiction, and the dead domain has no route into the new tree.</done>
</task>

<task type="auto">
  <name>Task 2: «Тествай сам своя лаптоп» — the self-diagnostic, routed</name>
  <files>src/test-laptop.html</files>
  <read_first>
    - `src/test-laptop.html` — the current stub. Note the locked informal ти-form: «Тествай сам» is an established nav label and this page plus the homepage block are the only places it survives.
    - `site-current/test-laptop.html` — read the whole page (~1,400 words). It has four symptom sections: power-on behaviour (four cases), boot and operating system (six steps, including a bootable live environment, a memory test, a temperature monitor and a rule to stop at 75-80 degrees), audio/WiFi/USB (three), and battery/adapter (three). The content is genuinely good; the work here is structuring and routing it, not rewriting it.
    - `src/includes/categories.php` and `src/includes/services.php` — the records every routing link resolves through. No page filename may be hand-typed on this page.
    - `src/index.html` — the self-diagnostic block that links here, and its copy, so this page does not restate it.
    - `.planning/phases/03-content-trust-signal-build-out/03-CONTEXT.md` — DIFF-01's remaining scope is routing links from each symptom to the matching service page, not authoring a new tool.
    - `.planning/phases/03-content-trust-signal-build-out/03-RESEARCH.md` §State of the Art — the bootable-optical-disc instruction is the deprecated item in this page's source copy.
  </read_first>
  <action>
Rewrite `src/test-laptop.html` through the generalized service-page renderer, keeping the source
page's four symptom groups and its substance, and adding the routing that makes it a differentiator
rather than a dead end.

- `$torin_title` = `Тествай сам лаптопа си — диагностика · Торин`
- `$torin_desc` — 120-140 characters ending in a call to action.
- `'name'` = `Тествай сам своя лаптоп`
- `'h1'` = `Тествай сам своя лаптоп` — the locked label; this page keeps the informal ти-form and does
  not take a keyword h1, because changing it would break the label continuity with the nav and the
  homepage block.
- `'crumbs'` = Начало → this page's own name.
- `'intro'` — one paragraph: this is a short guided check so the visitor can describe the fault
  precisely before calling, and free diagnosis is available either way.
- `'blocks'` — four `steps` entries, one per symptom group, each heading naming the symptom in
  customer language and each carrying a `link` to the service page that fixes that class of fault:
  1. `Не се включва или не дава признаци на живот` → link to the kat-1 or kat-4 page, whichever the
     symptom text points at, resolved through `torin_category_href()`.
  2. `Включва се, но не стига до Windows` → link resolved to the kat-3 page.
  3. `Звук, WiFi или USB не работят` → link resolved to the kat-2 hub.
  4. `Батерията или зарядното` → link to `za-bateriite.html`.
  Every item inside a group is one imperative sentence carried over from the source page. Modernise
  the one deprecated instruction: the live environment is booted from a USB stick, not from a burned
  optical disc. Keep the temperature rule — if the machine passes 75-80 degrees under load, stop the
  test and call — and keep the memory-test and temperature-monitor steps.
- `'related'` — links to all six categories, built by iterating `$torin_categories` with hrefs from
  `torin_category_href()`, so unpublished categories still route correctly and publishing one later
  needs no edit here.
- `'faq'` — 3 entries: whether the diagnosis is free, whether the test can make things worse, and
  what to write down before calling.
- Do not set `warranty_key` — this is not a service page and a warranty block here would be noise.

Formal **вие** everywhere except the page title and h1, which keep the established ти-form. No page
filename is hand-typed anywhere on this page.
  </action>
  <verify>
    <automated>
      set -e
      # `! cmd` does NOT trip set -e (POSIX suppresses errexit for inverted status), so every
      # negative assertion below runs through nomatch, which checks the status and halts.
      nomatch() { if "$@"; then echo "GATE FAILED - unexpected match: $*" >&2; exit 1; fi; }
      grep -c 'torin_category_href' src/test-laptop.html                  # >= 1
      nomatch grep -nE "'(mehanichni-problemi|optimizatsiq|zalivane-technosti|ekran-klaviatura-portove|pregryavane-ohlazhdane|problem-stari)\.html'" src/test-laptop.html
      grep -c 'za-bateriite.html' src/test-laptop.html                    # >= 1
      nomatch grep -n "'warranty_key'" src/test-laptop.html
      nomatch grep -niE 'CD/DVD|DVD|компакт диск' src/test-laptop.html
      grep -ci 'USB' src/test-laptop.html                                  # >= 1
      nomatch grep -c 'Тази страница е временен скелет' src/test-laptop.html
      nomatch grep -rnE ' on[a-z]+=|javascript:|class="(fa |col-md-|mb-[0-9]|font-size-)' src/test-laptop.html
      nomatch grep -rn '__DIR__' src/test-laptop.html
      nomatch grep -rnE '(=>[^;]*\]|\[\s*[^]]*=>)' src/test-laptop.html
      W=$(sed -n "/= array(/,/^);/p" src/test-laptop.html | tr -cs '[:alpha:][:digit:]' ' ' | wc -w); test "$W" -ge 600
      node -e 'const s=require("fs").readFileSync("src/test-laptop.html","utf8");if((s.match(/'"'"'/g)||[]).length%2)process.exit(1)'
      # LIVE
      scripts/deploy-new.sh test-laptop.html
      U=https://torin.bg/new/test-laptop.html
      curl -sf -o /dev/null -w '%{http_code}\n' "$U"                       # 200
      test "$(curl -s "$U" | grep -c 'svc__block')" -ge 4
      test "$(curl -s "$U" | grep -c '<h1')" -eq 1
      test "$(curl -s "$U" | grep -cE '<h[23][^>]*>[[:space:]]*</h[23]>')" -eq 0
      test "$(curl -s "$U" | grep -c 'svc__warranty')" -eq 0
      # every routing link resolves to a real URL
      for h in $(curl -s "$U" | grep -oE 'href="[a-z0-9./#-]+\.html[^"]*"' | sed 's/href="//;s/"//' | sort -u); do
        curl -sf -o /dev/null -w "$h %{http_code}\n" "https://torin.bg/new/${h%%#*}"
      done
    </automated>
    <human-check>Walk the four symptom groups on a phone as a visitor with a dead laptop would. Each group ends with a link that leads somewhere useful rather than back to the homepage.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -nE "'(mehanichni-problemi|optimizatsiq|zalivane-technosti|ekran-klaviatura-portove|pregryavane-ohlazhdane|problem-stari)\.html'" src/test-laptop.html` returns no match — every category route resolves through `torin_category_href()`.
    - `grep -niE 'CD/DVD|DVD|компакт диск' src/test-laptop.html` returns no match and the page mentions USB — the deprecated instruction is modernised.
    - `grep -n "'warranty_key'" src/test-laptop.html` returns no match and LIVE `svc__warranty` count is 0 — no warranty block on a non-service page.
    - The page's data array contains at least 600 words and no longer carries the stub sentinel sentence.
    - LIVE `test-laptop.html` returns 200, serves at least four `svc__block` sections, one `<h1`, and zero empty headings.
    - Every internal link on the served page returns 200.
  </acceptance_criteria>
  <done>DIFF-01 is met: the self-diagnostic is real content that routes each symptom to the service that fixes it, on published and unpublished categories alike.</done>
</task>

<task type="auto">
  <name>Task 3: «Профилактика на лаптоп» — the chip-level repair evidence</name>
  <files>src/profilaktika-laptop.html</files>
  <read_first>
    - `src/profilaktika-laptop.html` — the current stub.
    - `site-current/profilaktika-laptop.html` — read lines 315-415. Line 361 explains the three soldering methods and why a hot-air gun produces short-lived repairs and damaged contact pads; line 380 describes melted plastic components from hot-air work; line 398 describes the German machine whose infrared heat source is outside the visible spectrum, with its know-how caveat; line 411 carries the AMTECH flux, the cooling-system reinforcement, the up-to-ten-degree lower operating temperatures and the ninety-percent durability rate. This is DIFF-03's evidence, in the shop's own words, and it is the ceiling on what may be claimed.
    - `src/index.html` as written by plan 03-02 — the homepage chip-level block. This page is its depth destination and must not repeat its paragraphs.
    - `src/zalivane-technosti.html` as written by plan 03-01 — the category-4 page carrying the three board-level tiers. Cross-link, do not duplicate.
    - `src/img/repairs/` — the ported photographs, including the reflow station and the hot-air-gun damage shots.
    - `.planning/phases/03-content-trust-signal-build-out/03-RESEARCH.md` §Common Pitfalls P-11 — this page's source copy states working hours that disagree with the shared config value; the hours sentence is dropped, not ported.
    - `.planning/phases/02-design-system-information-architecture/02-CONTEXT.md` — D-28, the cross-listing of профилактика into categories 3 and 5 as a link rather than duplicated copy.
  </read_first>
  <action>
Rewrite `src/profilaktika-laptop.html` through the generalized service-page renderer, 600-1000
Bulgarian words. This page carries two jobs: it is the profilaktika service page that categories 3
and 5 cross-list into, and it is DIFF-03's evidence page.

- `$torin_title` = `Профилактика на лаптоп и реболинг на чипове · Торин`
- `$torin_desc` — 120-140 characters ending in a call to action.
- `'name'` = `Профилактика на лаптоп`
- `'h1'` = `Профилактика на лаптоп в София` (30 characters)
- `'crumbs'` = Начало → this page's own name.
- `'intro'` — one paragraph: dust removal, thermal-paste replacement and cooling-system service, and
  that the cheapest repair is the one that did not become necessary.
- `'fixes'` — cleaning and washing the cooling system, thermal-paste replacement, replacing soft
  thermal pads with aluminium or copper plates, fan replacement, cooling-system reinforcement after a
  chip-level repair.
- `'blocks'` — three entries:
  1. `prose`, heading `С какво работим`, two paragraphs on the shop's own equipment: a machine whose
     infrared heat source lies outside the visible spectrum, which protects both the operator and the
     board, computer-controlled with per-manufacturer profiles, and the AMTECH flux that determines
     the soldering environment. State the manufacturer's know-how caveat as the source does — the
     shop asked how the source works and was told it is the German manufacturer's know-how. Claim no
     certification and no exclusivity the source does not claim.
  2. `callout`, heading `Защо пистолетът за горещ въздух е проблем`, two paragraphs on uneven heating,
     damaged contact pads, melted plastic components and short-lived results, with a `link` to
     `zalivane-technosti.html` labelled `Ремонт на дънни платки`.
  3. `prose`, heading `Какво постигаме`, one or two paragraphs stating the up-to-ten-degree lower
     operating temperatures achieved by reinforcing the cooling system and the ninety-percent
     durability rate, each attributed as the shop's own measured experience rather than as a
     guarantee.
- `'evidence'` — three photographs: the reflow station, a hot-air-gun-damaged board, and a melted-plastic
  example, with captions describing what each proves, including
  `Инфрачервена станция за реболинг на BGA чипове` and
  `Повреда от нагряване с пистолет за горещ въздух`. Read each file's true intrinsic dimensions from
  disk.
- `'warranty_key'` = `default`.
- `'process'` — 4-6 steps.
- `'faq'` — 3-5 entries, one paragraph each, including how often profilaktika is needed and whether it
  helps a machine that already shuts down under load.
- `'related'` — links to the kat-3 and kat-5 category pages via `torin_category_href()` and to
  `zalivane-technosti.html`.

Do **not** port the source page's working-hours sentence. It states hours that disagree with the
shared config value, and hours are a footer fact with one writer; a second copy here is exactly the
drift the single-sourcing rule exists to prevent. Record that omission in a source comment naming the
config key that owns the fact.
  </action>
  <verify>
    <automated>
      set -e
      # `! cmd` does NOT trip set -e (POSIX suppresses errexit for inverted status), so every
      # negative assertion below runs through nomatch, which checks the status and halts.
      nomatch() { if "$@"; then echo "GATE FAILED - unexpected match: $*" >&2; exit 1; fi; }
      grep -c 'AMTECH' src/profilaktika-laptop.html                        # >= 1
      grep -c 'class="evidence"\|evidence' src/profilaktika-laptop.html    # >= 1
      grep -c "'warranty_key' *=> *'default'" src/profilaktika-laptop.html # 1
      grep -c 'torin_category_href' src/profilaktika-laptop.html           # >= 1
      nomatch grep -nE '9:00|17:00|8:00|16:00' src/profilaktika-laptop.html
      nomatch grep -nE '02 9549710|088 9458404|087 9128244' src/profilaktika-laptop.html
      nomatch grep -c 'Тази страница е временен скелет' src/profilaktika-laptop.html
      nomatch grep -rnE ' on[a-z]+=|javascript:|class="(fa |col-md-|mb-[0-9]|font-size-)' src/profilaktika-laptop.html
      nomatch grep -rn '__DIR__' src/profilaktika-laptop.html
      nomatch grep -rnE '(=>[^;]*\]|\[\s*[^]]*=>)' src/profilaktika-laptop.html
      W=$(sed -n "/= array(/,/^);/p" src/profilaktika-laptop.html | tr -cs '[:alpha:][:digit:]' ' ' | wc -w); test "$W" -ge 600
      node -e 'const s=require("fs").readFileSync("src/profilaktika-laptop.html","utf8");if((s.match(/'"'"'/g)||[]).length%2)process.exit(1)'
      # LIVE
      scripts/deploy-new.sh profilaktika-laptop.html
      U=https://torin.bg/new/profilaktika-laptop.html
      curl -sf -o /dev/null -w '%{http_code}\n' "$U"                        # 200
      curl -s "$U" | grep -c 'class="evidence"'                             # 1
      curl -s "$U" | grep -oc 'img/repairs/'                                # 3
      curl -s "$U" | grep -c 'Инфрачервена станция за реболинг на BGA чипове' # 1
      curl -s "$U" | grep -c '1 месец гаранция на всеки ремонт'              # 1
      test "$(curl -s "$U" | grep -c '<h1')" -eq 1
      test "$(curl -s "$U" | grep -cE '<h[23][^>]*>[[:space:]]*</h[23]>')" -eq 0
      # the homepage block and this page must not share a paragraph verbatim
      node -e 'const fs=require("fs");const strip=s=>s.replace(/<[^>]*>/g," ").replace(/\s+/g," ");const a=strip(fs.readFileSync("src/index.html","utf8")).split(/[.!?]\s/).map(s=>s.trim()).filter(s=>s.length>90);const b=strip(fs.readFileSync("src/profilaktika-laptop.html","utf8"));const dup=a.filter(s=>b.includes(s));if(dup.length){console.error("duplicated sentence(s):",dup);process.exit(1)}'
    </automated>
    <human-check>Read the homepage chip-level block and then this page back to back. They read as a summary and its depth, not as the same text twice.</human-check>
  </verify>
  <acceptance_criteria>
    - The page names the flux brand and carries three evidence photographs with the caption `Инфрачервена станция за реболинг на BGA чипове` present in the served HTML.
    - `grep -nE '9:00|17:00|8:00|16:00' src/profilaktika-laptop.html` returns no match — the contradicting hours sentence is dropped rather than ported, and a source comment names the config key that owns the fact.
    - `grep -c "'warranty_key' *=> *'default'"` returns 1 and LIVE serves `1 месец гаранция на всеки ремонт` exactly once.
    - The page's data array contains at least 600 words and no longer carries the stub sentinel sentence.
    - The duplication check finds no sentence longer than 90 characters shared verbatim between `src/index.html` and this page — the homepage block summarises and hands off rather than repeating.
    - LIVE returns 200 with one `<h1`, one evidence strip, three `img/repairs/` references and zero empty headings.
  </acceptance_criteria>
  <done>DIFF-03's evidence is on a page of its own, cross-linked from the homepage block and from the category-4 depth page, with no paragraph living in two places.</done>
</task>

</tasks>

## Artifacts this phase produces

New symbols created by this plan:

- **New page content (files already existed as stubs):** `src/za-bateriite.html`, `src/test-laptop.html`, `src/profilaktika-laptop.html`
- **New PHP functions, CSS selectors, config keys or includes:** none — every surface composes from
  what plans 03-01 and 03-02 shipped
- **First use of an existing symbol:** `'warranty_key' => 'battery'` is exercised for the first time
  on `src/za-bateriite.html`

## Probe rows discharged by this plan

| Source | Row | Disposition |
|---|---|---|
| Edge probe | DIFF-01 / unclassified | covered → truth (every symptom group routes through the publish gate) |
| Edge probe | DIFF-02 / unclassified | covered → truth (the two warranty terms cannot disagree; the dead domain has no route in) |
| Edge probe | DIFF-03 / unclassified | the backstop marker authored in plan 03-02 is discharged here by the automated verbatim-duplication check between the homepage block and this depth page |
| UI-SPEC §4 | populated, partial, zero-one-many | covered — the evidence strip is exercised at two and three photographs on two different pages |

## Flagged planner assumptions

- **RESEARCH A4 — the one-month service warranty and the one-year battery warranty are two different
  warranties rather than a contradiction.** The copy states the distinction explicitly and marks it
  `[ASSUMED]` against OWNER-QUESTIONS #23. If the assumption is wrong, the site is stating a term the
  shop does not honour, which is why the marker stays in source until the question is answered.
- **The claims ported here are the shop's own and are unverified by this project.** The temperature
  delta, the durability rate, the cell sourcing and the equipment description are quoted at their
  existing strength from copy the shop has published since roughly 2019. Nothing is amplified, and
  nothing is independently confirmed.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy site copy → new tree | Roughly 4,000 words of the shop's own Bulgarian cross from a Bootstrap-era template into the new one. |
| developer-authored content → served HTML | All rendered strings pass through the escaping helper. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-17 | Injection | Legacy markup carried across with the copy | medium | mitigate | Port strings into PHP arrays rendered by the template; the legacy markup is the thing the previous phase removed. Gate: `! grep -rnE ' on[a-z]+=\|javascript:\|class="(fa \|col-md-\|mb-[0-9]\|font-size-)'` on all three pages. |
| T-03-18 | Spoofing | Capability and figure claims | high | mitigate | Every technical claim is quoted at the strength the shop's own live copy states it; no certification, exclusivity or guarantee is added. The homepage-versus-depth duplication check also prevents a claim drifting into two differently-worded versions. |
| T-03-19 | Tampering | A dead external domain re-entering the tree | medium | mitigate | Do-not-import gate: `! grep -rniE 'smartbattery' src/`, plus the rule that the removal rationale cites the decision id rather than the domain so an explanatory comment cannot trip its own gate. |
| T-03-20 | Repudiation | Contradictory warranty terms across pages | medium | mitigate | Both terms render from one keyed set in `site-config.php`; no page authors a warranty literal. Gate: `! grep -n "'warranty' *=>" src/za-bateriite.html` plus the cross-page live assertion. |
| T-03-21 | Information disclosure | Duplicated contact facts drifting out of sync | low | mitigate | Gate: no page may carry a second copy of the hours or a phone number. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed; `03-RESEARCH.md` §Package Legitimacy Audit records the gate as not applicable. |
</threat_model>

<verification>
- All three pages return 200 live, each with one `<h1>`, zero empty headings, and no stub sentinel.
- `grep -rniE 'smartbattery' src/` returns no match.
- The battery page serves the battery warranty term and not the service term; the category-4 page
  serves the service term — the keyed set resolves per page.
- Every internal link on the self-diagnostic returns 200, including links to still-unpublished
  categories, which resolve to homepage anchors.
- No sentence over 90 characters is shared verbatim between the homepage and the profilaktika page.
</verification>

<success_criteria>
- Phase success criterion 4 is fully met: the self-diagnostic, the battery-regeneration story and the
  chip-level repair expertise each have distinct, prominent content and a depth page.
- The warranty contradiction the live site has carried for years is resolved into a stated
  distinction, marked as unconfirmed.
- The deprecated bootable-media instruction is gone.
</success_criteria>

<output>
Create `.planning/phases/03-content-trust-signal-build-out/03-04-SUMMARY.md` when done.

Record the word counts for all three pages, the evidence filenames used on each, and — for the
owner-question batch — that the battery-versus-service warranty distinction is now stated on the site
and needs one sentence of confirmation.
</output>
