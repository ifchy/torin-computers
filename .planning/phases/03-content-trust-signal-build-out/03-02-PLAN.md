---
phase: 03-content-trust-signal-build-out
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/includes/site-config.php
  - src/includes/icons.php
  - src/includes/brand-row.php
  - src/includes/rating-badge.php
  - src/includes/category-page.php
  - src/includes/jsonld.php
  - src/css/components.css
  - src/index.html
  - src/zalivane-technosti.html
  - src/.htaccess
  - src/img/repairs/
  - scripts/probes/trust-signals.js
autonomous: true
requirements: [TRUST-01, TRUST-02, DIFF-02, DIFF-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A visitor on the homepage sees a row of brand wordmarks headed «Обслужваме всички марки», closed with «и др.», followed by a visible disclaimer stating the shop is an independent out-of-warranty service (TRUST-01)"
    - "The same brand row appears on every service page and on no legal or utility page"
    - "Adjacent brand chips stay visually separate at every wrap point and the stored brand list contains no duplicate entry (probe TRUST-01/adjacency)"
    - "An empty brand list emits no brand section at all — not a heading and a disclaimer with no brands; a one-entry list still renders, closed by «и др.» (probe TRUST-01/empty)"
    - "Brand order in the served HTML is the stored array order, unsorted and stable, with «и др.» always last (probe TRUST-01/ordering)"
    - "A visitor sees a Google rating badge linking to the shop's Google Business Profile, as the last child of the CTA block, on the homepage and every service page (TRUST-02)"
    - "When the profile URL, the rating or the review count is empty the badge emits nothing at all — the served HTML contains no rating-badge substring and the CTA block's vertical rhythm is unchanged (probe TRUST-02/unclassified, UI-SPEC §2a)"
    - "A visitor sees the battery-regeneration story as its own homepage section with the shop's own evidence, linking to its depth page (DIFF-02)"
    - "A visitor sees the BGA/chip-level repair capability as its own homepage section with photographic evidence, linking to the category-4 depth page (DIFF-03)"
    - "Every evidence photo carries its file's true intrinsic width and height attributes, and a figure whose file is not on disk is dropped rather than rendered as a broken image"
    - statement: "The BGA/chip-level expertise reads as one story across the homepage block and the category-4 depth page — no paragraph is duplicated between them and the link between them is unambiguous"
      verification: backstop
  artifacts:
    - path: "src/includes/brand-row.php"
      provides: "TRUST-01 brand wordmark row rendered from $site['brands'], guarded on an empty list"
      contains: "brand-row__note"
    - path: "src/includes/rating-badge.php"
      provides: "TRUST-02 static badge, gated on a non-empty gbp_url, emitting nothing when unset"
      contains: "rating-badge"
    - path: "src/img/repairs/"
      provides: "First-party repair photographs ported from the legacy tree under ASCII filenames"
    - path: "src/index.html"
      provides: "Brand row, rating badge, DIFF-02 and DIFF-03 feature sections in the UI-SPEC section order"
    - path: "scripts/probes/trust-signals.js"
      provides: "CDP probe asserting brand-row wrap, badge contrast and target size, and evidence-strip layout"
  key_links:
    - from: "src/includes/brand-row.php"
      to: "src/includes/site-config.php"
      via: "foreach over $site['brands']; «и др.» is emitted by the template and never stored"
      pattern: "\\$site\\['brands'\\]"
    - from: "src/includes/rating-badge.php"
      to: "src/includes/site-config.php"
      via: "render gated on trim($site['gbp_url']) !== '' plus non-empty rating and count"
      pattern: "\\$site\\['gbp_url'\\]"
    - from: "src/includes/jsonld.php"
      to: "src/includes/site-config.php"
      via: "sameAs points at the Google Business Profile URL — the one permitted profile signal"
      pattern: "sameAs"
  prohibitions:
    - "MUST NOT present the brand row in a way that implies authorized or official manufacturer status: the independent out-of-warranty disclaimer is mandatory, not decoration, and no manufacturer logo or figurative mark may be used"
    - "MUST NOT name a manufacturer the shop does not actually service — the list is [ASSUMED] against OWNER-QUESTIONS #22 and the «и др.» closer exists so the row never claims to be exhaustive"
    - "MUST NOT publish a rating or a review count that has not been read off the live Google Business Profile; an unconfirmed figure renders as the badge's absence, never as a plausible-looking placeholder"
    - "MUST NOT emit aggregateRating or review structured data for the shop's own reviews under any schema type"
    - "MUST NOT assert a capability or a figure the shop's own copy does not state — the reflow machine, the flux brand, the temperature delta and the durability rate are quoted from the shop's existing Bulgarian, not amplified"
---

<objective>
Ship the two trust signals and the two differentiator surfaces that the phase goal names, on the
homepage and on every service page, single-sourced so no page can disagree with another.

Three of these four are decided against the market rather than assumed: D3-09 rejects brand logo
images on evidence that zero of eight competitors use them; D3-07 rejects the Places API and every
third-party reviews widget in favour of a static badge; and D3-11 promotes battery regeneration back
out of the folded catch-all that Phase 2 left it in. The fourth, DIFF-03, finally has concrete proof
to present — the reflow machine, the flux, the temperature delta and the durability rate all exist
verbatim in the shop's own Bulgarian on the live site, alongside roughly forty first-party repair
photographs that have never been ported.

Purpose: this is where phase success criteria 1, 2 and 4 are met or honestly recorded as unmet.
Output: two new render partials, the sixteenth icon, the ported photo set, and a homepage whose
section order matches the UI-SPEC contract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-content-trust-signal-build-out/03-CONTEXT.md
@.planning/phases/03-content-trust-signal-build-out/03-RESEARCH.md
@.planning/phases/03-content-trust-signal-build-out/03-PATTERNS.md
@.planning/phases/03-content-trust-signal-build-out/03-UI-SPEC.md
@.planning/phases/03-content-trust-signal-build-out/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brand wordmark row and the Google rating badge, single-sourced and honestly absent</name>
  <files>src/includes/site-config.php, src/includes/icons.php, src/includes/brand-row.php, src/includes/rating-badge.php, src/includes/category-page.php, src/includes/jsonld.php, src/css/components.css, src/index.html</files>
  <read_first>
    - `src/includes/site-config.php` — the header's provenance rule, the `phones` list key and its "no consumer may re-join this list" comment, and the `notice` key's `[ASSUMED]` + empty-string-disables shape. The two new keys copy both patterns exactly.
    - `src/index.html` lines 35-68 — the data-driven card grid: the inline `htmlspecialchars($v, ENT_QUOTES, 'UTF-8')` escaping style this page uses, and the comment explaining why hardcoded values are still escaped.
    - `src/index.html` lines 163-191 — the CTA block, and the `.cta-block__form` node at 183-188 whose comment states the optional-node discipline the badge's absent state copies verbatim.
    - `src/includes/category-page.php` — the CTA block at the end of `torin_render_service_page()`; both copies of the CTA must receive the badge or the two surfaces drift.
    - `src/includes/icons.php` lines 9-45 — the `switch` shape, the per-case glyph comment, `aria-hidden="true" focusable="false"`, and the unknown-name-returns-empty rule.
    - `src/css/components.css` lines 229-260 (`.trust-badge`) and 344-358 (`.cta-block`) — the pill and CTA rules the badge composes from.
    - `.planning/phases/03-content-trust-signal-build-out/03-UI-SPEC.md` §1 and §2 in full — §2a (the absent state, which is normative and specified first) is the load-bearing half.
    - `.planning/phases/03-content-trust-signal-build-out/03-RESEARCH.md` §Common Pitfalls P-1 and P-7 — why self-served rating markup is categorically ineligible, and why the disclaimer sentence is what makes naming brands defensible.
  </read_first>
  <action>
**1. `src/includes/site-config.php` — four new keys, each with a provenance comment.**

`'brands'` is a flat list of seven entries in this order: `Lenovo`, `HP`, `Dell`, `Asus`, `Acer`,
`Apple`, `MSI`. Marked `[ASSUMED]` against OWNER-QUESTIONS #22, with the comment recording that this
list came from requirements drafting and not from the owner, that naming a brand the shop does not
service is a promise it cannot keep, and that Apple is the riskiest entry because it needs different
parts and tooling. The «и др.» closer is **not** an array entry — it is emitted by the partial, the
same discipline as "no consumer may join the phone list back into a display string".

`'gbp_rating'`, `'gbp_reviews'` and `'gbp_url'` all ship as **empty strings**, marked `[ASSUMED]`
against OWNER-QUESTIONS #7. This is deliberate and is the whole point of the key: aggregator crawls
report the profile is healthy but nobody has read the live figures, and a plausible-looking number
on sixteen pages is worse than no badge. The comment must state that filling these three values is
the single cheapest unblock in the phase, that the figures must be read off the profile itself, and
that no rating or review structured data accompanies the badge under any circumstances.

**2. `src/includes/icons.php` — add the sixteenth icon, `star`.** A filled star, `fill:
currentColor`, deliberately breaking the file's stroke-only house style because a stroked star does
not read as a star at 1em; say so in the per-case comment. `aria-hidden="true" focusable="false"`
like every other icon.

**3. `src/includes/brand-row.php` — a new partial defining `torin_render_brand_row()`.** Emits
nothing at all when `$site['brands']` has no content, using the same `torin_has_content()` guard as
every template slot. Otherwise:
`<section class="section brands" aria-labelledby="brands-h">` → `.container` → the existing
`<span class="eyebrow" aria-hidden="true">` → `<h2 id="brands-h">Обслужваме всички марки</h2>` →
`<ul class="brand-row">` with one `<li class="brand-row__item">` per entry rendered in stored order
with no sort → a final `<li class="brand-row__item brand-row__item--more">и др.</li>` →
`<p class="brand-row__note">` carrying, verbatim:
`Независим извънгаранционен сервиз. Не сме оторизиран представител на нито един производител — имената на марките указват само каква техника приемаме.`
The disclaimer paragraph is mandatory; it is the element that makes naming the marks referential use
rather than an implied-authorisation claim, and the head comment must say so. Items are not links,
not buttons and not hoverable.

**4. `src/includes/rating-badge.php` — a new partial defining `torin_render_rating_badge()`.**
Specify the absent case first, both in code order and in the head comment: when any of `gbp_url`,
`gbp_rating` or `gbp_reviews` fails `torin_has_content()`, return having emitted nothing — no
skeleton, no placeholder, no greyed pill, and no empty-state copy, because the empty state is the
absence. Otherwise emit
`<a class="rating-badge" href="…" rel="noopener">` containing the `star` icon, a
`<span class="rating-badge__score">`, and a `<span class="rating-badge__label">` reading
`от {count} отзива в Google`, so the accessible name is the full concatenation and the score is never
the link's only text. No `target="_blank"` — the comment must record that this removes reverse
tabnabbing as a category rather than mitigating it, and that `rel="noopener"` is kept anyway. The
rating value is stored and rendered as a display string with a comma decimal separator, never
formatted at render time.

**5. Wire both partials in.** Include and call `torin_render_brand_row()` on `src/index.html`
between the category grid and the catch-all disclosure, and from `torin_render_service_page()` so
every service page gets it. Call `torin_render_rating_badge()` as the **last child** of
`.cta-block` — in `src/index.html` after the `.cta-block__form` node, and in the CTA block inside
`torin_render_service_page()`. The badge must own its own top margin and no parent rule may name it
positionally, so deleting the node is a subtraction rather than a redesign.

The brand row must **not** appear on the legal and utility pages (`uslovia`, `warrently`, `msg`,
`covid`, `laptopi`, `rezervni-chasti`) — a trademark disclaimer on a privacy policy is noise. Since
those pages do not call the service-page renderer, this falls out for free; state it in the partial's
head comment so a later phase does not "fix" it by including the partial in `footer.php`.

**6. `src/includes/jsonld.php` — add `sameAs`** to the LocalBusiness block as a one-element array
holding `$site['gbp_url']`, guarded so an empty URL omits the property entirely rather than emitting
an empty string. Read the key, never a literal. Add no rating and no review property.

**7. `src/css/components.css` — the two new component groups.** `.brands`/`.brand-row` per UI-SPEC
§1: `display:flex; flex-wrap:wrap; gap: var(--sp-sm) var(--sp-md); list-style:none` on the row;
`.brand-row__item` as `inline-flex`, `min-height:40px`, `padding-inline: var(--sp-md)`,
`--c-surface-2` fill, `--r-md`, `--c-ink-deep` text, bold, body size — the fixed-height variable-width
chip is what makes a two-character mark and a six-character mark read as peers.
`.brand-row__item--more` drops the fill and uses `--c-ink-muted` at regular weight.
`.brand-row__note` at body size, `--c-ink-muted`, `max-width: var(--measure)`. Add the
forward-compatibility rule `.brand-row__item > img { max-height: 24px; width: auto; display: block }`
so a later logo swap changes no row height or wrap point. `.rating-badge` per UI-SPEC §2b: navy
`--c-ink-deep` pill, `--c-on-dark` text, `min-height: 44px` as a floor not a fixed height so the
label wraps inside the pill at 320px, `--sp-sm` gap, `--r-md`, hover `--c-ink-deep-2`, focus ring
`--c-focus` with `outline-offset: 2px` (drawn on the white page outside the pill, where the on-dark
ring would be near-invisible), no underline. The star is `--c-brand` on the navy pill only — never on
the page or a tint surface. No `text-transform` anywhere in either group.
  </action>
  <verify>
    <automated>
      set -e
      # `! cmd` does NOT trip set -e (POSIX suppresses errexit for inverted status), so every
      # negative assertion below runs through nomatch, which checks the status and halts.
      nomatch() { if "$@"; then echo "GATE FAILED - unexpected match: $*" >&2; exit 1; fi; }
      grep -c "'brands'" src/includes/site-config.php                       # 1
      grep -c "'gbp_url'" src/includes/site-config.php                      # 1
      test "$(grep -c "'gbp_" src/includes/site-config.php)" -eq 3
      grep -n "'gbp_url' *=> *''" src/includes/site-config.php              # present: badge absent until #7 answered
      nomatch grep -n "'и др." src/includes/site-config.php
      grep -c "case 'star'" src/includes/icons.php                          # 1
      grep -c 'function torin_render_brand_row' src/includes/brand-row.php  # 1
      grep -c 'function torin_render_rating_badge' src/includes/rating-badge.php # 1
      grep -c 'brand-row__note' src/includes/brand-row.php                  # 1
      grep -c 'Независим извънгаранционен сервиз' src/includes/brand-row.php # 1
      grep -c 'rel="noopener"' src/includes/rating-badge.php                # 1
      nomatch grep -n 'target="_blank"' src/includes/rating-badge.php
      grep -c 'sameAs' src/includes/jsonld.php                              # 1
      nomatch grep -riE 'aggregateRating|"review"' src/includes/jsonld.php
      test "$(grep -rn 'text-transform' src/css/components.css | grep -ciE 'brand|rating')" -eq 0
      grep -c 'torin_render_brand_row' src/index.html src/includes/category-page.php
      grep -c 'torin_render_rating_badge' src/index.html src/includes/category-page.php
      nomatch grep -rnE '(=>[^;]*\]|\[\s*[^]]*=>)' src/includes/brand-row.php src/includes/rating-badge.php
      nomatch grep -rn '__DIR__' src/includes/brand-row.php src/includes/rating-badge.php
      # LIVE — absent state is the shipped state until OWNER-QUESTIONS #7 is answered
      scripts/deploy-new.sh includes/site-config.php includes/icons.php includes/brand-row.php includes/rating-badge.php includes/category-page.php includes/jsonld.php css/components.css index.html
      curl -s https://torin.bg/new/index.html | grep -c 'Обслужваме всички марки'            # 1
      curl -s https://torin.bg/new/index.html | grep -c 'brand-row__item'                     # 8 (7 brands + closer)
      curl -s https://torin.bg/new/index.html | grep -c 'и др.'                               # >= 1
      curl -s https://torin.bg/new/index.html | grep -c 'Независим извънгаранционен сервиз'   # 1
      test "$(curl -s https://torin.bg/new/index.html | grep -c 'rating-badge')" -eq 0
      test "$(curl -s https://torin.bg/new/index.html | grep -c 'sameAs')" -eq 0
      curl -s https://torin.bg/new/zalivane-technosti.html | grep -c 'brand-row__item'         # 8
      for p in uslovia warrently msg covid laptopi rezervni-chasti; do
        test "$(curl -s "https://torin.bg/new/$p.html" | grep -c 'brand-row__item')" -eq 0
      done
    </automated>
  </verify>
  <acceptance_criteria>
    - `site-config.php` carries `'brands'` with exactly seven entries in the order `Lenovo`, `HP`, `Dell`, `Asus`, `Acer`, `Apple`, `MSI`, and `grep -n "'и др." src/includes/site-config.php` returns no match — the closer is emitted by the partial, never stored.
    - `site-config.php` carries `gbp_rating`, `gbp_reviews` and `gbp_url` as empty strings, each with an `[ASSUMED]` comment naming OWNER-QUESTIONS #7.
    - `grep -c 'case ' src/includes/icons.php` returns 16.
    - `src/includes/brand-row.php` contains the exact string `Независим извънгаранционен сервиз. Не сме оторизиран представител на нито един производител — имената на марките указват само каква техника приемаме.`
    - `grep -c 'rel="noopener"' src/includes/rating-badge.php` returns 1 and `grep -n 'target="_blank"'` returns no match.
    - `grep -riE 'aggregateRating|"review"' src/includes/jsonld.php` returns no match.
    - LIVE `index.html` serves exactly 8 `brand-row__item` elements (seven brands plus the closer), one disclaimer paragraph, and **zero** occurrences of `rating-badge` — the badge's absent state is the shipped state until OWNER-QUESTIONS #7 is answered, and that is the honest outcome recorded in the SUMMARY.
    - LIVE `zalivane-technosti.html` serves the same 8 `brand-row__item` elements; LIVE `uslovia.html`, `warrently.html`, `msg.html`, `covid.html`, `laptopi.html` and `rezervni-chasti.html` each serve 0.
    - LIVE `index.html` serves zero occurrences of `sameAs` while `gbp_url` is empty — the guard omits the property rather than emitting an empty string.
  </acceptance_criteria>
  <done>Both trust signals are built, single-sourced and wired into both CTA surfaces; the brand row ships, and the badge ships as its specified absent state with the one unblocking question recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Port the shop's own repair photographs and the evidence-strip renderer</name>
  <files>src/img/repairs/, src/includes/category-page.php, src/css/components.css, src/.htaccess</files>
  <read_first>
    - `.planning/phases/03-content-trust-signal-build-out/03-UI-SPEC.md` §4 — the whole evidence-strip contract: 100x100 CSS px display size, the true-intrinsic-attribute rule, the explicit-columns arithmetic at 360px, the hard cap of three photos per page, the `alt`/`figcaption` branching rule, and the forbidden uses.
    - `.planning/phases/03-content-trust-signal-build-out/03-RESEARCH.md` §Content Inventory → "Photographic assets" — the measured file sets, their real dimensions, and the caching omission.
    - `src/css/components.css` lines 176-200 — `.cat-card__media` and `.cat-card__media > img`: the fixed-box `overflow:hidden` + `aspect-ratio` + `object-fit:cover` + `--c-surface-3` mechanism the evidence box copies, and the instruction at 176-177 to ship width/height on any such image.
    - `src/css/layout.css` lines 37-49 — the explicit-columns rule and why a content-driven track function is rejected for a known-size set.
    - `src/.htaccess` — the `ExpiresByType` block (the `image/png` line is the analog for the missing one) and the stale comment near line 23 asserting the host returns no Content-Encoding.
    - `src/includes/header.php` lines 42-43 — the dev-switcher include guard, the house shape for "use only if the file is present", which the per-figure existence check copies.
  </read_first>
  <action>
<!-- planner-discipline-allow: assets1 -->
**1. Port the photographs.** Copy the first-party repair photographs from
`site-current/assets1/img/` into a new `src/img/repairs/` directory. The relevant sets are the 17
`profilaktika*.jpg`, the 9 `meh-prob*.jpg`, the 6 `tok*.jpg`, the 3 `zalivane*.jpg`, the 3
`stari*.jpg`, `baterry.jpg` and `baterry2.jpg`. Keep the existing basenames — they are already ASCII
and already map to their subjects. Run each through `sips` once to strip camera and editor metadata
and re-encode at quality 80; do not upscale, do not crop, and do not rename. These are the shop's own
photographs already in the repository, so there is no licence question and no external fetch. Record
the resulting per-file byte sizes and the total in the SUMMARY.

`ouroffice.jpg` is a shop-premises photo, not repair evidence — port it too but into `src/img/`
rather than `src/img/repairs/`, so the evidence namespace stays reviewable.

**2. `src/.htaccess` — two edits, one change.** Add `ExpiresByType image/jpeg "access plus 30 days"`
immediately beside the existing `image/png` line; without it every ported photograph falls to
undeclared heuristic caching. Then correct the stale comment near line 23 that asserts this host
returns no Content-Encoding: Phase 2's own plan 02-01 added the compression block after that comment
was written, and `02-VERIFICATION.md:287` records gzip on CSS from the live origin. A comment that
contradicts measured live behaviour is how the next phase re-derives a wrong budget; replace it with
the measured figure and the date.

**3. `src/includes/category-page.php` — add `torin_render_evidence($items)`.** Each item is
`array('file' => …, 'w' => …, 'h' => …, 'caption' => …)`. The renderer:
- Drops any item whose file is not present under `src/img/repairs/`, using a `file_exists()` check
  built on the 5.2-safe directory idiom already used throughout this tree. A bad path must degrade to
  fewer photographs, never to a broken-image icon on a trust surface. One stat call per photo against
  a hard cap of three is negligible, and it makes the porting step self-correcting.
- Emits nothing at all when no item survives, so a feature section renders prose-only and still reads
  as complete.
- Emits `<ul class="evidence">` → `<li><figure>` → `<img src="img/repairs/{file}" width="{w}"
  height="{h}" alt="" loading="lazy" decoding="async">` → `<figcaption>{caption}</figcaption>`.
- The width and height attributes carry the **file's true intrinsic pixels**, which vary per file —
  this is what reserves the box before load and is the entire layout-stability defence. CSS sets the
  display size; the attributes never do.
- `alt` is empty when a descriptive caption is present, because the caption is already in the
  accessible tree and describing the image in both places double-announces. An item shipped without a
  caption must carry its description in `alt` instead. Never a generic word, never a filename.
- Hard-caps at three items per call, and says why in the comment.
Add an `evidence` key to the recognised-keys doc block and to the guard-declaration block, rendered
inside the first `.svc` section beneath the intro.

**4. `src/css/components.css` — the `.evidence` group.** `display:grid; gap: var(--sp-md);
grid-template-columns: repeat(2, 100px)`, becoming `repeat(3, 100px)` at the existing 35rem
breakpoint. Explicit column counts, never a content-driven track function — the set is small and
known, and a fractional track would stretch a one-photo strip into an orphan cell. The image box is
100x100 CSS px with `aspect-ratio: 1/1`, `object-fit: cover`, `--r-md` and a `--c-surface-3` backing,
copying `.cat-card__media`'s mechanism so any source ratio crops rather than distorts. `<figcaption>`
at body size, `--c-ink-muted`, `margin-block-start: var(--sp-sm)`, wrapping freely beneath its image
and never truncated. No hover state, no cursor change, no click target — these are not interactive.

**5. Add three evidence photographs to `src/zalivane-technosti.html`** via the new `evidence` key:
`zalivane1.jpg`, `zalivane2.jpg` and `zalivane3.jpg`, each with its true intrinsic dimensions read
from the file, and captions describing what the photograph proves rather than labelling it — the
first is `Корозия по пътечките на дънна платка след заливане`.
  </action>
  <verify>
    <automated>
      set -e
      # `! cmd` does NOT trip set -e (POSIX suppresses errexit for inverted status), so every
      # negative assertion below runs through nomatch, which checks the status and halts.
      nomatch() { if "$@"; then echo "GATE FAILED - unexpected match: $*" >&2; exit 1; fi; }
      test -d src/img/repairs
      test "$(ls src/img/repairs/*.jpg | wc -l)" -ge 38
      test -f src/img/ouroffice.jpg
      test "$(ls src/img/repairs/ | grep -cP '[^\x00-\x7F]')" -eq 0
      grep -c 'ExpiresByType image/jpeg' src/.htaccess                          # 1
      nomatch grep -n 'NO Content-Encoding' src/.htaccess
      grep -c 'function torin_render_evidence' src/includes/category-page.php   # 1
      grep -c 'file_exists' src/includes/category-page.php                       # >= 1
      grep -c 'loading="lazy"' src/includes/category-page.php                    # 1
      grep -c 'decoding="async"' src/includes/category-page.php                  # 1
      nomatch grep -rn '__DIR__' src/includes/category-page.php
      grep -c 'grid-template-columns: *repeat(2, *100px)' src/css/components.css # 1
      nomatch grep -n 'auto-fit' src/css/components.css
      # CSS transfer budget (UI-SPEC §CSS Budget): <= 20 KB gzipped for production CSS
      T=$(cat src/css/base.css src/css/layout.css src/css/components.css src/css/no-js.css | gzip -9 | wc -c); echo "gzipped CSS: $T"; test "$T" -le 20480
      # LIVE
      scripts/deploy-new.sh .htaccess includes/category-page.php css/components.css zalivane-technosti.html
      for f in zalivane1 zalivane2 zalivane3; do
        scripts/deploy-new.sh "img/repairs/$f.jpg"
        curl -sf -o /dev/null -w "$f %{http_code}\n" "https://torin.bg/new/img/repairs/$f.jpg"
      done
      curl -sI https://torin.bg/new/img/repairs/zalivane1.jpg | grep -ci 'expires\|cache-control'   # >= 1
      curl -s https://torin.bg/new/zalivane-technosti.html | grep -c 'class="evidence"'              # 1
      curl -s https://torin.bg/new/zalivane-technosti.html | grep -oc 'img/repairs/'                 # 3
      curl -s https://torin.bg/new/zalivane-technosti.html | grep -c 'Корозия по пътечките на дънна платка след заливане'  # 1
      curl -s https://torin.bg/new/zalivane-technosti.html | grep -oE '<img[^>]*img/repairs/[^>]*>' | grep -c 'width=' # 3
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/img/repairs/` contains at least 38 `.jpg` files and no filename contains a non-ASCII byte.
    - `grep -c 'ExpiresByType image/jpeg' src/.htaccess` returns 1, and the stale no-compression assertion is gone from that file.
    - `grep -c 'function torin_render_evidence' src/includes/category-page.php` returns 1 and the renderer calls `file_exists` per figure.
    - The renderer emits `loading="lazy"` and `decoding="async"` unconditionally on every evidence image.
    - `grep -n 'auto-fit' src/css/components.css` returns no match — evidence columns are explicit at both breakpoints.
    - Total gzipped production CSS (`base` + `layout` + `components` + `no-js`) is at most 20480 bytes.
    - LIVE `zalivane-technosti.html` serves exactly one `class="evidence"` element, exactly three `img/repairs/` references, and every one of those three `<img>` elements carries a `width` attribute.
    - LIVE `img/repairs/zalivane1.jpg` returns 200 and its response carries an explicit caching header.
    - LIVE `zalivane-technosti.html` contains the caption `Корозия по пътечките на дънна платка след заливане`.
  </acceptance_criteria>
  <done>The shop's own repair photographs are in the new tree under a caching rule that covers them, and any figure whose file is missing degrades to fewer photographs rather than a broken image.</done>
</task>

<task type="auto">
  <name>Task 3: DIFF-02 and DIFF-03 homepage sections, in the contracted section order</name>
  <precondition>`filezilla-server-data.xml` is present at the repo root and Brave is installed, so the live and rendered checks can actually run. Halt and report rather than recording an unrun check as passed.</precondition>
  <files>src/index.html, scripts/probes/trust-signals.js</files>
  <read_first>
    - `src/index.html` in full — the four existing `<section class="section">` blocks, the `.selftest` block at 150-161 which is the smallest complete example the two new sections copy, and the current tint assignment on each.
    - `.planning/phases/03-content-trust-signal-build-out/03-UI-SPEC.md` §Section order and tint alternation — the required final homepage order is an eight-row table and it reassigns the tint on three sections that already exist.
    - `.planning/phases/03-content-trust-signal-build-out/03-UI-SPEC.md` §Copywriting Contract — the exact headings and link labels for both new sections.
    - `site-current/index.html` lines 593-640 — the three BGA tiers in the shop's own Bulgarian (dozapoyavane, reboulding, chipset replacement). Condense; do not translate back from any English summary.
    - `site-current/index.html` lines 650-660 — the battery-regeneration positioning, including that the shop stopped importing new batteries and the Panasonic-cell claim. The source misspells two words in that passage (the noun for regeneration and the adjective for Japanese); the ported copy must carry the correct spellings `регенерация` and `японски`.
    - `site-current/profilaktika-laptop.html` lines 398 and 411 — the German infrared reflow machine, the AMTECH flux, the up-to-10-degree lower operating temperatures and the 90% durability rate, all in the shop's own words. These are the ceiling on what may be claimed.
    - `src/zalivane-technosti.html` as written by plan 03-01 — the homepage DIFF-03 block must not duplicate a paragraph from it.
    - `scripts/render-check.sh` and one existing probe under `scripts/probes/` for the module contract.
  </read_first>
  <action>
**1. Reorder and re-tint the homepage.** The required final order is: hero (dark) → category grid
(tint) → brand row (plain) → catch-all disclosure (tint) → DIFF-03 section (plain) → DIFF-02 section
(tint) → self-diagnostic (plain) → CTA with the rating badge (tint). Three existing sections change
tint state as a consequence: the catch-all, the self-diagnostic and the CTA. Move nothing above the
category grid — the above-the-fold arithmetic depends on card 1's bottom edge staying where it is.

**2. Add the DIFF-03 section**, `<h2>Какво можем, което другите отказват</h2>`, one or two paragraphs
condensed from the shop's own copy: the three board-level tiers, the German infrared reflow machine
whose heat source is outside the visible spectrum, the AMTECH flux, the up-to-10-degree lower
operating temperatures achieved by reinforcing the cooling system, and the 90% durability rate. Claim
nothing the source does not state. Follow with an evidence strip of three photographs —
`profilaktika7.jpg`, `profilaktika3.jpg` and one showing hot-air-gun damage — captioned to describe
what each proves, including `Инфрачервена станция за реболинг на BGA чипове` and
`Повреда от нагряване с пистолет за горещ въздух`. Close with a link labelled
`Ремонт на дънни платки` to `zalivane-technosti.html`. This block is a condensed summary that hands
off to the depth page; it must not restate a paragraph that page already carries.

**3. Add the DIFF-02 section**, `<h2>Регенерация на батерии за лаптоп</h2>`, one or two paragraphs
carrying the positioning the shop actually holds: it stopped importing new batteries because of
low-quality cells, so it offers full regeneration instead; cells are spot-welded with HILUMIN strip
rather than soldered, because any thermal intervention damages the cell chemistry; and the cells are
Japanese Panasonic. Follow with an evidence strip using `baterry.jpg` and `baterry2.jpg`, and a link
labelled `Как работи регенерацията` to `za-bateriite.html`. This section is what moves DIFF-02 from
the folded catch-all Phase 2 left it in to a distinct, prominent surface (D3-11) — the corresponding
catch-all disclosure entry stays, because it serves a visitor searching by symptom, but it is no
longer the only place the story appears.

Both sections use the existing section markup contract: `<section class="section [section--tint]">`
→ `.container` → eyebrow span → `<h2>` → content. Both use `index.html`'s own inline escaping style,
not the template's helper — do not introduce a third escaping style on this page.

**4. Write `scripts/probes/trust-signals.js`** following the existing probe module contract, and run
it against the deployed homepage at 360x640 and 1440x900. It must return: the ordered list of
`<main>` sections with their tint state and their `<h2>` text; the count of adjacent tinted pairs;
`scrollWidth` versus `innerWidth`; the number of rendered rows the brand row occupies (distinct
`offsetTop` values across `.brand-row__item`); the computed background colour and box height of
`.rating-badge` if present; and the rendered box size of every `.evidence img`. Record both JSON
results in the SUMMARY.
  </action>
  <verify>
    <automated>
      set -e
      # `! cmd` does NOT trip set -e (POSIX suppresses errexit for inverted status), so every
      # negative assertion below runs through nomatch, which checks the status and halts.
      nomatch() { if "$@"; then echo "GATE FAILED - unexpected match: $*" >&2; exit 1; fi; }
      grep -c 'Какво можем, което другите отказват' src/index.html   # 1
      grep -c 'Регенерация на батерии за лаптоп' src/index.html      # 1
      grep -c 'Ремонт на дънни платки' src/index.html                # 1
      grep -c 'Как работи регенерацията' src/index.html              # 1
      grep -c 'регенерация' src/index.html                          # >= 1 (correct spelling shipped)
      grep -c 'японски' src/index.html                              # >= 1 (correct spelling shipped)
      nomatch grep -rniE 'smartbattery' src/
      nomatch grep -rnE ' on[a-z]+=|javascript:' src/index.html
      # this page uses its own inline escaping style — the shared helper must not appear
      nomatch grep -n 'torin_esc(' src/index.html
      test "$(grep -c '<section class="section' src/index.html)" -ge 6
      # LIVE
      scripts/deploy-new.sh index.html
      for f in profilaktika7 profilaktika3 baterry baterry2; do scripts/deploy-new.sh "img/repairs/$f.jpg" 2>/dev/null || scripts/deploy-new.sh "img/repairs/$f.jpg"; done
      curl -s https://torin.bg/new/index.html | grep -c 'Какво можем, което другите отказват'   # 1
      curl -s https://torin.bg/new/index.html | grep -c 'Регенерация на батерии за лаптоп'      # 1
      curl -s https://torin.bg/new/index.html | grep -c 'class="evidence"'                       # 2
      test "$(curl -s https://torin.bg/new/index.html | grep -c '<h1')" -eq 1
      test "$(curl -s https://torin.bg/new/index.html | grep -cE '<h[23][^>]*>[[:space:]]*</h[23]>')" -eq 0
      scripts/render-check.sh scripts/probes/trust-signals.js https://torin.bg/new/index.html 360 640 | tee /tmp/trust-360.json
      scripts/render-check.sh scripts/probes/trust-signals.js https://torin.bg/new/index.html 1440 900 | tee /tmp/trust-1440.json
      node -e 'const r=require("/tmp/trust-360.json");if(r.adjacentTintedPairs!==0)throw new Error("adjacent tinted bands");if(r.scrollWidth>r.innerWidth)throw new Error("horizontal scroll at 360px");if(r.brandRowRows<2)throw new Error("brand row did not wrap at 360px: "+r.brandRowRows);if(r.evidenceBoxes.some(b=>Math.round(b.w)!==100||Math.round(b.h)!==100))throw new Error("evidence box not 100x100")'
      node -e 'const r=require("/tmp/trust-1440.json");if(r.adjacentTintedPairs!==0)throw new Error("adjacent tinted bands at desktop");if(r.sectionOrder.length<8)throw new Error("section count "+r.sectionOrder.length)'
    </automated>
    <human-check>Load https://torin.bg/new/index.html on a phone. The brand row reads as a designed row of peers rather than a comma list, the disclaimer under it is legible rather than fine print, and both differentiator sections read as distinct surfaces rather than more body copy.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/index.html` contains the exact headings `Какво можем, което другите отказват` and `Регенерация на батерии за лаптоп`, and the link labels `Ремонт на дънни платки` and `Как работи регенерацията`.
    - `grep -c 'регенерация' src/index.html` and `grep -c 'японски' src/index.html` each return at least 1 — the two words the source misspells ship correctly spelled.
    - `grep -c 'torin_esc(' src/index.html` returns 0 — the page keeps its own inline escaping style and no third style is introduced.
    - `grep -rnE ' on[a-z]+=|javascript:' src/index.html` returns no match.
    - LIVE `index.html` serves exactly two `class="evidence"` elements, exactly one `<h1`, and zero empty `<h2>`/`<h3>` elements.
    - The 360x640 probe reports `adjacentTintedPairs` of 0, `scrollWidth` no greater than `innerWidth`, `brandRowRows` of at least 2 (the row genuinely wraps at 360px rather than overflowing), and every `evidenceBoxes` entry measuring 100x100.
    - The 1440x900 probe reports `adjacentTintedPairs` of 0 and a `sectionOrder` of at least 8 entries matching the UI-SPEC order table.
    - The SUMMARY records both probe JSON results verbatim.
  </acceptance_criteria>
  <done>The homepage carries all four surfaces in the contracted order, and the alternation, wrapping and evidence sizing are proven in a real rendering engine.</done>
</task>

</tasks>

## Artifacts this phase produces

New symbols created by this plan:

- **New files:** `src/includes/brand-row.php`, `src/includes/rating-badge.php`, `scripts/probes/trust-signals.js`
- **New directory:** `src/img/repairs/` (plus `src/img/ouroffice.jpg`)
- **New PHP functions:** `torin_render_brand_row()`, `torin_render_rating_badge()`, `torin_render_evidence()`
- **New icon case:** `star` in `src/includes/icons.php` (the sixteenth)
- **New `$site` keys:** `brands`, `gbp_rating`, `gbp_reviews`, `gbp_url`
- **New `$page` key:** `evidence` (entries: `file`, `w`, `h`, `caption`)
- **New CSS selectors:** `.brands`, `.brand-row`, `.brand-row__item`, `.brand-row__item--more`, `.brand-row__note`, `.rating-badge`, `.rating-badge__star`, `.rating-badge__score`, `.rating-badge__label`, `.evidence`
- **New `.htaccess` directive:** `ExpiresByType image/jpeg`

## Probe rows discharged by this plan

| Source | Row | Disposition |
|---|---|---|
| Edge probe | TRUST-01 / adjacency | covered → truth (chip gap and no duplicate stored entry) |
| Edge probe | TRUST-01 / empty | covered → truth (whole-section guard; one-entry list still renders) |
| Edge probe | TRUST-01 / ordering | covered → truth (stored array order, unsorted, closer always last) |
| Edge probe | TRUST-02 / unclassified | covered → truth (the normative absent state) |
| Edge probe | DIFF-03 / unclassified | **backstop** → structured marker in `must_haves.truths` |
| UI-SPEC §1 | empty, populated, partial, overflow, zero-one-many, long-text | covered by the wrap contract and the section guard; `loading` and `error` carried forward with the UI-SPEC's dismissal reasons (false-positive `media` classification, local array not a request) |
| UI-SPEC §2 | empty, populated, overflow, long-text | covered; `loading` and `error` carried forward dismissed with reason (static anchor, no runtime request) |
| UI-SPEC §4 | empty, loading, error, populated, partial, overflow, zero-one-many, long-text | covered by the existence check, the intrinsic-attribute rule, the explicit columns and the three-photo cap |
| UI-SPEC §4 | populated *(3x displays)* | **unresolved** — carried below as a flagged planner assumption |

## Flagged planner assumptions

- **UI-SPEC §4 `populated (3x displays)` / RESEARCH A7 — unresolved, not dismissed.** The ported
  photographs are mostly 200px square, giving a comfortable 2x effective density at the contracted
  100 CSS px, but **3x is not covered by these files and cannot be**. No value is invented and no
  larger source exists. The mitigation is structural rather than visual: the fixed-box slot lets
  larger files replace these with zero layout change if the owner ever supplies them
  (OWNER-QUESTIONS #12 and #13, which ask for *new* photography and remain open).
- **Phase success criterion 2 ships unmet by design.** `gbp_url` is empty, so the badge renders as
  its specified absence. This is preferable to publishing an unconfirmed rating on every page, and it
  is the cheapest unblock in the phase: one URL and two numbers read off the profile once. Record it
  in the SUMMARY as an explicit, owner-blocked gap rather than a build failure.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-authored content → served HTML | Brand names, rating figures and captions all cross into markup. |
| visitor browser → Google Business Profile | The badge is the only outbound link this phase adds. |
| repository → served static assets | Roughly forty first-party binaries move into the served tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering | Rating badge external link | low | mitigate | `rel="noopener"` and no `target="_blank"`; the URL is a developer-authored config literal never assembled from a request value. Gate: `grep -c 'rel="noopener"'` returns 1 and `target="_blank"` is absent. |
| T-03-08 | Information disclosure / third-party trust | Reviews display | high | mitigate | No third-party script, no iframe, no API key, no Places call — the badge is a styled static anchor (D3-07, DH-1/DH-2). Gate: no remote script host appears anywhere in `src/`. Reopening this must be gated behind a blocking human checkpoint and treated as an unvetted script running on all pages with access to visitor data. |
| T-03-09 | Spoofing | Structured data and on-page rating claims | high | mitigate | No rating or review property is emitted under any schema type, and no rating figure is published until it is read off the live profile — the empty config values make the absent state the shipped state rather than relying on discipline. Gates: `! grep -riE 'aggregateRating\|"review"' src/includes/jsonld.php` and LIVE `rating-badge` count of 0. |
| T-03-10 | Spoofing | Brand-name row | medium | mitigate | The mandatory disclaimer paragraph states the shop is an independent out-of-warranty service and is not an authorised representative — this is what makes naming the marks referential use rather than an implied-authorisation claim. No figurative mark or logo file is introduced. Gate: the disclaimer literal is present in the partial and in the served HTML. |
| T-03-11 | Denial of service (self-inflicted) | Ported image assets | low | mitigate | Explicit intrinsic dimensions on every image, `loading="lazy"` and `decoding="async"` unconditionally, a hard cap of three per page, and an explicit caching rule added in the same change that lands the files. |
| T-03-12 | Tampering | Non-ASCII asset filenames | low | mitigate | Ported filenames are ASCII only; a percent-encoding-dependent path is a known shared-hosting and FTP hazard. Gate: `! ls src/img/repairs/ \| grep -P '[^\x00-\x7F]'`. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no package in any ecosystem; `03-RESEARCH.md` §Package Legitimacy Audit records the gate as not applicable with zero `[ASSUMED]`/`[SUS]`/`[SLOP]` entries. The only third-party code the phase could plausibly acquire is a reviews widget, which D3-07 forbids and T-03-08 gates. |
</threat_model>

<verification>
- LIVE homepage serves the brand row with seven brands plus the closer and the disclaimer, two
  evidence strips, both differentiator headings, one `<h1>`, and zero empty headings.
- LIVE homepage serves zero `rating-badge` occurrences and zero `sameAs` occurrences while the
  profile keys are empty — the absent state is real, not asserted.
- The brand row appears on every service page and on none of the six legal/utility pages.
- Total gzipped production CSS is at most 20 KB.
- Both rendered probes report zero adjacent tinted bands and no horizontal scroll at 360px.
</verification>

<success_criteria>
- Phase success criterion 1 (brand row) is met.
- Phase success criterion 2 (rating badge) is built and correct, and is recorded as owner-blocked
  rather than faked.
- Phase success criterion 4 is met for the battery-regeneration story and the chip-level repair
  expertise; the self-diagnostic half is met by plan 03-04.
- No third-party runtime code, no secret and no API key enters the tree.
</success_criteria>

<output>
Create `.planning/phases/03-content-trust-signal-build-out/03-02-SUMMARY.md` when done.

Record: the ported photo inventory with per-file sizes and the total; both probe JSON results
verbatim; the gzipped CSS total against the 20 KB ceiling; and — prominently — that filling
`gbp_rating`, `gbp_reviews` and `gbp_url` is a one-line unblock for phase success criterion 2, with
the exact three key names a reader needs.
</output>
