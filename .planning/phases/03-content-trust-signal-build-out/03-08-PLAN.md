---
phase: 03-content-trust-signal-build-out
plan: 08
type: execute
wave: 4
depends_on: ["03-03", "03-04"]
files_modified:
  - src/mehanichni-problemi.html
  - src/optimizatsiq.html
  - src/pregryavane-ohlazhdane.html
  - src/includes/categories.php
autonomous: true
requirements: [SEO-01, TRUST-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A visitor on the breakage-and-mechanical-damage page sees real depth — what is repaired, how, the warranty, the process and answers to real objections — instead of two lines of sub-services"
    - "A visitor on the optimisation page is told plainly which of the four causes of a slow machine applies to them, including the one where their data is at risk"
    - "A visitor with an overheating laptop reaches a published category-5 page rather than a homepage anchor"
    - "The overheating page cross-lists the profilaktika service as a link to the one existing page rather than duplicating its copy"
    - "Five of the six categories are published and each resolves to a page that returns 200; the sixth stays gated on its open owner question"
    - "Each of the three pages shows the shared warranty summary on the page itself (TRUST-03)"
  artifacts:
    - path: "src/mehanichni-problemi.html"
      provides: "Category 1 deepened to the Definition of Done"
      min_lines: 80
    - path: "src/optimizatsiq.html"
      provides: "Category 3 deepened to the Definition of Done, including the data-loss angle"
      min_lines: 80
    - path: "src/pregryavane-ohlazhdane.html"
      provides: "Category 5 published, with profilaktika cross-listed as a link"
      min_lines: 80
    - path: "src/includes/categories.php"
      provides: "kat-5 published; kat-6 still gated"
  key_links:
    - from: "src/pregryavane-ohlazhdane.html"
      to: "src/profilaktika-laptop.html"
      via: "a fixes entry carrying an href, so the service is cross-listed as a link rather than duplicated copy"
      pattern: "profilaktika-laptop.html"
    - from: "src/mehanichni-problemi.html"
      to: "src/smyana-na-panti.html"
      via: "related links into the category-2 child pages, resolved through torin_service_href()"
      pattern: "torin_service_href"
    - from: "src/includes/categories.php"
      to: "src/pregryavane-ohlazhdane.html"
      via: "kat-5 published flag flipped so the card and nav route to the page"
      pattern: "'published' *=> *true"
  prohibitions:
    - "MUST NOT duplicate the profilaktika page's body copy onto the overheating page — the service is cross-listed as a link so there is one URL, one canonical page and no duplicate-content exposure"
    - "MUST NOT publish the category-5 page while its file does not exist, and MUST NOT flip the category-6 flag under any circumstances in this plan"
    - "MUST NOT state or imply a price, a turnaround time or a guaranteed outcome on any of these three pages"
---

<objective>
Bring the last three category pages up to the Definition of Done: deepen the two that shipped
deliberately thin in Phase 2, and publish the one that has never had a page.

Category 1 currently carries two sub-service lines and nothing else. Category 3 is the weakest of the
six and needs the most new writing — its source copy names four causes of a slow machine, one of
which is a failing drive, which is the one case where the visitor's data is at risk and the advice is
genuinely urgent rather than commercial. Category 5 has no file at all; that is the D-23 gate working
as designed, and this plan is what closes it. Its thinness was resolved in Phase 2 by cross-listing
profilaktika into it — as a link to the one existing page, never as duplicated copy, because one URL
with one canonical page and an internal link from a second topical context is the entire benefit.

Purpose: five of six categories published with real depth, and the sixth honestly gated.
Output: three finished category pages and one publish flip.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-content-trust-signal-build-out/03-CONTEXT.md
@.planning/phases/03-content-trust-signal-build-out/03-RESEARCH.md
@.planning/phases/03-content-trust-signal-build-out/03-PATTERNS.md
@.planning/phases/03-content-trust-signal-build-out/03-UI-SPEC.md
@.planning/phases/03-content-trust-signal-build-out/03-01-SUMMARY.md
@.planning/phases/03-content-trust-signal-build-out/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Category 1 — «Счупвания и механични повреди», deepened</name>
  <files>src/mehanichni-problemi.html</files>
  <read_first>
    - `src/mehanichni-problemi.html` — the current thin page. It carries `cat_id` and a two-entry fixes list and nothing else; everything else is added here.
    - `src/zalivane-technosti.html` as written by plan 03-01 — the reference for a category page authored to the full Definition of Done, including how a `blocks` entry, `crumbs` and an `h1` override are shaped, and how the title becomes a literal rather than being derived from the record.
    - `site-current/mehanichni-problemi.html` — read lines 5-20 and 110-165. The opening covers what a drop does: a head crash on a spinning drive, how fragile an LCD panel is, the lid and hinge mechanism, and the keyboard. Line 115 explains that lid-mechanism breakage comes as often from poor design and a loosening hinge as from a drop. Lines 157-159 carry the gradual-damage argument for jacks and connectors.
    - `src/includes/services.php` — the five category-2 child records, several of which are the natural related links from this page.
    - `.planning/phases/03-content-trust-signal-build-out/03-RESEARCH.md` §SEO Metadata Architecture — the measured title candidate for this page.
  </read_first>
  <action>
Deepen `src/mehanichni-problemi.html` to the D3-14 Definition of Done, 600-1000 Bulgarian words,
keeping `'cat_id' => 'kat-1'` so the display name and symptom line stay read from the record.

Replace the derived title with a literal — decoupling the title from the heading is the prerequisite
for a keyword-first title, and this page is one of the three that still derives it.

- `$torin_title` = `Ремонт на счупен лаптоп София · Торин`
- `$torin_desc` — 120-140 characters ending in a call to action, with the call inside the first 95.
- `'h1'` = `Ремонт на счупен лаптоп в София` (30 characters)
- `'crumbs'` = Начало → the record name. Assign `$torin_crumbs` from the same data before the footer
  include.
- `'intro'` — one paragraph: a drop rarely breaks only the thing you can see.
- `'fixes'` — extend the existing two entries to five or six: casing and chassis repair, hinge and lid
  mechanism, display panel replacement, keyboard replacement, connector and jack repair, data recovery
  from a drive damaged by impact. Where an entry maps to a category-2 child page, give it an `href`
  resolved through `torin_service_href()` so it renders as a link — no child filename is typed.
- `'blocks'` — two entries:
  1. `prose`, heading `Какво се случва при падане`, two or three paragraphs on the head crash on a
     spinning drive, how little force a display panel tolerates, and the lid and hinge mechanism.
  2. `callout`, heading `Данните са отделен въпрос от ремонта`, one or two paragraphs stating plainly
     that a drive damaged by impact should be assessed before anything is powered on again, with a
     `link` to `test-laptop.html`.
- `'warranty_key'` = `default`.
- `'process'` — 4-6 numbered steps.
- `'faq'` — 3-5 entries, one paragraph each, answering real objections: whether a cracked casing is
  worth repairing, whether the machine can be used until the part arrives, whether an impact-damaged
  drive can be read.
- `'related'` — links to the category-2 hub, `smyana-na-matrica.html` and `test-laptop.html`, every
  href resolved through an accessor rather than typed.

No price, no turnaround time, no guaranteed outcome. Port the strings only.
  </action>
  <verify>
    <automated>
      set -e
      # `! cmd` does NOT trip set -e (POSIX suppresses errexit for inverted status), so every
      # negative assertion below runs through nomatch, which checks the status and halts.
      nomatch() { if "$@"; then echo "GATE FAILED - unexpected match: $*" >&2; exit 1; fi; }
      grep -c "'cat_id' *=> *'kat-1'" src/mehanichni-problemi.html            # 1
      nomatch grep -n "torin_cat\['name'\] *\." src/mehanichni-problemi.html
      nomatch grep -n 'Счупвания и механични повреди' src/mehanichni-problemi.html  # name comes from the record
      grep -c "'h1' *=>" src/mehanichni-problemi.html                         # 1
      grep -c 'torin_crumbs' src/mehanichni-problemi.html                     # >= 1
      grep -c "'warranty_key' *=> *'default'" src/mehanichni-problemi.html    # 1
      grep -c 'torin_service_href' src/mehanichni-problemi.html               # >= 1
      nomatch grep -nE "'(smyana-na-matrica|smyana-na-panti|smyana-na-klaviatura|remont-na-portove|smyana-na-buksa)\.html'" src/mehanichni-problemi.html
      nomatch grep -rniE 'лв\.|лева|евро|EUR|работни дни|гарантираме, че' src/mehanichni-problemi.html
      nomatch grep -rnE ' on[a-z]+=|javascript:|class="(fa |col-md-|mb-[0-9]|font-size-)' src/mehanichni-problemi.html
      nomatch grep -rn '__DIR__' src/mehanichni-problemi.html
      nomatch grep -rnE '(=>[^;]*\]|\[\s*[^]]*=>)' src/mehanichni-problemi.html
      W=$(sed -n "/= array(/,/^);/p" src/mehanichni-problemi.html | tr -cs '[:alpha:][:digit:]' ' ' | wc -w); test "$W" -ge 600
      node -e 'const s=require("fs").readFileSync("src/mehanichni-problemi.html","utf8");if((s.match(/'"'"'/g)||[]).length%2)process.exit(1)'
      # LIVE
      scripts/deploy-new.sh mehanichni-problemi.html
      U=https://torin.bg/new/mehanichni-problemi.html
      curl -sf -o /dev/null -w '%{http_code}\n' "$U"                          # 200
      curl -s "$U" | grep -c 'Ремонт на счупен лаптоп в София'                # 1
      curl -s "$U" | grep -o '<title>[^<]*</title>'
      curl -s "$U" | grep -c 'svc__warranty__term'                            # 1
      curl -s "$U" | grep -c 'class="breadcrumbs"'                            # 1
      test "$(curl -s "$U" | grep -c '<h1')" -eq 1
      test "$(curl -s "$U" | grep -cE '<h[23][^>]*>[[:space:]]*</h[23]>')" -eq 0
      test "$(curl -s "$U" | grep -c '<h2')" -ge 5
    </automated>
  </verify>
  <acceptance_criteria>
    - The page keeps `'cat_id' => 'kat-1'`, and `grep -c 'Счупвания и механични повреди'` on the file returns 0 — the display name is still read from the record.
    - The title is a literal, not derived from the record: `grep -n "torin_cat\['name'\] *\." src/mehanichni-problemi.html` returns no match.
    - `grep -nE "'(smyana-na-matrica|smyana-na-panti|smyana-na-klaviatura|remont-na-portove|smyana-na-buksa)\.html'"` returns no match — every child link resolves through `torin_service_href()`.
    - The page's data array contains at least 600 words.
    - `grep -rniE 'лв\.|лева|евро|EUR|работни дни|гарантираме, че'` returns no match.
    - LIVE returns 200, serves the keyword heading, one warranty term line, one breadcrumb nav, one `<h1`, at least five `<h2>` sections and zero empty headings.
  </acceptance_criteria>
  <done>Category 1 is a real page rather than two lines of sub-services, and its title is decoupled from its heading.</done>
</task>

<task type="auto">
  <name>Task 2: Category 3 — «Оптимизация», deepened</name>
  <files>src/optimizatsiq.html</files>
  <read_first>
    - `src/optimizatsiq.html` — the current thin page and its derived title.
    - `src/zalivane-technosti.html` — the Definition-of-Done reference.
    - `site-current/optimizatsiq.html` — read lines 50-70. The source names four causes of a slow machine: an operating system carrying years of accumulated software, hardware that was never specified for what is now being asked of it, a failing hard drive, and a board fault. The failing-drive passage carries the data-loss argument — the photographs are irreplaceable — and it is the one case where the honest advice is urgent rather than commercial.
    - `src/profilaktika-laptop.html` as written by plan 03-04 — this page cross-links to it; check for overlap so nothing is duplicated.
    - `src/test-laptop.html` as written by plan 03-04 — the self-diagnostic this page routes uncertain visitors to.
    - `.planning/phases/02-design-system-information-architecture/02-CONTEXT.md` — D-28, why profilaktika is cross-listed as a link.
    - `.planning/phases/03-content-trust-signal-build-out/03-RESEARCH.md` §Content Inventory row `optimizatsiq.html` — this is the weakest of the six and needs the most new writing.
  </read_first>
  <action>
Deepen `src/optimizatsiq.html` to the D3-14 Definition of Done, 600-1000 Bulgarian words, keeping
`'cat_id' => 'kat-3'`. Replace the derived title with a literal.

- `$torin_title` = `Оптимизация и ускоряване на лаптоп · Торин`
- `$torin_desc` — 120-140 characters ending in a call to action.
- `'h1'` = `Оптимизация и ускоряване на лаптоп в София` (41 characters)
- `'crumbs'` = Начало → the record name.
- `'intro'` — one paragraph: «бавен е» has four common causes and they need different work, so the
  first job is finding out which one applies.
- `'fixes'` — clean operating-system reinstall with data preserved, solid-state drive replacement and
  migration, memory upgrade, removal of accumulated startup software, cooling service where thermal
  throttling is the real cause, board diagnosis where none of the above is.
- `'blocks'` — two entries:
  1. `prose`, heading `Четирите причини лаптопът да е бавен`, three or four paragraphs, one per
     cause, each ending with what the corresponding work actually is.
  2. `callout`, `tone` `urgent`, heading `Ако дискът отказва, данните са първи`, one or two
     paragraphs stating plainly that a drive showing symptoms should be imaged before anything else
     is attempted, that continuing to use the machine reduces what can be recovered, and that
     photographs and documents are what people actually lose. Include a `link` to
     `test-laptop.html`. This is the second and last urgent-tone block in the phase and it is earned:
     the heading itself states the urgency, so colour is not the sole carrier.
- `'warranty_key'` = `default`.
- `'process'` — 4-6 numbered steps opening with diagnosis rather than with a recommendation.
- `'faq'` — 3-5 entries, one paragraph each: whether data survives a reinstall, whether a solid-state
  drive is worth it on an older machine, whether more memory helps, whether the machine is worth
  keeping at all. Answer the last one honestly, including the case where it is not.
- `'related'` — a link to `profilaktika-laptop.html` and to the category-5 page via
  `torin_category_href()`, plus `test-laptop.html`.

Do not duplicate the profilaktika page's copy here — cross-list it as a link. No price, no turnaround
time, no promised speed improvement.
  </action>
  <verify>
    <automated>
      set -e
      # `! cmd` does NOT trip set -e (POSIX suppresses errexit for inverted status), so every
      # negative assertion below runs through nomatch, which checks the status and halts.
      nomatch() { if "$@"; then echo "GATE FAILED - unexpected match: $*" >&2; exit 1; fi; }
      grep -c "'cat_id' *=> *'kat-3'" src/optimizatsiq.html                   # 1
      nomatch grep -n "torin_cat\['name'\] *\." src/optimizatsiq.html
      grep -c 'Оптимизация' src/optimizatsiq.html                             # >= 1 (in the h1/title literals, not as the record name)
      grep -c "'h1' *=>" src/optimizatsiq.html                                # 1
      grep -c "'tone' *=> *'urgent'" src/optimizatsiq.html                    # 1
      grep -c 'profilaktika-laptop.html' src/optimizatsiq.html                # >= 1
      grep -c 'torin_category_href' src/optimizatsiq.html                     # >= 1
      grep -c "'warranty_key' *=> *'default'" src/optimizatsiq.html           # 1
      nomatch grep -rniE 'лв\.|лева|евро|EUR|работни дни|пъти по-бърз' src/optimizatsiq.html
      nomatch grep -rnE ' on[a-z]+=|javascript:|class="(fa |col-md-|mb-[0-9]|font-size-)' src/optimizatsiq.html
      nomatch grep -rn '__DIR__' src/optimizatsiq.html
      nomatch grep -rnE '(=>[^;]*\]|\[\s*[^]]*=>)' src/optimizatsiq.html
      W=$(sed -n "/= array(/,/^);/p" src/optimizatsiq.html | tr -cs '[:alpha:][:digit:]' ' ' | wc -w); test "$W" -ge 600
      node -e 'const s=require("fs").readFileSync("src/optimizatsiq.html","utf8");if((s.match(/'"'"'/g)||[]).length%2)process.exit(1)'
      # no paragraph duplicated from the profilaktika page
      node -e 'const fs=require("fs");const strip=s=>s.replace(/<[^>]*>/g," ").replace(/\s+/g," ");const a=strip(fs.readFileSync("src/profilaktika-laptop.html","utf8")).split(/[.!?]\s/).map(s=>s.trim()).filter(s=>s.length>90);const b=strip(fs.readFileSync("src/optimizatsiq.html","utf8"));const dup=a.filter(s=>b.includes(s));if(dup.length){console.error(dup);process.exit(1)}'
      # LIVE
      scripts/deploy-new.sh optimizatsiq.html
      U=https://torin.bg/new/optimizatsiq.html
      curl -sf -o /dev/null -w '%{http_code}\n' "$U"                          # 200
      curl -s "$U" | grep -c 'svc__block--urgent'                             # 1
      curl -s "$U" | grep -c 'svc__warranty__term'                            # 1
      test "$(curl -s "$U" | grep -c '<h1')" -eq 1
      test "$(curl -s "$U" | grep -cE '<h[23][^>]*>[[:space:]]*</h[23]>')" -eq 0
      test "$(curl -s "$U" | grep -c '<h2')" -ge 5
    </automated>
  </verify>
  <acceptance_criteria>
    - The page keeps `'cat_id' => 'kat-3'` and its title is a literal rather than derived from the record.
    - Exactly one urgent-tone block is present, and it is the data-loss block.
    - `grep -c 'profilaktika-laptop.html'` returns at least 1, and no sentence longer than 90 characters is shared verbatim with that page — the service is cross-listed, not duplicated.
    - The page's data array contains at least 600 words.
    - `grep -rniE 'лв\.|лева|евро|EUR|работни дни|пъти по-бърз'` returns no match — no price and no promised speed improvement.
    - LIVE returns 200 with one urgent block, one warranty term line, one `<h1`, at least five `<h2>` sections and zero empty headings.
  </acceptance_criteria>
  <done>The weakest of the six categories is a real page, and the one case where the visitor's data is at risk is stated plainly rather than sold around.</done>
</task>

<task type="auto">
  <name>Task 3: Publish category 5 — «Прегряване и охлаждане»</name>
  <reversibility rating="one-way">Publishing puts a new slug into the index; reverting it afterwards needs a redirect and forfeits signal twice. The slug is already locked in the category record and the decision is D3-04's, so it is recorded rather than re-asked.</reversibility>
  <precondition>`filezilla-server-data.xml` is present at the repo root so the live sweep at the end of this task can run. Halt and report rather than recording an unrun check as passed.</precondition>
  <files>src/pregryavane-ohlazhdane.html, src/includes/categories.php</files>
  <read_first>
    - `src/includes/categories.php` — the kat-5 record. Its `page` value is already locked and must not change; only the publish flag flips. Read the D-40 comment on that record explaining why the category is named for the symptom rather than the fix, and do not reopen that wording.
    - `src/zalivane-technosti.html` — the Definition-of-Done reference and the page skeleton to copy structurally.
    - `src/profilaktika-laptop.html` as written by plan 03-04 — the page this category cross-lists to as a link. Read it so nothing is duplicated here.
    - `site-current/warrently.html` lines 118-124 — the cooling-system reasoning in the shop's own words: every successfully repaired machine gets a cooling correction, the whole system is cleaned and washed, soft thermal pads are replaced with aluminium or copper plates that do not age, and cooling capacity declines year on year as the margin the manufacturer designed in is used up.
    - `.planning/phases/02-design-system-information-architecture/02-CONTEXT.md` — D-28 and D-29: profilaktika cross-lists into this category as a link, which is what resolved its thinness, and the category is named for the symptom deliberately.
  </read_first>
  <action>
Create `src/pregryavane-ohlazhdane.html` — the filename is already locked in the kat-5 record and must
not be changed — to the D3-14 Definition of Done, 600-1000 Bulgarian words, with
`'cat_id' => 'kat-5'`.

- `$torin_title` = `Прегряване на лаптоп и профилактика · Торин`
- `$torin_desc` — 120-140 characters ending in a call to action.
- `'h1'` = `Прегряване на лаптоп и охлаждане в София` (39 characters)
- `'crumbs'` = Начало → the record name.
- `'intro'` — one paragraph: a machine that runs hot, runs loud or shuts itself down is telling you
  the cooling system is no longer keeping up, and that is a repairable condition rather than an age.
- `'fixes'` — fan replacement, cooling-system cleaning and washing, thermal-paste replacement,
  replacement of soft thermal pads with aluminium or copper plates, heat-pipe assessment, and
  **профилактика на лаптоп as an entry carrying an `href` to `profilaktika-laptop.html`** so it
  renders as a link. That cross-listing is what resolves this category's thinness, and it must be one
  URL with one canonical page rather than copied body text.
- `'blocks'` — two entries:
  1. `prose`, heading `Защо охлаждането отслабва с годините`, two or three paragraphs on how the
     manufacturer designs in a cooling margin, how that margin is consumed as the system clogs and
     ages, and why a machine that coped when it was half-blocked when new no longer does.
  2. `steps`, heading `Какво да направите, докато чакате ремонт`, four items: do not run the machine
     on a bed, a lap or a sofa where the intakes are blocked; stop heavy work when the fan runs
     continuously; do not open the cooling system with a vacuum cleaner; call if the machine shuts
     itself down under load. No urgent tone — the phase uses that treatment twice and both are
     already earned elsewhere.
- `'warranty_key'` = `default`.
- `'process'` — 4-6 numbered steps, including that a machine that has had a board repair also gets a
  cooling correction.
- `'faq'` — 3-5 entries, one paragraph each: how often profilaktika is needed, whether a cooling pad
  helps, whether shutting down under load always means a board fault, whether the noise can be fixed
  without replacing the fan.
- `'related'` — links to `profilaktika-laptop.html`, `zalivane-technosti.html` and the category-3 page
  via `torin_category_href()`.

Then flip the kat-5 record's publish flag to `true` in the same change that lands the file, with a
one-line comment naming the plan that published it. Do **not** touch the kat-6 flag — OWNER-QUESTIONS
#3 is launch-blocking and that gate stays closed regardless of how the drafted page reads.

Finish with a sweep: every one of the six category records must either resolve to a page that returns
200 or route to its homepage anchor, and exactly one must still be routing to an anchor.
  </action>
  <verify>
    <automated>
      set -e
      # `! cmd` does NOT trip set -e (POSIX suppresses errexit for inverted status), so every
      # negative assertion below runs through nomatch, which checks the status and halts.
      nomatch() { if "$@"; then echo "GATE FAILED - unexpected match: $*" >&2; exit 1; fi; }
      test -f src/pregryavane-ohlazhdane.html
      grep -c "'cat_id' *=> *'kat-5'" src/pregryavane-ohlazhdane.html         # 1
      nomatch grep -n 'Прегряване и охлаждане' src/pregryavane-ohlazhdane.html      # name comes from the record
      grep -c 'profilaktika-laptop.html' src/pregryavane-ohlazhdane.html      # >= 1
      grep -c "'warranty_key' *=> *'default'" src/pregryavane-ohlazhdane.html # 1
      nomatch grep -n "'tone' *=> *'urgent'" src/pregryavane-ohlazhdane.html
      nomatch grep -rniE 'лв\.|лева|евро|EUR|работни дни' src/pregryavane-ohlazhdane.html
      nomatch grep -rnE ' on[a-z]+=|javascript:|class="(fa |col-md-|mb-[0-9]|font-size-)' src/pregryavane-ohlazhdane.html
      nomatch grep -rn '__DIR__' src/pregryavane-ohlazhdane.html
      nomatch grep -rnE '(=>[^;]*\]|\[\s*[^]]*=>)' src/pregryavane-ohlazhdane.html
      W=$(sed -n "/= array(/,/^);/p" src/pregryavane-ohlazhdane.html | tr -cs '[:alpha:][:digit:]' ' ' | wc -w); test "$W" -ge 600
      node -e 'const s=require("fs").readFileSync("src/pregryavane-ohlazhdane.html","utf8");if((s.match(/'"'"'/g)||[]).length%2)process.exit(1)'
      # no paragraph duplicated from the profilaktika page
      node -e 'const fs=require("fs");const strip=s=>s.replace(/<[^>]*>/g," ").replace(/\s+/g," ");const a=strip(fs.readFileSync("src/profilaktika-laptop.html","utf8")).split(/[.!?]\s/).map(s=>s.trim()).filter(s=>s.length>90);const b=strip(fs.readFileSync("src/pregryavane-ohlazhdane.html","utf8"));const dup=a.filter(s=>b.includes(s));if(dup.length){console.error(dup);process.exit(1)}'
      # exactly five categories published; kat-6 still gated
      test "$(grep -c "'published' *=> *true" src/includes/categories.php)" -eq 5
      node -e 'const s=require("fs").readFileSync("src/includes/categories.php","utf8");const i=s.indexOf("kat-6");if(!/'"'"'published'"'"' *=> *false/.test(s.slice(i)))process.exit(1)'
      for p in $(grep -oP "(?<='page' *=> *')[^']*" src/includes/categories.php); do test -f "src/$p"; done
      # LIVE
      scripts/deploy-new.sh includes/categories.php pregryavane-ohlazhdane.html
      U=https://torin.bg/new/pregryavane-ohlazhdane.html
      curl -sf -o /dev/null -w '%{http_code}\n' "$U"                          # 200
      curl -s "$U" | grep -c 'profilaktika-laptop.html'                       # >= 1
      curl -s "$U" | grep -c 'svc__warranty__term'                            # 1
      test "$(curl -s "$U" | grep -c '<h1')" -eq 1
      test "$(curl -s "$U" | grep -cE '<h[23][^>]*>[[:space:]]*</h[23]>')" -eq 0
      test "$(curl -s "$U" | grep -c '<h2')" -ge 5
      # every category card resolves; exactly one still routes to an anchor
      H=$(curl -s https://torin.bg/new/index.html)
      test "$(printf '%s' "$H" | grep -oc 'index.html#kat-')" -eq 1
      for p in mehanichni-problemi ekran-klaviatura-portove optimizatsiq zalivane-technosti pregryavane-ohlazhdane; do
        printf '%s' "$H" | grep -c "$p.html"                                   # >= 1 each
        curl -sf -o /dev/null -w "$p %{http_code}\n" "https://torin.bg/new/$p.html"
      done
    </automated>
    <human-check>Open the homepage card grid on a phone and tap every one of the six cards. Five open a real category page; the sixth scrolls to its own homepage anchor, which is the gate working as designed.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/pregryavane-ohlazhdane.html` exists, sets `'cat_id' => 'kat-5'`, and `grep -c 'Прегряване и охлаждане'` on the file returns 0.
    - The page links to `profilaktika-laptop.html` and shares no sentence longer than 90 characters with it — cross-listed as a link, not duplicated.
    - The page carries no urgent-tone block.
    - `grep -c "'published' *=> *true" src/includes/categories.php` returns exactly 5, kat-6 is still `false`, and every `page` value in that file names a file that exists on disk.
    - The page's data array contains at least 600 words.
    - LIVE returns 200 with one warranty term line, one `<h1`, at least five `<h2>` sections and zero empty headings.
    - LIVE `index.html` contains exactly one `index.html#kat-` anchor and links to the other five category pages, each of which returns 200.
  </acceptance_criteria>
  <done>Five of six categories are published with real depth and each resolves to a live page; the sixth is gated on its open owner question and nothing else.</done>
</task>

</tasks>

## Artifacts this phase produces

New symbols created by this plan:

- **New page file:** `src/pregryavane-ohlazhdane.html`
- **New page content (files already existed, thin):** `src/mehanichni-problemi.html`, `src/optimizatsiq.html`
- **Newly published record:** `kat-5` in `categories.php`
- **New PHP functions, CSS selectors or config keys:** none

## Probe rows discharged by this plan

| Source | Row | Disposition |
|---|---|---|
| Edge probe | SEO-01 / ordering | the two remaining record-derived titles become keyword-first literals here; the site-wide ordering assertion is plan 03-09's |
| UI-SPEC §5 | populated, `tone: urgent` | the urgent tone reaches its second and final earned use; every other block on these pages uses a neutral variant |
| RESEARCH | D-28 cross-listing | covered → prohibition and gated by the verbatim-duplication check between the overheating page and the profilaktika page |

## Flagged planner assumptions

- **Category 6 remains the only gated category after this plan.** That is the intended end state for
  this phase: OWNER-QUESTIONS #3 is launch-blocking, and flipping that one flag is the whole remaining
  action once it is answered.
- **The category-3 page required the most new writing of the six.** Its source copy is thin, so more
  of this page is newly authored than ported. Nothing in it asserts a capability the shop has not
  claimed, but it is the page where a reviewer should look hardest for drift from the shop's voice.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer-authored content → served HTML | All page copy crosses here. |
| publish gate → visitor-facing links | Flipping kat-5 makes a URL reachable from the card grid and the navigation at once. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-38 | Denial of service | Publish gate opened ahead of a file | high | mitigate | The kat-5 flag flips in the same change that lands the file, and the gate asserts that every `page` value in the records file names a file that exists on disk, followed by a live 200 sweep over all five published category URLs. |
| T-03-39 | Spoofing | Category-6 promoted before its scope is confirmed | high | mitigate | This plan does not touch the kat-6 flag, and the gate asserts it is still false plus that exactly one homepage anchor remains in the card grid. |
| T-03-40 | Repudiation | Duplicate-content exposure from cross-listing | medium | mitigate | Profilaktika is cross-listed as a link to the one existing page; the verbatim-duplication check between the two pages is what proves it. |
| T-03-41 | Repudiation | Copy implying commitments the shop has not made | medium | mitigate | Gate: `! grep -rniE 'лв\.\|лева\|евро\|EUR\|работни дни\|пъти по-бърз\|гарантираме, че'` across all three pages. |
| T-03-42 | Injection | Legacy markup carried across with the copy | medium | mitigate | Port strings, not markup. Gate: `! grep -rnE ' on[a-z]+=\|javascript:\|class="(fa \|col-md-\|mb-[0-9]\|font-size-)'`. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed; `03-RESEARCH.md` §Package Legitimacy Audit records the gate as not applicable. |
</threat_model>

<verification>
- All three pages return 200 live, each with one `<h1>`, at least five `<h2>` sections, a warranty
  term line and zero empty headings.
- Exactly five category records are published, every published `page` value names an existing file,
  and kat-6 is still gated.
- The homepage card grid contains exactly one homepage anchor and links to five real pages.
- Neither the optimisation page nor the overheating page shares a paragraph with the profilaktika page.
</verification>

<success_criteria>
- Every category the owner named has real content, and the one that does not is gated on a question
  rather than on effort.
- The three record-derived titles are now literals, which is what makes plan 03-09's keyword-first
  pass possible.
</success_criteria>

<output>
Create `.planning/phases/03-content-trust-signal-build-out/03-08-SUMMARY.md` when done.

Record the word count and final title for each of the three pages, and state plainly that kat-6 is
the single remaining gated category and that flipping its flag is the whole action once
OWNER-QUESTIONS #3 is answered.
</output>
